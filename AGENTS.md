# AGENTS.md

This file provides guidance to coding agents (Codex and others) when working with code in this repository.

Last verified against the tree on 2026-09-07 (audit + a real `next build`). If something here contradicts the code, the code wins — and fix this file.

## Project

Sunnsteel is a fitness/workout-tracking web app with a classical/Renaissance visual theme. It is the frontend half of a two-repo system: the NestJS backend lives at `../sunnsteel-backend` and the shared types package at `../sunnsteel-contracts` (imported as `@sunsteel/contracts`).

Stack: **Next.js 15.5 (App Router) · React 18.3 · TypeScript 5 (strict) · TailwindCSS 4.3 · shadcn/ui + Radix · TanStack Query 5 · Supabase JS 2**.

**It is effectively an SPA hosted on Next.js.** The Server Components are `app/page.tsx` (a redirect based on a cookie), `app/api/session/route.ts`, the six `loading.tsx` files, and the static layouts around the public member-profile and shared-session routes. Every data-owning `page.tsx` remains `'use client'`; there is no RSC data fetching, no Server Actions, no `fetch` caching/`revalidate`, and no streaming. Data is fetched client-side via TanStack Query against the external backend. Keep this in mind before reaching for a Next.js server feature.

**`@sunsteel/contracts` is a published npm dependency (`^0.63.0` in [package.json](package.json)), not a `file:` link.** It must resolve from the registry because Vercel only clones this repo — pointing it at the local sibling (`file:../sunnsteel-contracts`) breaks the deploy (the shared types silently degrade to `any`). Trade-off: local edits to `../sunnsteel-contracts` are **not** picked up until you `npm publish` a new version and bump it here.

Runs on Windows 11.

## Commands

```bash
npm run dev            # Next.js dev server (Turbopack) on :3000
npm run dev:all        # PowerShell launcher: backend (:4000) + frontend (:3000) in separate windows
npm run typecheck      # tsc --noEmit
npm run lint           # eslint .
npm run lint:fix       # eslint . --fix
npm run build          # next build
npm run lock:check     # every dependency named in package-lock.json has an entry (TD-33); reads only the lock
npm run lock:repair    # restore missing lock entries, unchanged, from HEAD (TD-33); postinstall runs it too
npm run verify         # lock:check + lint + typecheck + test + build (run before considering work done)
npm run ui:capture:portfolio  # Playwright: portfolio screenshots + manifest (dev server, backend, ui:login; not CI)
npm run ui:refresh     # renew the saved sign-in through the app's own refresh path (before a long sweep)
npm run ui:regression  # the 480-case sweep; scope it with -- -g "<title pattern>" (see UI and styling)
```

**Vitest is configured** (added in T-01) — `npm test` / `npm run test:watch`. `npm run verify` runs lock:check → lint → typecheck → **test** → build, and CI ([.github/workflows/ci.yml](.github/workflows/ci.yml)) mirrors it on Node 22 (required by the current Supabase client).

Coverage stays in Node: **pure logic, auth event orchestration and API-contract tests**, 579 tests across eighty-four files, including `lib/api/types/workout-stats.test.ts` for local week boundaries, previous-performance, strength-trend ranges/chart mapping, exercise-performance, muscle-heatmap and volume-trend query serialization, heatmap matrix/intensity ranking, volume comparison summaries, weight-unit conversion, training-location preferences, finite-inventory plate loading, live-record celebration, progression-change presentation, stale-session recovery, session-resolution copy, profile sharing, profile-card identity/rank/accomplishment mapping and filenames, featured-profile ordering, shared slot limits, reached-rank selection and public profile achievement-ledger presence, rank identity (every ladder ID has its own pigment and crest tier, none borrows a locked role, and an unreached rank is ink-3), relationship-list routes, copy and follow-state cache updates, training-partner state labels, stable member matching and the fixed encouragement vocabulary, dashboard primary-action precedence, empty-state actions, the dashboard's upcoming-milestone placement, its stated training facts and its bounded preview of the followed-members feed (DASH-08), privacy-overview grouping, session-share links, field toggles and shared-recap sections, motion-preference resolution, the pre-paint motion script and splash timing, duration formatting, training-identity labels, routine day- and exercise-count pluralisation, routine schedules (rotation next day, day titles, wizard mode switching and rotation presets), the weekly schedule (planned and not-logged rules, session placement by start date, rotation notes, planned rest days and which entries can start or resume), training blocks in the schedule (each date by the plan in force, a rotation block from its first day and the baseline resuming after it, move targets per date and the session's snapshot prescription), the monthly calendar (grid weeks, one mark per day by precedence and month-only counts), rotations on training weekdays (the plan from today, after a live or finished session of the day, and the builder's carry-over), moved workouts (both dates, a session on either date, rest replaced, the move targets and actions), skipped workouts (never not logged, a session still counts, a skipped day free to move onto, the skip limit and postponing), in-app notifications (their wording and destinations, the bell's label and marking read in the cache), navigation indicators (exact unread labels, compact counts and active-session schedule suppression), push availability (VAPID decoding, device labels, the iOS install requirement and every refusal it outranks), notification-control copy (overnight quiet windows, the reminder rule and the notice that outranks the others), routine-sharing visibility capping and link urls, routine-clone copy, summary lines and the member-routine sub-path, block and report copy, moderation-review copy (what a withheld subject may be called, which actions a report offers and the receipt that still promises no timetable), routine-lineage wording and links, routine-discovery copy, summaries and query serialization, activity copy (the record each entry names in the viewer's unit, workout grouping, link routes, audience-cap wording, reaction labels and counts, and query serialization), routine versions (day and exercise matching, change wording and the restores the server would refuse), the routine quality summary (weekly muscle sets, duration estimates, location-equipment matching and imbalance flags), exercise-alternative ranking and its reasons, catalog browsing filters (URL state, muscle/equipment/gym/pattern/trained matching and empty states), exercise-page routine usage and set-scheme labels, starred/recent picker grouping and optimistic star updates, plateau-watch copy (thresholds, counts, closest-set wording and the sensitivity options), achievement grouping, threshold order, safe next-milestone presentation and comeback evidence, password-reset redirects and forms, in-session exercise substitutions, logout marker recovery in the auth suites, the middleware matcher covering every protected prefix in both directions (TD-45), activity-comment copy plus the count patched into every loaded query (SOC-06), which routine days "Train another day" offers (LIVE-06), account-deletion copy, the moderator refusal and the typed confirmation (TRUST-01), avatar storage paths, stale-file selection and the cropper's size cap (PROF-01), the export file's name, readable JSON and plain-language description (EXPORT-01), and post-workout corrections: the window and closed-reason copy, set values and changes in the viewer's unit, the draft that sends an untouched pound weight back exactly, and the rows it refuses (LIVE-17), session-note copy, labels, the character count and slot lookup (LIVE-16), and the service worker's policy: owned and legacy cache names, which pages and files it caches and what it never caches (TD-44). Network calls are mocked. [vitest.config.ts](vitest.config.ts) uses `environment: 'node'` on purpose — there is no jsdom and no React Testing Library, so **hooks and components cannot be rendered in tests** without first deciding to add them.

The goals coverage includes query serialization plus target validation, uniqueness and kg/lb conversion in [lib/utils/measurable-goals.test.ts](lib/utils/measurable-goals.test.ts).

**Never run `npm run verify` (or `npm run build`) while `npm run dev` is running.** Both write to the same `.next/` directory, so the production build overwrites the dev server's manifests and the running server starts answering **500 on every route**. In this app that surfaces as a **black screen**, because the layout never mounts — it looks exactly like an auth bug and will send you chasing the wrong thing (`localStorage` throwing `SecurityError` and the tab title falling back to the URL are the tells that it's a dead server, not client code). This has already burned an hour once. Stop the dev server first, or verify only when you are done poking at the app.

This build/dev restriction applies to **this checkout's `.next/` directory**, not to port 3000 globally. A different project running on that port does not conflict. Check the Node process command line and project path before stopping a server, so you stop the one you meant.

Start, restart and stop either dev server yourself when you need to, and say so when you do — there is no need to ask the owner first. It is still not a casual act, because they may be looking at the app: prefer running `npm run lint`, `npm run typecheck` and `npm test` while the servers stay up, push the coherent change, and require the clean CI build to pass before closure. Stop a server for `npm run verify`, `npm run build` or a local production-build diagnosis. For authenticated browser checks, use the running app and ask the owner only to sign in if the current browser session is unauthenticated.

There is no `docs:check` / `docs:update` script — both were broken (one pointed at a `.sh` that does not exist, the other was an `echo`) and were removed in CL-05.

## Architecture

### Layers (`lib/api/`)

Data flows through a strict service → hook → component layering:

- **Services** ([lib/api/services/](lib/api/services/)`*Service.ts`): raw endpoint calls. All go through `httpClient` in [lib/api/services/httpClient.ts](lib/api/services/httpClient.ts). Base URL is `NEXT_PUBLIC_API_URL` (defaults to `http://localhost:4000/api`).
- **Hooks** ([lib/api/hooks/](lib/api/hooks/)`use*.ts`): TanStack Query wrappers around services. Components consume these, not services directly.
- **Types** ([lib/api/types/](lib/api/types/)`*.type.ts`): prefer re-exporting/wrapping shared shapes from `@sunsteel/contracts` over redefining API shapes.

There is no exception to that layering any more: the avatar upload, which used to call the Supabase client from [app/(protected)/settings/page.tsx](<app/(protected)/settings/page.tsx>), goes through [avatarService](lib/api/services/avatarService.ts) and [useUploadAvatar](lib/api/hooks/useUploadAvatar.ts) (PROF-01).

`httpClient` signature note: the secure flag is a **positional boolean**, e.g. `httpClient.get<T>(url, true)` / `httpClient.request(url, { method, secure: true })` — **not** an options object like `{ secure: true }`. `secure: true` attaches `Authorization: Bearer <supabase access_token>` (pulled from `supabase.auth.getSession()` on every call); all requests use `credentials: 'include'`. Use `requestWithMeta` (same file) when you need response headers/status without throwing — `workoutService.startSession` uses it to branch on 4xx via a `STATUS:<code>:<message>` error prefix.

### Auth

Two-step: Supabase auth (email/password or Google OAuth) → backend verification.

1. [lib/api/services/supabaseAuthService.ts](lib/api/services/supabaseAuthService.ts) signs in with Supabase.
2. `verifyToken()` posts the access token to the backend at `/auth/supabase/verify`.
3. On success it calls `POST /api/session` — **the frontend's own Route Handler** ([app/api/session/route.ts](app/api/session/route.ts)) — which sets the HttpOnly `ss_session=1` cookie (7-day maxAge). The cookie **cannot** be set by the backend: a cookie in the backend's response is scoped to the backend's domain and is never sent to this app's domain, so middleware would never see it.

[providers/supabase-auth-provider.tsx](providers/supabase-auth-provider.tsx) relies solely on a **synchronous** `onAuthStateChange` callback (no parallel `getInitialSession`). [lib/auth/auth-session-controller.ts](lib/auth/auth-session-controller.ts) defers verification outside the Supabase auth callback, discards stale completions after logout/account changes/unmount, and cancels scheduled work on cleanup. Repeated `SIGNED_IN` and established-profile `TOKEN_REFRESHED` events do not re-verify or invalidate `['user']`. `USER_UPDATED` explicitly refreshes both. Account switches clear the old query cache before publishing the new session.

`verifyToken` shares its pending/completed result for the current token in memory, so email login and the provider do not duplicate the backend request. Logout, account changes and explicit user updates invalidate that result; failed requests are not cached. A cold page load still costs `POST /auth/supabase/verify` + `POST /api/session`. The OAuth callback consumes the provider's verified profile instead of making its own request. Auth pages now observe the real initial `isLoading` state, so the callback cannot redirect before the initial session is known.

Marker POST/DELETE requests are serialized. A stale verification cannot enqueue a new POST after invalidation; a DELETE waits for any already-started POST. A failed POST rejects verification rather than caching a false login success. Concurrent DELETE cleanup shares one pending request; failures surface in the protected layout without exposing cached data, and the user can retry until the routing marker is cleared.

**That verification is deliberately not a gate — do not turn it back into one.** `isLoading` flips to `false` as soon as the provider knows _whether_ there is a session, **before** the `await verifyToken`, and data hooks are enabled on `!!session`. This is safe because the backend's `SupabaseJwtGuard` re-verifies the token and get-or-creates the user on **every** protected request (`../sunnsteel-backend/src/auth/strategies/supabase-jwt.strategy.ts`) -- locally against the project's JWKS since TD-43, with no call to Supabase Auth -- so `/auth/supabase/verify` duplicates that work rather than being a prerequisite for it. `isAuthenticated` is still `!!session && !!user`, but it now means "the verified profile has arrived" — use it only for UI that genuinely needs the profile, never to gate queries or rendering. Full reasoning in TD-18.

Route protection is in [middleware.ts](middleware.ts) and checks **only** `ss_session === '1'`. Protected prefixes: `/dashboard`, `/workouts`, `/routines`, `/profile`, `/progress`, `/exercises`, `/schedule`, `/achievements`, `/notifications`, `/settings`, `/search`, `/activity`, `/moderation`. Unauthenticated hits redirect to `/login?redirectTo=<original>`; authenticated hits on `/login`/`/signup` redirect to `/dashboard`. [app/(protected)/layout.tsx](<app/(protected)/layout.tsx>) adds a second, client-side redirect: it bounces to `/login` when there is no Supabase session, or when the backend verification actually **failed** (`error && !user`) — not merely when the profile has yet to arrive.

**Password reset (FIX-11) rides the existing callback.** `/forgot-password` calls `resetPasswordForEmail` with `redirectTo` built by [lib/auth/password-reset.ts](lib/auth/password-reset.ts): `<site>/auth/callback?callbackUrl=/reset-password`, the same shape Google sign-in already uses, so it needs no new Supabase redirect-URL entry. The confirmation never says whether the address has an account. The recovery link signs the user in like any other session (the provider verifies it and sets the marker), and the callback forwards to `/reset-password`, which calls `updateUser({ password })`; its `USER_UPDATED` event re-verifies the profile. A callback that arrives without a session for `/reset-password` (expired or already-used link) goes to `/forgot-password?link=expired`, not to Login. Both pages are public: they are in neither `PROTECTED_PREFIXES` nor `AUTH_PAGES`, so a signed-in visitor can still open them.

**The middleware is a UX gate, not a security boundary.** The cookie's 7-day lifetime is independent of the Supabase session, so the two can drift in both directions. Real authorization is the bearer token on each backend request.

**Invariant: the marker cookie must be cleared _before any render can observe a null session_** — `await supabaseAuthService.clearSessionMarker()` (`DELETE /api/session`) runs before `setSession(null)`, and before `setError(...)` on the failed-verification path. If the marker outlives the Supabase session, middleware waves `/dashboard` through, the layout bounces to `/login`, middleware bounces back, and the app parks on `/dashboard` rendering its empty shell — **a black screen, not a visible loop**.

"Clear it before redirecting" is **not** a strong enough rule: it's the render that triggers the redirect. A first attempt cleared the cookie after `setSession(null)` and appeared to work only because `isLoading` happened to gate the layout's effect on first load — logout, where loading is already resolved, still broke. See TD-21. The ordering _is_ the fix; preserve it when refactoring.

**Service-worker state must not participate in development debugging.** [providers/pwa-provider.tsx](providers/pwa-provider.tsx) registers `/sw.js` only in production. In development it removes an existing Sunnsteel `/sw.js` registration, deletes only `ss-*` caches and reloads once if that worker controlled the page, preventing the old controller from immediately recreating those caches. That reload is deferred on `/workouts/sessions/*`. Do not remove the cleanup or broaden it to caches the app does not own.

The Supabase client ([lib/supabase/client.ts](lib/supabase/client.ts)) falls back to a dummy client when env vars are missing during build, and only throws at runtime on the client side.

### Query keys

Two coexisting styles — match whichever domain you are in:

- **Routines**: `routineQueryKeys` in [lib/api/routines/routine-query.ts](lib/api/routines/routine-query.ts) → `['routines']`, `['routines', <serialized filters>]`, `['routines', <id>]`. The same file holds `RoutineFilters`, filter serialization and the `URLSearchParams` builders — build query strings via those helpers, not ad hoc. Note lists and details share the `['routines']` prefix, so invalidating it hits both.
- **Workouts**: a local `qk` object inside [lib/api/hooks/useWorkoutSession.ts](lib/api/hooks/useWorkoutSession.ts) → session, history and Progress keys under the `['workout']` prefix, including `['workout','progress','session-comparison',routineDayId]`. Use the key factory instead of recreating these arrays in components.
- Everything else is an inline literal: `['user']`, `['exercises']`, `['users','search',q,limit]`. Relationship lists and follow suggestions use `relationshipQueryKeys` in [lib/api/hooks/useRelationships.ts](lib/api/hooks/useRelationships.ts): `['users','relationships',identifier,kind]` (infinite) and `['users','suggestions',limit]`; a follow toggle from a list updates loaded rows in place and marks both stale without refetching. Session share links use `sessionShareKeys` in [lib/api/hooks/useSessionShares.ts](lib/api/hooks/useSessionShares.ts): `['workout','session',id,'shares']` for the owner's active links and `['shared','session',token]` for the public read. Schedule overrides (SCHED-04) use `scheduleOverrideKeys` in [lib/api/hooks/useScheduleOverrides.ts](lib/api/hooks/useScheduleOverrides.ts): `['schedule-overrides', from, to]`; a move, skip or undo invalidates the whole prefix. Notifications (NOTIF-01) use `notificationKeys` in [lib/api/hooks/useNotifications.ts](lib/api/hooks/useNotifications.ts): `['notifications']`, refreshed on focus and every five minutes because the top bar reads it on every page. Activity (SOC-03/SOC-04) uses `activityKeys` in [lib/api/hooks/useActivity.ts](lib/api/hooks/useActivity.ts), everything under `['activity']` so one invalidation reaches the feed, a profile's activity, the owner's list and both previews; a privacy change, a routine visibility change and a block all invalidate it, because each can change who sees an entry.

**If you add a prefetch, it must use the exact key the consuming hook reads.** A hand-rolled data-prefetch layer that wrote to keys nobody read (`hooks/use-navigation-prefetch.ts`) was deleted for exactly this reason. Treat that as historical evidence: start from the [active technical-debt register](docs/roadmaps/technical-debt.md), which links the closed audit when older decision context is needed. Route prefetching is `next/link`'s job; don't reimplement it.

### Caching

- **TanStack Query** ([providers/query-provider.tsx](providers/query-provider.tsx)): `staleTime` 5 min, `gcTime` 10 min, `refetchOnMount: true`, `refetchOnWindowFocus: false`, `refetchOnReconnect: true`, no retry on 4xx (the predicate reads `error.status`, which `httpClient` supplies via `HttpError`).
- **Next.js Router Cache** ([next.config.ts](next.config.ts)): `experimental.staleTimes` = `{ dynamic: 30, static: 180 }` seconds. Without it Next 15 defaults `dynamic` to 0 and the five `ƒ` routes refetch their RSC payload on every navigation (TD-19). It does not affect data freshness — data comes from TanStack Query, not the RSC payload. TanStack Query uses `refetchOnMount: true`, so fresh data survives page remounts within its `staleTime`.
- **Service worker** ([worker/sw.ts](worker/sw.ts), TD-44): built by `@serwist/next` into `public/sw.js` during `next build` (the file is gitignored and ESLint-ignored; development never generates it). Its precache is **the build's own content-revisioned manifest** -- every `/_next/static` chunk, CSS and font, plus `/site.webmanifest`, `/icon-192.png`, `/favicon.ico` (revisioned by file hash in [next.config.ts](next.config.ts)) and the static `/offline` page (one revision per deploy) -- so there is no `CACHE_VERSION` to bump. The policy stays ours, in [service-worker-policy.ts](lib/pwa/service-worker-policy.ts): pages network-first into `ss-pages` with `/offline` as the fallback, same-origin static files stale-while-revalidate into `ss-assets`, and **nothing** for the backend, other origins, `/api/*` or `/auth/*`. `setCacheNameDetails({ prefix: 'ss' })` runs before `new Serwist` because `cacheId` alone reaches the precache too late and leaves it `serwist-precache-…`, which development cleanup would not remove. [providers/pwa-provider.tsx](providers/pwa-provider.tsx) still registers it only in production; the plugin neither registers nor skips waiting, and Serwist answers the page's `SKIP_WAITING` message, so updates still activate through the waiting worker and reload once, both deferred while the user is on `/workouts/sessions/*`. The worker compiles against the `webworker` lib through [worker/tsconfig.json](worker/tsconfig.json), which `npm run typecheck` runs as a second project; the app's own tsconfig excludes `worker/`.
- **No persisted query cache** — no IndexedDB, no `persistQueryClient`.

### State

- Server state: TanStack Query (~90% of real state).
- Local state: plain `useState` per page/component.
- Context: `SupabaseAuthContext`, `ToastProvider`, `next-themes`. No Redux (imports are commented out in [providers/app-provider.tsx](providers/app-provider.tsx)), no Zustand.
- One hand-rolled global store: [lib/utils/save-status-store.ts](lib/utils/save-status-store.ts) — a `Map` + listener `Set` driving per-set save status (`idle/pending/saving/saved/error`) in the active session.

### Directory map

- `app/` — App Router. `(auth)` = public login/signup/OAuth callback plus forgot/reset password; `(public)` = the signed-out-safe `/members/:identifier` profile surface and `/shared/sessions/:token` workout shares; `(protected)` = the authed shell (dashboard, routines, workouts, progress, exercises, schedule, achievements, notifications, activity, moderation, profile, settings, search). `app/api/session/` is the only Route Handler.
- `features/` — feature-scoped components/hooks (`routines/` + `routines/wizard/`, `workout/`, `exercises/`, `activity/`, `moderation/`, `shell/`, `initial-load-animation/`).
- `components/ui/` — shadcn/ui primitives (`new-york` style), extended with classical theme variants via `cva`.
- `providers/` — nested in [providers/app-provider.tsx](providers/app-provider.tsx): `QueryProvider` → `SupabaseAuthProvider` → `AppToastProvider`. `ThemeProvider` and `PwaProvider` sit above it in [app/layout.tsx](app/layout.tsx).
- `lib/config/env.ts` — `PUBLIC_ENV` and the `SHOULD_*` flags (performance panel/logs, debug logs). `schema/env.client.ts` validates client env at mount (never throws; warns).
- `hooks/` — standalone (non-API) React hooks.
- `lib/auth/` — framework-independent auth event controller and cancellation error; exercised with mocked dependencies in Node tests.

## UI and styling

Any change that affects rendered UI — including new components, loading/empty/error states, responsive layout, themes, interaction states or motion — must follow the [Sunnsteel Design System](docs/ui-design-system.md), currently **v1.0 · LOCKED**. Existing styling is not precedent where it conflicts with that document. If a locked rule cannot be implemented as written, report the contradiction instead of making a new visual decision locally.

Before editing UI, always read §4.3 (colour rules), §5.3 (type-rank usage), §§7–10 (radii, elevation, motion and responsive behaviour) and §15 (verification), plus the relevant component subsection in §11. If changing tokens or global CSS, read §§3–4 in full. Restyle progress, direction, migration and QA/review documents are historical evidence only; they are not current styling authority.

High-risk invariants:

- **Use semantic tokens in component styling.** Do not add hardcoded colour literals or raw Tailwind palette classes. Literal values are permitted only when defining the approved tokens or when platform metadata requires them, and must match the design system.
- **One filled primary action per region.** Repeated list controls are never primary.
- **Colour roles do not overlap.** `success` means completed as planned; `honour` means exceeded expectations and is capped at two marks per viewport and one per row; `destructive` means data destruction; warning is a non-text mark. Colour never communicates state without a glyph, label or structural cue.
- **Every visible `h1`–`h4` uses one `type-*` rank.** Do not combine it with utilities that override font family, size, weight, tracking, casing or line-height. Colour and layout utilities are allowed; screen-reader-only headings are exempt.
- **Use only the defined radius roles in new UI:** none, small or medium; full is reserved for avatars. Do not introduce arbitrary radii.
- **Elevation uses tone, not decoration.** `shadow-overlay` is the only shadow and is reserved for overlays. Do not add generic shadows, gradients, glow, backdrop blur or translucent cards.
- **Ruled lists are the default.** Reuse the existing styled shadcn/Radix primitives and Sunnsteel patterns instead of inventing parallel components or replacing semantic primitives with styled generic elements.
- **Motion uses the motion tokens and supports `prefers-reduced-motion`.** Do not add prohibited page entrances, per-section fade-ups, scale-on-hover, floating cards, glow or animated gradients.
- **Inputs remain 16px below `md`.** Preserve their explicit boundary, focus and invalid-state treatment in both themes.
- **Responsive decisions must account for the protected shell.** Apply the `viewport - 256px` content budget and exact-breakpoint verification described in Gotchas; do not reason from viewport width alone.

For every UI change, inspect the affected states in both themes at the relevant exact boundary widths and run the regression sweep. It requires the backend and a valid saved sign-in; if either prerequisite is unavailable, report exactly what blocked the run and what was verified instead — **never imply the sweep passed**. Follow the existing build/dev-server restriction when running the normal repository gates.

**Scope the sweep to what you touched; that is the default, not a shortcut.** The full run is 480 cases — 30 routes × 7 widths × 2 themes (420) plus 60 interaction cases — at roughly five seconds each, so it costs about 37 minutes and re-checks every route you did not change. One route is 14 cases. Select by test title (`layout <slug> @ <width> <theme>`, and the `navigation`/`drawer`/`sidebar`/`dialog`/`dropdowns`/`hover`/`keyboard focus` suites in [e2e/regression.spec.ts](e2e/regression.spec.ts)):

```bash
npm run ui:regression -- -g "layout activity|navigation|drawer|sidebar|dialog|dropdowns|hover|keyboard focus"
```

That is 87 cases, about 7 minutes: the two `activity` routes plus every interaction suite. Drop the suites when nothing outside one page changed (`-g "layout activity"`, 28 cases), and check a pattern with `--list` before trusting it — `layout activity @` matches `activity` but not `activity-yours`.

**Run the whole 480 when the change is not confined to a page**: the protected shell or [Sidebar.tsx](features/shell/components/Sidebar.tsx), [app/globals.css](app/globals.css) or the design tokens, any `components/ui/*` primitive, motion or theme handling — and before a release. State in the closure which of the two you ran, with its tally.

Four hazards, three of which have each already cost a run. **Start the local PostgreSQL server first**: the backend cannot serve a request without it, and it is not registered as a Windows service, so a reboot leaves it down — the backend's `AGENTS.md` carries the `pg_ctl` command. **Renew the saved sign-in** with `npm run ui:refresh`: the access token lasts an hour, a full sweep outlives it, and every context then refreshes from the same stored refresh token — the failure that lost two cases to a 401 in the `PROF-10` sweep. A refresh that cannot renew now **leaves `.auth/state.json` alone** and says so, rather than writing a signed-out browser over a recoverable session; when it reports that, `npm run ui:login` is the only way back and it needs the owner. **Do not write files in `../sunnsteel-backend` while a sweep runs**: `npm run start:dev` watches the tree, restarts, and every case in flight fails with the backend unreachable. **Check whether another agent is working before running the sweep in parallel**, and use one worker when one is: the local PostgreSQL server is shared by every agent on this machine, and on 2026-09-22 a three-worker run alongside another agent's work drove the backend into a crash loop. The same scope passed 28/28 at `--workers=1` straight afterwards.

That failure is worth describing, because **none of its symptoms named its cause**. Prisma reported `Can't reach database server at localhost:5432`, which reads like the hazard above -- a stopped Postgres. Postgres was up the whole time: `server.log` in its data directory showed continuous uptime and 9 of 100 connections, and what it had actually logged was clients being forcibly disconnected, which is the backend dying rather than the database. The first real error was an interactive-transaction timeout (5137ms against Prisma's 5000ms), i.e. contention. **Read that log before restarting anything**, and do not conclude the database is down from a Prisma message alone.

**A crash loop leaves a trap for the next run.** `node --watch` restarts the backend on a crash, so one of those restarts can bind port 4000 while the parent watcher gives up with `EADDRINUSE` -- leaving an orphan serving requests with nothing supervising it. The next sweep then failed on its first case with the app signing the saved session out, because `POST /api/auth/supabase/verify` failed against that orphan. After any crash loop, kill whatever holds port 4000, start one backend, and prove `/api/health` **and** the verify route answer (`401` to a bad token) before re-running -- a healthy `/api/health` alone did not catch it.

When the whole run is unavoidable, `-- --workers=3` after a refresh cuts it to about sixteen minutes: measured 2026-09-20, 437/437 passing serial (36.7m) and at three workers (16.1m), same cases, and again 2026-09-21 at 451/451 in 12.5m at three workers. The committed default stays one worker, because parallel contexts share the one saved sign-in and that is the thing that has invalidated it before; renew first, and keep the run short enough that no context has to refresh mid-run. They also share the local PostgreSQL server with every other agent on this machine, so three workers is only safe when nothing else is running against it.

**Portfolio screenshots.** When a user-facing page ships or visibly changes, add or update its entry in [e2e/portfolio-targets.ts](e2e/portfolio-targets.ts) in the same change (slug, route, roadmap IDs, optional setup with its cleanup, caption hint). `npm run ui:capture:portfolio` writes 1440×900 dark viewport frames to `docs/portfolio/screenshots/<slug>.png` and regenerates `docs/portfolio/manifest.json`, replacing the owner's email with `eze@sunnsteel.app`. It needs the same prerequisites as `ui:regression`, refuses to run while a workout session is active, and is not part of CI. A setup step may start a scratch session but never finishes one; it discards it afterwards.

## Closing a slice

The order the gates run in, and the work each one is the only owner of. Doing
one of these twice is the most common way a routine change takes an afternoon.

1. **Contracts**, only when a shared shape changed: edit `../sunnsteel-contracts`, verify, publish, then install the new version here and in the backend.
2. **`npm run verify`, once, at the end**, with the dev servers stopped. While iterating run `npm run lint`, `npm run typecheck` and `npm test`; the full gate re-runs all of them plus `next build`, so running it after every edit buys nothing. **The test tally is in `verify`'s own output** — do not run `npm test` again to read it.
3. **Real-stack checks** against the running servers, for what a Node test structurally cannot reach: SQL and Prisma `where` clauses, JSON-path filters, authorization order, cursors over real rows. A rule already covered by a pure test does not need a second assertion over HTTP.
4. **Browser pass** for behaviour only — links, copy, mutations, the states a person actually sees. Do not re-assert horizontal overflow, console errors or `h1` counts by hand: the sweep owns those, at more widths than a hand pass will ever cover.
5. **Sweep**, scoped by the rule in [UI and styling](#ui-and-styling).
6. **Docs**: the roadmap closure with its decisions and what you did *not* verify, `CLAUDE.md`/`AGENTS.md` kept in sync, `../FEATURES.md` when a feature ships.

## Conventions

- **Formatting is enforced, not a matter of taste (CL-06).** [.prettierrc](.prettierrc) is the single source of truth — tabs, single quotes, no semicolons, `printWidth` 80, `arrowParens: avoid`, `endOfLine: auto` — and `prettier/prettier` runs as an ESLint **error**, so `npm run lint` and CI fail on drift. Imports are sorted by `simple-import-sort`; run `npm run lint:fix` rather than arranging them by hand. Side-effect imports are not reordered. The repo used to have two coexisting styles with nothing arbitrating; don't reintroduce that by "matching the surrounding file".
- **`endOfLine` is `auto` on purpose.** `core.autocrlf=true` means the working tree is CRLF while the repo stores LF; a hard `"lf"` made Prettier flag every file on a fresh checkout.
- Imports use the `@/` alias (maps to repo root).
- Naming: PascalCase components/types, `use*` hooks, `*Service.ts` services, `*.type.ts` types, kebab-case multi-word filenames, `UPPER_SNAKE_CASE` constants.
- `@typescript-eslint/no-explicit-any` is turned **off** — `any` is permitted.
- [eslint.config.mjs](eslint.config.mjs) wires up three plugins beyond `next/core-web-vitals` and `next/typescript`, after `eslint-config-prettier` turns off the stylistic rules that would fight Prettier: `eslint-plugin-prettier` (`prettier/prettier`), `eslint-plugin-simple-import-sort` (`imports` and `exports`) and `eslint-plugin-import`, of which only `import/no-duplicates` is enabled. Its path-resolution rules (`no-unresolved`, `no-cycle`) are off on purpose: they need `eslint-import-resolver-typescript`, which is not installed, and would only re-check what `tsc` already checks. All four enabled rules are errors.

## Gotchas

These were re-verified against the current frontend, backend and contracts trees on 2026-09-10 and will bite you if you assume otherwise:

- **There is no `tailwind.config.ts`, and you must not add one.** Tailwind is v4 with CSS-first config: [app/globals.css](app/globals.css) starts with `@import 'tailwindcss'` + `@theme inline` + `@plugin "tailwindcss-animate"`, there is no `@config` directive, and `components.json` has `"config": ""`. A config file is only loaded via `@config`, so one would be silently ignored. Theme tokens and utilities (`heading-classical`, `hide-scrollbar`) live in `globals.css`. The old dead config was deleted in TD-12.
- **RtF (Reps-to-Failure) is gone.** Earlier versions of this file described a large dormant RtF codebase (`etag-client.ts`, `rtf-offline-cache.ts`, `useRtF.ts`, `Rtf*` components, `tm-trend.ts`). **None of it exists anymore.** Do not go looking for it; do not reintroduce it.
- **`refetchOnMount` is `true`, not `'always'`** (changed in TD-02), so `staleTime` is now load-bearing: a fresh query is served from cache on navigation and does not hit the network. If a hook must never serve stale data, give it its own `staleTime: 0` — `useSession` does, because it backs the live training session.
- **`next/dynamic` is used only for the two heavy wizard steps** (`BuildDays`, `ReviewAndCreate`, in both `routines/new` and `routines/edit/[id]`) and the perf debug panel. There is no `React.lazy` and **no `React.memo` anywhere** — and adding memo to the session screen would not help, because `groupSetLogsByExercise` rebuilds every object on each call, so every prop is a fresh reference (see TD-07).
- **[lib/utils/dynamic-imports.tsx](lib/utils/dynamic-imports.tsx) is preload-on-_hover_ only.** `preloadAllCriticalComponents`, which eagerly `import()`ed ~11 page modules 2 s after auth, was removed (TD-06); `preloadComponents` now holds only the 3 entries `preloadOnHover` actually uses. Don't reintroduce eager preloading.
- **The mobile splash mounts `children` immediately.** [InitialLoadAnimation.tsx](features/initial-load-animation/InitialLoadAnimation.tsx) is an overlay _on top of_ the app, not a gate _in front of_ it. It used to withhold `children` for 3.4 s, which meant no page query started until it finished (TD-03). If you touch it, keep `children` mounted from the first frame and animate only opacity.
- **Reduced motion has two sources, and both must be honoured.** The OS `prefers-reduced-motion` media query and the per-device Settings preference (A11Y-01), stored in `localStorage` as `ss-motion` and applied as `data-motion="reduce"` on `<html>` by an inline script in [app/layout.tsx](app/layout.tsx) before first paint. [app/globals.css](app/globals.css) carries the reduction twice — the media block and a `:root[data-motion='reduce']` mirror — because CSS cannot share one block between them, so change both together. framer-motion values CSS cannot reach (the splash stagger, looping dots and load bar, the toast timer width) read [hooks/use-motion-preference.ts](hooks/use-motion-preference.ts); `MotionConfig` switches to `"always"` under the device preference. The preference can only add reduction, never re-enable motion the OS reduced, and it decides how state arrives, never whether it is shown.
- **Eruda is gone** (TD-05). It used to be a devDependency imported from app code that shipped a 488 KB chunk. No repository code reads `NEXT_PUBLIC_ENABLE_ERUDA`.
- **Icons/assets are generated, not hand-made.** `icon-192/512/512-maskable.png`, `apple-touch-icon.png` and `og-image.jpg` were produced from the original 1024px `logo.png` with `sharp` (a transitive dep of `next`, so no install needed). The 1.76 MB `logo.png` and the 5.23 MB hero original were **deleted from the tree but remain in git history** — recover them with `git show 0dbe9a^:public/logo.png` and `git show 0dbe9a^:public/backgrounds/vertical-hero-greek-columns.webp`. If you need another size, regenerate from there rather than resizing a derivative.
- **There are no routine mocks and no `next/dynamic` component exports left.** `features/routines/mocks/`, `components/ui/command.tsx`, `DashboardStats` and four dead `lib/utils` modules were deleted in block 5 (CL-03/CL-04), along with eight unused dependencies (`@dnd-kit/*`, `recharts`, `@supabase/ssr`, `@tanstack/react-query-devtools`, `cmdk`, `concurrently`). Don't reintroduce them looking for something that "used to be there".
- **The routine card shows a schedule note, not a completion bar.** `RoutineCard` used to render `<RoutineProgress completed={routine.isCompleted} />`, which mapped a boolean to `100`/`0` under the label "Completion" — so every routine in active use read "Completion 0%" no matter how many sessions were logged against it, and the toggle that sets `isCompleted` is `disabled` in that card anyway. `Routine` in `@sunsteel/contracts` carries no session counts and no program length, so **there is no per-routine completion ratio to show** — don't invent one without changing the published contract. [RoutineScheduleNote.tsx](features/routines/components/RoutineScheduleNote.tsx) renders "Next session · Today/Tomorrow/<weekday>" from `nextScheduledDay` ([lib/utils/date.ts](lib/utils/date.ts)) instead; a rotation routine (ROUT-11) shows "Next in rotation · <day>" from `nextRotationDayId`. `RoutineProgress` and its only remaining consumer, the hardcoded mock `app/(protected)/routines/components/ActiveWorkout.tsx`, were deleted with it.
- **Usernames are lowercase public identifiers with two deliberate profile contexts.** `@sunsteel/contracts` owns the 3–30 character format and reserved-name list; the backend normalizes again and the database enforces format and uniqueness. Search returns name, username and avatar only — never email. Authenticated `/profile/<identifier>` uses `GET /users/:identifier`, retaining UUID compatibility plus owner/follower access and social controls. Share Profile copies `/members/<username>`; that route stays outside the protected shell and uses unguarded `GET /profiles/:identifier`, which always evaluates privacy as a signed-out visitor.
- **Featured accomplishments store stable references, not copied presentation data.** Settings can order at most six current personal records, earned achievements, shared routines and one attained rank title in the same list. The achievement picker reads the authoritative `/achievements` response, and rank choices are the stable ladder entries through the current rank. Public profiles receive only references the backend can resolve at read time: records follow Personal Records privacy, achievements and ranks follow Achievements privacy, and stale or denied references are omitted. Keep the shared six-slot and one-rank guards in the UI, but treat backend validation as authoritative.
- **Account discovery is separate from profile authorization.** Settings stores independent booleans for name search, username search and future contact matching. Name and username default on; contacts default off, and no contact upload or matching exists yet. The backend gates only the corresponding search branches. A direct `/profile/<username>` visit still resolves normally, then `viewerAccess` controls what the viewer may read.
- **`/profile/<identifier>/<followers|following|mutuals>` are sub-pages of the optional catch-all, not separate routes.** [app/(protected)/profile/[[...userId]]/page.tsx](<app/(protected)/profile/[[...userId]]/page.tsx>) reads the second segment through `parseRelationshipListKind`; any other sub-path renders "Page not found" instead of silently aliasing the profile. `mutuals` is viewer-relative. Relationship lists exist only inside the authenticated shell: the signed-out `/members` route passes no `relationshipHrefs`, so its counts stay plain text.
- **`/shared/sessions/<token>` is the second signed-out route, and the token is its only credential.** It sits in `(public)` beside `/members`, reads unguarded `GET /shared/sessions/:token` through `useSharedSession` with `staleTime: 0` so a revoked link stops at once, and renders `SessionRecapContent` with the owner's `weightUnit` and only the sections the owner selected (`sharedRecapToRecapView`). Hidden sections are omitted, never shown as zero, and the previous-session comparison is never shared. Owners create and revoke links from the history recap through `SessionShareButton`; revocation is permanent.
- **Profile privacy is enforced by the backend before sensitive reads.** Eight independent settings (`biography`, `location`, `trainingIdentity`, `workoutHistory`, `records`, `routines`, `achievements`, `bodyMetrics`) use `PUBLIC`/`FOLLOWERS`/`PRIVATE` and default to private. Owners always retain access; `FOLLOWERS` means the viewer follows the profile owner. Authenticated and anonymous member-profile responses share the same `viewerAccess` shape and only query/return allowed biography, location, training identity, training summaries, personal records, achievement ledgers and body metrics; neither includes email. The anonymous endpoint treats every `FOLLOWERS` rule as denied and sends `Cache-Control: no-store`, so a privacy change cannot leave a shared page cached with stale access. Biography, location and training identity have honest real, empty and private states. Achievement access exposes only the current rank, earned milestones and bounded comeback summary; next-milestone progress and next-rank requirements stay owner-only on `/achievements`. Routine visibility reaches profiles through `PROF-08`'s featured routine slot.
- **Training identity is profile metadata, not measurable programming.** `PROF-05` owns bounded broad goals, experience, disciplines, preferred routine style and up to five catalog-backed favorite exercises. `PROG-08` stores measurable targets separately as private Settings preferences. Favorite exercises are stored by exercise ID and returned in user-defined order; never accept free-text favorites or treat labels as prescriptions.
- **Quick Workout is gone, and "Train another day" replaced it (`LIVE-06`, 2026-09-22).** The old handler created a routine whose only day had `exercises: []`, so the session it opened had nothing loggable and no way to add anything; `FIX-04` hid the button, and this removed it along with its `SHOW_QUICK_WORKOUT_ENTRY` flag. **The replacement adds no capability**: `startSession` validates that the caller owns the routine day and nothing about the date, and `/routines/[id]` has always started a specific day, so training off-plan already worked — it was four taps deep. [features/workout/train-another-day-dialog.tsx](features/workout/train-another-day-dialog.tsx) puts it on `/workouts` and on the dashboard, over [lib/utils/train-another-day.ts](lib/utils/train-another-day.ts). **A day with no exercises is never offered** — that is the old bug — and nor is a day of a completed routine, matching `routinesPlannedOn` on the server. Do not add a way to start a session with an empty prescription, and do not let exercises be added to a live session: the snapshot is the prescription (`LIVE-11`), and free-form ad-hoc sessions were rejected outright, not deferred.
- **`groupSetLogsByExercise` drops fields silently, and a cast hides it.** [lib/utils/session-progress.utils.ts](lib/utils/session-progress.utils.ts) builds each grouped set by hand and ends with `as GroupedExerciseLogs`, so a field present on the type but missing from the object is not a type error. It has already swallowed `rpe` (fixed in LIVE-04) and `restSeconds` (fixed in LIVE-01), each time leaving a UI that read `undefined` forever. If you add a field to a set, confirm it survives this function.
- **Workout weights are stored canonically in kilograms.** [lib/utils/weight-unit.ts](lib/utils/weight-unit.ts) is the display and entry boundary for `KG`/`LB`; use it for set logs, routine prescriptions, records and volume instead of relabelling raw API numbers. Inputs convert back to kilograms before validation or persistence. Its small equality tolerance absorbs only two-decimal pound round-trip noise, so switching units or mounting from an auth fallback cannot trigger background set writes.
- **Progress has eight intentionally different bounded reads.** `/workouts/progress/strength` reconstructs the capped `PERSONAL_RECORD` frontier and its pre-range baseline. `/workouts/progress/performance` paginates terminal sessions with completed work, then composes snapshot context, sets/RPE, notes and progression events. `/workouts/progress/muscles` reads only 4–12 Monday-based `WorkoutMuscleRollup` weeks from the active, timezone-matched projection; its completed-set equivalents are already weighted primary 1.0 / secondary 0.5 and zero cells are filled at the response boundary. `/workouts/progress/volume` combines those bounded weekly rollups with at most 12 weeks of completed-session summaries and completed set logs to compare total, muscle, historical-routine and exercise workload. `/workouts/progress/session-comparison` caps routine-day discovery at 100 and loads only the latest two completed snapshots for one stable routine-day identity; exercises and sets match by `routineExerciseId` and `setNumber`, never names. `/workouts/progress/timeline` cursor-paginates persisted `PERSONAL_RECORD` and `PROGRESSION_CHANGED` events in one stable order (optionally for one `exerciseId`), joins the immediately previous record to explain why it advanced, and adds snapshot routine/day context without scanning set logs. `/workouts/progress/goals` evaluates at most eight private goals from bounded current-week rollups, the active streak projection, persisted estimated-1RM records and the owner's current body weight. `/workouts/progress/plateaus` (PROG-09) flags recently trained lifts from the persisted best per exercise plus the completed loaded sets of an 8-week window: at least the account's minimum sessions since the best (four by default, its own session excluded), a best at least three weeks old, the lift trained in the last three weeks, and never when the estimated 1RM rose. The minimum is a per-account preference (PREF-05: `PUT /users/preferences/plateaus`, 3–8, chosen in the section itself, where `useUpdatePlateauPreferences` stays pending until the list has refetched); the other thresholds are exported contract constants, all returned with every response, the UI prints them above the list and repeats a flagged lift's note on its exercise page, and the copy never suggests a cause. Volume means external load × reps, not strength; bodyweight sets count toward completed sets but add zero kg. Never replace any of them with an unbounded lifetime `SetLog` response.
- **Achievements are event-backed and historical recognition is explicit.** `/achievements` reads the 25-item shared catalog through protected `GET /achievements`. Existing projection totals are reconciled once into immutable `ACHIEVEMENT_UNLOCKED` events marked `backfilled`, so the earned ledger says "Recognized from history" instead of inventing an earned date or session. New milestones retain their actual time and source session. The same response carries one next fixed milestone per category from the verified totals; [milestone-progress.tsx](features/achievements/milestone-progress.tsx) keeps catalog order rather than ranking what is closest, uses exact values without percentage bars or deadlines, and explicitly tells users not to chase volume or streaks at the expense of planned loads and recovery.
- **A rank has an identity, keyed by its stable ID (ACH-09).** [rank-identity.ts](lib/utils/rank-identity.ts) maps each contract rank ID (`INITIATE` … `LAUREATE`) to its `text-rank-*` pigment and its crest tier; [RankCrest](features/achievements/rank-crest.tsx) draws the *testa di cavallo* crest family from design system §19 as inline SVG on `currentColor`. It is on `/achievements` (current and next rank), the profile Achievements section, a featured rank row, the Settings rank picker and the dashboard's next-rank milestone. **The rank tokens are marks, never text**: the name beside the crest stays ink, the crest is `aria-hidden` and never appears without the name, and **a rank the member has not reached is drawn in `ink-3`**, not its pigment. The old `text-honour` on the `/achievements` rank title is gone on purpose -- a rank is attendance, not "better than planned". Artisan and above cut their charges out with an SVG `mask`, so each crest takes a `useId` mask id: two crests on one page must never share one. Never key a crest by title or by a position you computed, and never let another role borrow a rank token.
- **Comebacks are bounded, local-calendar evidence rather than a streak penalty.** The achievements response derives them from the 500 most recent immutable `SESSION_COMPLETED` events: after 14 full local-calendar days away, three distinct active dates within an inclusive 14-day return window record one comeback. Same-day sessions count once, the response discloses when older history was outside the scan, and [comeback-recognition.tsx](features/achievements/comeback-recognition.tsx) states the evidence without penalising time away.
- **The routine review step carries a derived quality summary (ROUT-10), not advice.** [routine-quality.ts](features/routines/wizard/utils/routine-quality.ts) computes it client-side from the planned sets and the EXER-09 catalog metadata already in `['exercises']`: weekly muscle set-equivalents (primary 1.0 / secondary 0.5, the same weighting as Progress), a per-day duration estimate whose assumptions are printed under it, the equipment the routine needs checked against the default training location, and imbalance flags that always state their numbers. The thresholds are exported constants there. Location equipment is free text, so [exercise-equipment.ts](lib/utils/exercise-equipment.ts) maps it onto the catalog vocabulary through plurals and a small alias table; an unmatched item is reported as "not listed", never as missing from the gym. Nothing in the summary blocks saving.
- **Routines are weekly or a rotation (ROUT-11).** `Routine.scheduleMode` is `WEEKLY` (each day has a `dayOfWeek` and starts only on it, as before) or `ROTATION` (days have `dayOfWeek: null`, run in `order` and can be started any day). The backend reports `nextRotationDayId`, the day after the last completed session (an aborted one does not advance it); [routine-schedule.ts](lib/utils/routine-schedule.ts) turns it into the day the routine card, the detail page and the dashboard offer (`startableDayToday`), and [date.ts](lib/utils/date.ts) treats a null weekday as startable. Every day may carry a `name`: `routineDayLabel` from contracts names a day (its name, else its weekday, else "Day A"…) and the backend's history responses use the same rule, so snapshots taken before names existed keep their weekday. In the wizard a day's `slot` is its weekday on a weekly routine and its position on a rotation ([schedule.ts](features/routines/wizard/utils/schedule.ts)); switching modes keeps every day and its exercises, and either mode holds at most `ROUTINE_DAYS_MAX` (7) days.
- **Routine versions (ROUT-08) are immutable setups, compared client-side.** A version (contracts `RoutineVersionSetup`) copies everything a routine edit can change, loads included, without ids, because every edit replaces the stored days. [RoutineVersions](features/routines/components/RoutineVersions.tsx) on the detail page lists them through [useRoutineVersions](lib/api/hooks/useRoutineVersions.ts) (key `routineQueryKeys.versions(id)` = `['routine-versions', id]`, outside the `['routines']` prefix on purpose, since the list-shaped cache updates under it expect routines), saves one with an optional name, and compares one with the routine as it is now through the pure [routine-versions.ts](lib/utils/routine-versions.ts): days by weekday (by position when either side is a rotation), exercises by catalog exercise within a day. Restoring states the change first and is disabled with the reason when the server would refuse it (a live session of the routine, 20 versions kept, an identical setup); the server saves the replaced setup as a `BEFORE_RESTORE` version, and the response routine replaces the detail cache.
- **The weekly schedule (SCHED-01) composes existing reads and states its rules.** `/schedule` ([schedule-week-view.tsx](features/schedule/schedule-week-view.tsx)) shows one Monday-based week in the device's time zone, built by the pure `buildScheduleWeek` ([schedule-week.ts](lib/utils/schedule-week.ts)) from the cached routines, the sessions list for the week and the active session. The list read filters terminal sessions by their end, so the range runs one day past the week and sessions are placed by their start. A weekly day with no session of its routine that date is "Planned" from today on and "Not logged" before it, never "missed", and only from the routine's creation date; archived (`isCompleted`) routines plan nothing. Rest is never inferred: SCHED-07 stores `Routine.restDays`, the weekdays a weekly routine rests on by plan (chosen among its non-training weekdays in the wizard's schedule step, cleared for rotations and dropped when a weekday becomes a training day), shown as "Rest day" before and after today, never "not logged", and replaced by a session of that routine on the day. A rotation without training weekdays has no dates: the week notes its next day and shows its sessions where they happened. SCHED-06 stores `Routine.rotationWeekdays`, chosen in the builder's schedule step (a weekly routine's weekdays carry over when it becomes a rotation): `buildScheduleWeek` then places the rotation from today, its next day on the first training weekday without a session of the routine (a live session trains that day, so the plan continues after it) and the following days in order, and a past training weekday without a session is "Not logged" with no day name. Where the plan starts depends on today's sessions, so `useScheduleData` reads sessions from today even for a later range. The dashboard lists such a rotation only on its training weekdays; it still starts on any day. Planned days follow the routines as they are now, which the page says. Only a completed session uses `success`. SCHED-03 adds actions through the pure `scheduleEntryAction`/`rotationStartAction`: Start on today's planned weekly days and on each rotation's next day in the current week, Resume on the live session, and no Start while a session is live, because `useStartSession` would silently resume that one instead. Starting goes through `useStartSession`, which toasts its own failures, then opens the session; every Start is an outline row control.
- **What a date trains is resolved, never read off `routine.days` (ROUT-15).** A training block (ROUT-09, [RoutineTrainingBlocks](features/routines/components/RoutineTrainingBlocks.tsx)) in force on a date replaces the routine's schedule and days for that date: the owner's routine read carries every current revision as `Routine.trainingBlocks`, each with its own working-copy `days` (real ids, separate from the baseline `days`), and [routineOn](lib/utils/routine-schedule.ts) returns the routine as it trains on a date through contracts' `resolveRoutinePlan` -- the same resolver the backend's start, reminders and overrides use -- with `trainingBlock` naming the block or null. **Everything that decides what a date trains reads through it**: `buildScheduleWeek` plans each week in segments at block boundaries (a rotation counts its slots from the plan's own start, so a block begins on its first day and the baseline resumes at its own next day after it), `moveTargets` judges each date by its own plan, `useTodaysWorkouts`, the routine card and detail page, the routine-list start and `trainableDays` offer the days of today's plan. **The server refuses a day of any other plan (409)**, so offering a baseline day during a block is a bug, not a choice. Block names appear beside planned entries, on the dashboard row, the routine card and page (`describeBlockInForce`) and in history. **The live session screen trains the session's own snapshot day** (`sessionPrescription` in [session-prescription.ts](lib/utils/session-prescription.ts)), never the routine as it is now -- a block's day exists nowhere else -- and names the block through `sessionRoutineTitle`; a swap's "also use it" updates the block's working copy while one is trained, which the dialog says. Progression advances the block's copy, never the baseline.
- **The monthly calendar (PROG-05/SCHED-02) is the week rules, not new ones.** [schedule-month.ts](lib/utils/schedule-month.ts) builds each row of the Monday-based grid with `buildScheduleWeek` and marks a day with its strongest entry (completed, in progress, ended early, not logged, planned, rest); the totals and "Trained on X of Y days" count only the month's own days, trained days only up to today. The Schedule page toggles Week/Month over one [useScheduleData](features/schedule/use-schedule-data.ts) (routines, every page of the sessions list for the range, the active session), and choosing a day opens its week. Progress shows the same [ScheduleMonthView](features/schedule/schedule-month-view.tsx) as `ConsistencyCalendar`, with its own month and non-interactive cells. The month read spans the whole grid plus one day, for the same end-date reason as the week.
- **A moved or skipped workout is a per-date override, not a routine edit (SCHED-04/05).** `ScheduleOverride` rows (`GET`, `PUT /move`, `PUT /skip` and `DELETE /schedule/overrides`) are read by `useScheduleData` for the grid and by the dashboard for today, and `buildScheduleWeek` applies them to weekly routines only. A move turns the planned date into a neutral "Moved to <day>" entry, and the target carries the routine day of the planned weekday with `movedFrom`, planned or not logged like any other, with Start when it is today; a session of the routine on either date completes it, a move onto a rest day replaces the rest, and a move whose weekday the routine no longer trains on is ignored. A skip (kind `SKIP`, no target) turns the planned date into a neutral "Skipped" entry, never "not logged"; a session of the routine that date still shows, skipping a moved workout skips the occurrence, and a skipped date is free for another workout of the routine to move onto. The dashboard drops a workout moved away from or skipped for today. The pure `scheduleMoveAction` offers Reschedule on future weekly occurrences, Undo on a moved or skipped one (or when only putting it back remains) and Mark skipped on a passed day without a session up to `SCHEDULE_SKIP_PAST_DAYS` back; `moveTargets` lists the free days within `SCHEDULE_MOVE_MAX_DAYS` and `postponeTarget` the first of them after where the workout sits, all mirroring the server's checks. [move-occurrence-dialog.tsx](features/schedule/move-occurrence-dialog.tsx) postpones, moves, skips or puts it back. Rotations are never moved nor skipped: their plan re-flows from the next day.
- **Notifications (NOTIF-01) are gathered on read, not pushed.** `GET /notifications` first gathers the owner's achievements earned live (never a backfilled one), one note per finished session with new records or load changes and new followers from the last `NOTIFICATIONS_LOOKBACK_DAYS`, then returns the newest `NOTIFICATIONS_LIST_LIMIT` with the unread count. [NotificationBell](features/notifications/notification-bell.tsx) in the top bar links to `/notifications` and shows unread only through its dotted glyph and accessible name; restrained sidebar and schedule indicators are `NAV-05`. The page has Today ([today-actions.tsx](features/notifications/today-actions.tsx): the live session to resume, else today's planned workouts to start, from the dashboard's `useTodaysWorkouts`, never stored and never unread) and Updates ([notification-list.tsx](features/notifications/notification-list.tsx), worded by the pure `describeNotification` in [notifications.ts](lib/utils/notifications.ts), with "New" as a word, never colour alone). Opening one marks it read and Mark all read clears the rest, both through `useMarkNotificationsRead`, which updates the cache first with `markReadInCache`. Push delivery arrived with `NOTIF-02`/`NOTIF-03`, and reminders and per-category controls with `NOTIF-04`/`NOTIF-05`. **Partner encouragement (SOC-09)** is the one direct notification kind: an active partner sees the four fixed prompts on the recipient's profile only when that recipient granted `encouragement`; there is no free text or conversation surface.
- **Exercise alternatives (EXER-05) are suggestions, never automatic swaps.** [exercise-alternatives.ts](lib/utils/exercise-alternatives.ts) ranks catalog exercises sharing the target's `substitutionGroup` (near-identical) ahead of those sharing its `movementPattern` and at least one primary muscle. Equipment listed at the default training location only reorders them and names what is not listed; it never hides one, and an empty list counts as unknown. The routine wizard shows them at the top of an exercise card's change (pencil) dropdown, excluding exercises already on that day; choosing one swaps the exercise id and keeps its sets, rest, note and progression. It is a pure function so `LIVE-11` can reuse it for in-session substitution.
- **The Exercises catalog (EXER-02) filters client-side over existing reads.** `/exercises` filters the cached `['exercises']` catalog through [exercise-catalog.ts](lib/utils/exercise-catalog.ts): name, muscle (primary or secondary, primary matches first), one equipment item or everything listed at the default training location (bodyweight never needs listing; an empty list is unknown and never matches), movement pattern, and "Trained by me". Trained exercises and their last date come from `useTrainedExercises`, the existing `/workouts/progress/performance` read with `limit: 1` (its exercise list ignores the date range), so there is no backend read of its own. [use-catalog-filters.ts](features/exercises/use-catalog-filters.ts) mirrors the filters into the URL with `history.replaceState`; state drives every write and the URL is followed only for outside navigation, because composing writes from lagging search params once let a stale search write undo Clear filters. Instructions and media stay hidden until EXER-03/EXER-04, and the "Starred" filter arrived with EXER-07. Native selects share the [NativeSelect](components/ui/native-select.tsx) primitive with workout history.
- **The personal exercise page (EXER-01) composes existing reads.** `/exercises/<id>` ([exercise-detail.tsx](features/exercises/exercise-detail.tsx)) shows catalog facts, the owner's routine days that program the exercise ([exercise-detail.ts](lib/utils/exercise-detail.ts) over the cached `useRoutines` list), the current best set and estimated 1RM from the all-time strength read, recent performances and a progression history. The history is the Progress timeline narrowed by `exerciseId`, a filter added to `GET /workouts/progress/timeline` in `@sunsteel/contracts@0.31.0`; `ProgressTimeline` and `ExercisePerformanceHistory` take heading/copy props so the page reuses them. Every training section waits for `useTrainedExercises`, so an untrained exercise shows one honest empty state and never requests a history that would 404. Catalog rows and the Progress exercise heading link to the page.
- **Starred exercises (EXER-07) are private working preferences, not profile favorites.** `StarredExercise` rows (`GET /exercises/starred`, `PUT`/`DELETE /exercises/:id/star`, capped at `STARRED_EXERCISES_MAX`) are separate from the public `PROF-05` training-identity favorites and are never shown to other members. [StarToggle](features/exercises/star-toggle.tsx) stars from a catalog row or an exercise page through `useToggleExerciseStar`, which updates `['exercises','starred']` optimistically and restores it on failure. The catalog adds a "Starred" filter (`starred=1`), and both routine-wizard pickers group their options with [exercise-picker.ts](lib/utils/exercise-picker.ts): starred first, then up to five recently trained (from `useTrainedExercises`), then the rest; a search shows plain matches. The trained list loads when a picker mounts; until it arrives the recent group shows a "Loading recent exercises…" status row instead of appearing unannounced. Quick Workout is still hidden (`FIX-04`), so it has no picker to prioritize.
- **A swapped exercise is a session fact, not a routine edit (LIVE-11).** `WorkoutSession.exerciseSubstitutions` lists slots done with another exercise; the routine (and the backend snapshot) keep the prescription. The session screen builds its groups from the session's own snapshot day (`sessionPrescription`, ROUT-15) through `applySessionSubstitutions` ([session-substitutions.ts](lib/utils/session-substitutions.ts)), so a swapped slot shows and logs the substitute's `exerciseId`, keeps its rep targets and drops the prescribed load; "Last time" appears only when the previous set was the same exercise (`comparablePrevious`). The swap dialog ([exercise-swap-dialog.tsx](features/workout/exercise-swap-dialog.tsx)) ranks EXER-05 alternatives against the routine's own exercise plus a catalog search, can also update the routine, and is refused by the server once the slot has a completed set; the server clears that slot's incomplete drafts and returns the whole session, which replaces the cache. History overlays the swap in `buildExerciseGroups` with a "Swapped from" caption. Progression never advances a swapped slot.
- **Push notifications are optional and every unavailable case is stated (NOTIF-02/NOTIF-03).** [lib/utils/push.ts](lib/utils/push.ts) holds the pure availability rules and [usePushNotifications](lib/api/hooks/usePushNotifications.ts) the browser dance; the Settings card ([push-notifications-card.tsx](features/settings/push-notifications-card.tsx)) is the only place a permission prompt is raised, and only from a click — a prompt on load is the fastest way to be blocked for good. `resolvePushStatus` checks the iOS-install requirement **before** every other refusal: on iPhone and iPad, Web Push works only in a PWA added to the Home Screen (16.4+) and does nothing in a tab, so prompting there would spend the one prompt and deliver nothing. The server is the only holder of the VAPID public key and serves it with the subscription list, so a deployment without keys reports push unavailable rather than half-offering it. The worker ([worker/sw.ts](worker/sw.ts)) carries `push`, `notificationclick` (focus the open session rather than opening a second copy) and `pushsubscriptionchange` handlers. The rest alert ([useRestAlert](lib/api/hooks/useRestAlert.ts)) is fire-and-forget behind LIVE-01's in-app tone: it is scheduled against the same `deadline` the bar counts down to, re-sent when the timer is extended, cancelled when it is dismissed, and **only attempted for accounts with a subscribed device**, so an account that never enabled notifications makes no extra request per set. A failed schedule never toasts mid-workout. **No surface may promise a ticking countdown** — the Web Notifications API has no chronometer field on any platform, so the alert is a static line naming the next lift.
- **Notification controls are honest about what cannot reach you (NOTIF-05/NOTIF-04).** [notification-preferences.ts](lib/utils/notification-preferences.ts) holds the copy and the minute-of-day conversions; [NotificationControlsCard](features/settings/notification-controls-card.tsx) sits below the Notifications card, which owns whether the device is subscribed at all. Writes are **partial** through `useUpdateNotificationPreferences`, so two toggles changed in quick succession cannot overwrite each other with a stale whole object; omitting `quietHours` keeps the stored window while sending `null` clears it, and the two are different intents. `preferencesNotice` states the one thing worth saying above the controls -- no server keys, no subscribed device, or every category off -- because a switch left on with nothing to deliver it reads as working. There is **no channel control**: Web Push is the only delivery channel, so a channel switch would be the category switch again, and the NOTIF-01 centre is gathered on read rather than delivered. The reminder is a **time the owner picks**, never a countdown: the product stores training weekdays and no session hour, which the card says in as many words, and `describeReminder` is tested for not implying otherwise. The account time zone the server evaluates it in is already registered by `useWorkoutAnalytics` with `onlyIfUnset`, so no new write was needed.
- **Routine sharing separates two controls that are easy to confuse (ROUT-04).** [routine-sharing.ts](lib/utils/routine-sharing.ts) holds the copy; [RoutineSharing](features/routines/components/RoutineSharing.tsx) on the routine detail page owns both. **Visibility decides who can find the routine; a link opens it for whoever holds the link regardless**, which the card says rather than leaving an owner to assume a private setting hides an existing link. `describeVisibilityCap` names the account rule when it narrows what the routine asked for, so marking a routine public inside a followers-only account cannot read as publishing it. The public `/shared/routines/[token]` page is signed-out-safe, `noindex`, reads with `staleTime: 0` so a revoked link stops at once, and shows a revoked and a nonexistent link identically. [SharedRoutineView](features/routines/components/SharedRoutineView.tsx) renders the prescription with its target loads and says in as many words that it carries none of the owner's workouts, records or notes.
- **A featured routine is a fourth slot, and it links somewhere different depending on who is looking (PROF-08).** `FeaturedProfileItem` gained `kind: 'ROUTINE'`, carrying the ROUT-04 `SharedRoutineSummary`; [featured-accomplishments.tsx](features/profile/featured-accomplishments.tsx) renders it in the same ordered list as the other three and takes a `routineHref` rather than deciding the destination itself. [ProfileView](features/profile/profile-view.tsx) supplies it: **your own** featured routine opens `/routines/<id>`, the real page with its editing and sharing controls; **somebody else's** opens `/profile/<identifier>/routines/<routineId>`; the signed-out `/members` route passes none, for the same reason it passes no `relationshipHrefs` — reading one needs an account — and the row still names the routine. The Settings card offers only routines whose `effectiveRoutineVisibility` is not `PRIVATE` and repeats `describeVisibilityCap` on each, so an owner cannot feature a routine nobody else can open; the backend revalidates and omits a denied or narrowed slot at read time.
- **`/profile/<identifier>/routines/<routineId>` is a third sub-path of the optional catch-all, not a new route (PROF-08/ROUT-05).** `parseProfileRoutineId` in [routine-sharing.ts](lib/utils/routine-sharing.ts) reads it beside `parseRelationshipListKind`; anything else with three segments is still "Page not found" rather than a silent alias. [MemberRoutineView](features/routines/components/MemberRoutineView.tsx) reads `GET /users/:identifier/routines/:routineId` through `useMemberRoutine` with `staleTime: 0`, so a routine narrowed since the profile loaded stops resolving, and it shows a denied and a nonexistent routine identically. On your own profile the same URL redirects to `/routines/<id>`.
- **Cloning is offered wherever a routine can be read, and says what it is first (ROUT-05).** [CloneSharedRoutine](features/routines/components/CloneSharedRoutine.tsx) posts `/routines/clones` with **exactly one** source — `{ token }` from `/shared/routines/[token]`, `{ routineId }` from the member page — and on success invalidates `['routines']` and opens the new routine. The copy states the two things "clone" leaves open: the copy is independent (editing it never reaches the original owner) and it **starts private whoever could see the original**. The signed-out shared page offers a sign-in that returns to the link instead of a button that would fail, because the copy needs an account to own it.
- **Blocking and reporting say what they do, and what they do not (PROF-10).** [moderation.ts](lib/utils/moderation.ts) holds the copy; [MemberModerationMenu](features/profile/member-moderation-menu.tsx) sits beside Follow on another member's profile **inside the authenticated shell only** — the signed-out `/members` route has no viewer to act as. Blocking confirms first and states three things, because leaving any of them unsaid turns a safety control into a guess: it is **symmetric**, it **removes the follows in both directions**, and it **cannot withdraw a link already shared**, since a `SOC-07` or `ROUT-04` token carries no viewer identity — revoking the link is that control. After a block the viewer is routed away, because the profile they are standing on now answers 404. [BlockedMembersCard](features/settings/blocked-members-card.tsx) in Settings is the one place the whole list lives and repeats the link caveat, since that is where someone comes to check the block worked. The report dialog's receipt says the report was **recorded** and nothing about review or timing, and it did not change when `TRUST-04` shipped: a queue existing is not a promise about when anyone reaches a row in it, and a test asserts the copy has not drifted into one. `useModeration` invalidates `['users']`, `['routines']` and `['notifications']` on a block rather than patching one cache — a stale list still showing a blocked member reads as the control failing.
- **Moderation says what a reviewer may not see, rather than showing it (TRUST-04).** The review half of [moderation.ts](lib/utils/moderation.ts) holds the copy; `/moderation` ([report-queue.tsx](features/moderation/report-queue.tsx), [enforcement-record.tsx](features/moderation/enforcement-record.tsx), [review-action-dialog.tsx](features/moderation/review-action-dialog.tsx)) is a real destination rather than a Settings card, because it is a working surface with its own paging and its own record. **The sidebar entry and the page both key off `user.isModerator`**, which is on the owner's own profile read only and never on `PublicUserProfile`; that is presentation, not the control — every `/moderation` route answers 404 without the flag, so hiding the row is politeness rather than security. **The queue never fills in what the server withheld.** A subject arriving `isWithheld` is named by its kind alone, carries no title, no owner and no link, and `moderationSubjectHref` returns `null` for it — a link that 404s on arrival reads as a bug rather than as the rule working. **Opening a subject awaits its audit record before navigating**: `useRecordSubjectView` must resolve first, and a failure keeps the reviewer where they are rather than opening the content unlogged. **A reviewer opens the subject's own page** — `/profile/<id>`, `/routines/<id>`, `/shared/sessions/<token>` — never a moderation copy of it, so what they see is decided by the rules that decide it for everyone. Every action is confirmed in a dialog that states what it does *and does not* do: hiding leaves the owner their content, deletes nothing and can be restored. **The owner is told when something of theirs is hidden**, on the routine sharing card and in the session share dialog, because a routine that resolves for nobody while its own visibility still reads "Public" is a switch with nothing behind it. The review hooks invalidate the whole `['moderation']` prefix plus `['users']`, `['routines']` and `['activity']`, for the reason `PROF-10`'s block does.
- **A cloned routine says so (ROUT-06).** `Routine.lineage` arrives resolved: the server has already applied `canViewRoutine`, so the frontend never decides what a viewer may learn about a source. [RoutineLineageNote](features/routines/components/RoutineLineageNote.tsx) under the routine header names the original author and links to `/profile/<username>/routines/<id>` when `isSourceHidden` is false, and otherwise **still says the routine is a clone** whose original is unavailable — staying silent would quietly present somebody else's programme as original work. A routine that was never cloned has no `lineage` and renders nothing.
- **Discovery is a separate destination, and says what it is (ROUT-07).** `/routines/discover` ([RoutineDiscovery.tsx](features/routines/components/RoutineDiscovery.tsx)) has its own sidebar entry because the Routines page is about **your own** routines and folding discovery in would hide it. It states its scope once at the top — it lists what the viewer could already open from a profile, so browsing reveals nothing that was private — and labels every session length an **estimate**, since the number comes from `ROUT-10`'s rule applied to the prescription rather than from anyone's workouts. A truncated scan says so rather than looking like the whole catalogue. Filtering happens server-side; the client only serializes, and `buildDiscoveryParams` **repeats** `equipment` rather than joining it, because a comma-joined value would arrive as one nonexistent equipment item. `describeClassification` returns `null` for an unclassified routine instead of rendering "Not specified" — a routine is not worse for making no claim. The wizard's basic-info step is where `goal` and `experienceLevel` are declared, both optional and both sent as `null` when cleared, so editing a routine back to unspecified actually removes the claim rather than leaving the old one.
- **Activity is generated, never composed, and who sees it is the server's decision (SOC-03/SOC-04).** `/activity` has two views: Following ([activity-feed.tsx](features/activity/activity-feed.tsx)), the members the viewer follows, and Yours ([own-activity.tsx](features/activity/own-activity.tsx)), every entry of the owner's with a per-entry audience select and a "See it as" toggle whose follower and other-member previews come from `GET /activity/mine/preview` -- the same read an audience gets, not a client-side filter. Another member's profile shows their activity through [member-activity.tsx](features/profile/member-activity.tsx), passed into `ProfileView` as the `activity` slot, so the signed-out `/members` route, which passes none, never shows activity; "Everyone" therefore means every signed-in member, and the copy says so. Defaults per kind live in Settings ([activity-sharing-card.tsx](features/settings/activity-sharing-card.tsx)), a draft saved in one step because a default reaches past entries too; every kind starts at Only me. **Never decide visibility or links in the client**: each entry's `link` arrives resolved (a record only while it is the profile's current best, a session or load progression never for another member) and [activity.ts](lib/utils/activity.ts) only maps it to a route; `sharing.cappedBy` names the profile section or the routine that narrowed a choice, and the row must state it rather than let a wider choice look honoured. `groupActivity` joins one author's facts of one workout into one row even when another member falls between them. Withdrawing is choosing Only me on the row; there is no second state. `ActivityFact` icons stay ink -- a feed can hold many records, and honour is capped at two marks per viewport. **Reactions (`SOC-05`) hang off the same entry** ([activity-reactions.tsx](features/activity/activity-reactions.tsx)): four controls under each fact in the feed and on a member's profile, ghost because they repeat on every entry, with the viewer's choice carried by `aria-pressed` and an accessible name that also says choosing it again removes it — never colour alone. A count renders only when somebody reacted, so no zero is shown, and your own activity gets the counts without controls because the server refuses a reaction to your own entry. The summary arrives **inside** the entry, so a reaction can never appear on something the viewer was not allowed to see, and `useSetActivityReaction` patches every loaded activity query, because one entry can be on screen in the feed, a profile and the owner's list at once.
- **The dashboard's social preview is the feed, not a copy of it (DASH-08).** [FollowingPreview](<app/(protected)/dashboard/components/FollowingPreview.tsx>) sits last on the dashboard, below every training section, and calls `useActivityFeed` -- the same key and cache as `/activity`, so opening Activity after the dashboard is served from cache and the server's `SOC-04` audiences decide every entry. [dashboard-following.ts](lib/utils/dashboard-following.ts) only bounds it: three `groupActivity` rows of at most two facts, the remainder counted as "and N more from this workout". It never fetches another page, and it renders `ActivityFact` with `discussion={false}`, so reactions and comments stay on `/activity`. Its empty states are `describeEmptyFeed`'s. Do not give it its own query, a `limit`, or its own gathering: a second key would drift from the feed that every activity mutation patches.
- **Comments are discussion on one verified fact, and the entry read is their only gate (SOC-06).** [activity-comments.tsx](features/activity/activity-comments.tsx) renders under every entry in the feed, on a member's profile and in the owner's list. **The list is collapsed until asked for**: an entry carries only its count, and a feed page that fetched every comment on every entry would read the whole conversation to show a number — opening one is what loads it. There are **no threads, no mentions, no editing and no reactions on comments**; adding any of them is a new decision rather than an extension of this one. `canDelete` arrives resolved, for the same reason a `SOC-03` link does: the server decides what a viewer may do, and a control the server would refuse must never be rendered. Two people see it — the author and the owner of the activity — so the confirmation says which of the two deletions the viewer is making, and it is an `AlertDialog` like every other destructive confirmation rather than a native `window.confirm`, which appears nowhere in this product. **The composer is replaced by its reason when the daily budget is spent**, because a control that is present and refuses on click reads as broken. `useActivityComments` reads with `staleTime: 0` so a comment deleted by its author or hidden by a moderator stops showing at once, and the mutations patch the count into every loaded activity query — one entry can be on screen in three places at once — while invalidating the list itself. Reporting one reuses `ReportDialog` with `subjectKind: 'COMMENT'`; `moderationSubjectHref` returns `null` for a comment on purpose, since it has no page of its own and the queue already shows its text.
- **Avatars live in `avatars/<Supabase auth uid>/<timestamp>.jpg`, and only the member may write there (PROF-01).** The bucket and its policies are a versioned migration, [supabase/migrations/20260923103003_avatars_bucket.sql](supabase/migrations/20260923103003_avatars_bucket.sql) -- the project's first; it had **no bucket at all** until 2026-09-23, so every upload since PROF-01 shipped had failed with "Bucket not found". Reads are public (a public bucket bypasses RLS for `/object/public/*`); insert, update, list and delete are allowed to `authenticated` only inside the folder whose name is `auth.uid()`. **The folder is the auth uid, never the local `User.id`**, because `auth.uid()` is all RLS can see -- the old code named files after `user.id` from `useUser()`, which no policy could have matched. [avatar-storage.ts](lib/utils/avatar-storage.ts) owns the path; each upload gets a new name, because a public URL is cached and a new name is a new image everywhere at once. [useUploadAvatar](lib/api/hooks/useUploadAvatar.ts) uploads, points the profile at the new URL, then removes the member's older files; if the profile update fails it removes the file it just stored. [cropImage.ts](lib/utils/cropImage.ts) scales the crop down to 512px (a phone crop is thousands of pixels and several MB), and the bucket refuses anything over 2 MiB or not JPEG/PNG/WebP. The backend's account deletion removes the whole folder with the service role. A bucket or policy change is a new file in `supabase/migrations/`, applied to the project and recorded there -- never a dashboard click.
- **Notes are about this workout and never touch the routine (LIVE-16).** [session-notes.tsx](features/workout/session-notes.tsx) holds `ExerciseNoteButton` (a ghost icon on every exercise of the live session and of a finished one's history page, its state in its accessible name) and `WorkoutNoteButton` (the session screen's Workout Note section and the recap's notes, through `SessionRecapPanel`'s `notesAction`); both save through `useUpdateSessionNotes`, which patches `session.notes`/`session.exerciseNotes` from the server's answer and refetches the recap and history lists. **The routine's exercise note is the standing instruction**: `ExerciseGroup` shows it read-only as "Routine note" and the dialog repeats it above the field. The session screen's old note button wrote that routine note, so a remark about today silently replaced the instruction for every later workout; `useUpdateExerciseNote` and `routineService.updateExerciseNote` went with it. The routine wizard still edits routine notes in its own form state. Exercise notes appear in the recap under Session notes and in a shared link only when notes are shared.
- **A finished workout is corrected on its history page, and the server re-derives the rest (LIVE-17).** [session-corrections.tsx](features/workout/session-corrections.tsx) puts a Corrections section under the recap on `/workouts/history/<id>`: while `GET /workouts/sessions/:id/corrections` reports the window open (the latest completed workout, 48 hours, until another starts) it says until when and offers **Correct sets**, which swaps the exercise list for an editor of the logged sets (weight in the viewer's unit, reps, RPE, done); the trail of saved corrections shows on any workout that has one, and an older workout shows nothing at all. [session-corrections.ts](lib/utils/session-corrections.ts) owns the copy and the draft: **an untouched weight is sent back exactly as stored**, because a pound value rounded for the box would otherwise convert back as a correction nobody made, and only changed sets are sent. Saving goes through a confirmation that lists each change and states what is recalculated and what leaves the activity feed. [useCorrectSession](lib/api/hooks/useWorkoutSession.ts) invalidates `['workout']` (the recap is cached forever otherwise), `['routines']`, `['achievements']`, `['activity']`, `['notifications']` and `['users']`, since the server rewrote records, totals, loads and activity; `useSessionCorrections` reads with `staleTime: 0` because the window closes with the clock. Never re-derive anything client-side.
- **Download Your Data is the step before Delete Account, and the file is the server's (EXPORT-01).** [DownloadDataCard](features/settings/download-data-card.tsx) sits directly above the deletion card, which points to it. `GET /users/me/export` needs the bearer token, so it cannot be a plain link: [useExportAccount](lib/api/hooks/useExportAccount.ts) fetches the contracts `AccountExportV1`, and [account-export.ts](lib/utils/account-export.ts) names it (`accountExportFileName` from contracts: `sunnsteel-<username>-<date>.json`) and writes it as indented JSON before a Blob download. **The browser never edits the document** -- what the file contains and leaves out is decided by the backend, which also states it in the file's `omitted` list. The server allows five exports per ten minutes, so the toast on failure shows its message rather than retrying.
- **Account deletion is immediate and leaves by a full page load (TRUST-01).** [DeleteAccountCard](features/settings/delete-account-card.tsx) is the last card in Settings; its own control is an outline that only opens the confirmation, and the one control that deletes is the `destructiveSolid` submit inside the dialog, disabled until the username is typed (`matchesDeletionConfirmation` from contracts, the same comparison the server makes). The dialog cannot be dismissed while the request is in flight. A moderator sees why the account cannot be deleted instead of a control, because the server refuses it. The copy in [account-deletion.ts](lib/utils/account-deletion.ts) lists what goes, the one thing that stays and points to **Download Your Data** directly above (`EXPORT-01`); a test keeps it from ever promising a restore. On success [useDeleteAccount](lib/api/hooks/useDeleteAccount.ts) clears the marker, signs out **locally** (the Supabase user no longer exists, so a network sign-out could only fail) and then `window.location.replace`s to `/login?account=deleted`, which shows the notice. The full load is deliberate: the protected layout also redirects the moment the session is gone, and a client-side race between the two can drop the notice. **Never submit this form in a local check with the owner's account**: the local backend deletes the sign-in in the real Supabase project.
- **A Tailwind variant that does not exist emits no CSS and no error.** It passes `tsc`, passes `eslint`, and silently drops the rule — `hidden xs:flex` therefore meant `hidden` at every width for months, because `xs` is not a breakpoint here (v4 defaults are `sm`-`2xl` and there is no config file). When a responsive or state class "does nothing", grep the compiled stylesheet: `.next/static/css/*.css` after a build, and note there are **two** bundles — the small one is not the main one. That is how TD-28 and TD-29 were found and how their fixes were verified.
- **Tailwind v4 emits `translate` and `rotate` as their own CSS properties, not as `transform`.** A node carrying `-translate-x-full` or `rotate-180` computes `transform: none`, so a check that reads `getComputedStyle(el).transform` reports no motion on an element that is visibly animating - read `.translate` and `.rotate` instead, or the bounding box. The transition still works because Tailwind's `transition-transform` lists all four (`transform, translate, scale, rotate`). Verified 2026-09-17 while confirming the mobile drawer slide: the drawer read `transform: none` in both states while `translate` went `-100%` -> `-9.13%` -> `0` across the 300ms enter.
- **A breakpoint fires at a _viewport_ width, but the protected shell hands its
  main column `viewport - 256`.** The sidebar takes 256px, so `md:` (768) leaves
  **512px** of content and `lg:` (1024) leaves 768px. Every multi-column rule
  inside the shell is one step more optimistic than it reads. This has caused
  three separate defects: a dashboard stat band overflowing at 768 because a
  third column and a type step share that breakpoint; the routine wizard's
  horizontal stepper running 88px past the edge at 768; and the settings form
  collapsing to ~60px field cells, rendering a select as "Ki". **Budget
  `viewport - 256`, and verify at the breakpoint itself rather than above it** -
  all three were invisible at 320 and at 1440.
- **The active session screen already has a rest timer and a wake lock.** [hooks/use-rest-timer.ts](hooks/use-rest-timer.ts) counts down from an absolute deadline (never accumulated ticks, so backgrounding cannot make it drift) with its pure arithmetic and tests in `lib/utils/rest-timer.utils.ts`; [hooks/use-screen-wake-lock.ts](hooks/use-screen-wake-lock.ts) re-acquires on `visibilitychange` because the browser drops the lock whenever the document hides. Don't reimplement either.
- **Set-log upserts return a mutation result, not a bare set.** Since LIVE-07, `PUT /workouts/sessions/:id/set-logs` returns `UpsertSetLogResponse` with `setLog` plus independently earned weight, rep, volume and estimated-1RM records. Reconcile the session cache with `setLog`; celebrate only `earnedRecords`. The backend suppresses identical/RPE-only repeats by comparing the row's pre-mutation values.
- **Stale active sessions are recoverable, never silently expired.** The backend no longer auto-aborts them. [features/workout/stale-session-recovery-dialog.tsx](features/workout/stale-session-recovery-dialog.tsx) opens globally after 48 hours without activity and requires Resume, finish saved work, or discard. The active-session response does not include `setLogs`, so the dialog conditionally reads the session detail only after it becomes stale. Resume must go through the idempotent start endpoint, which refreshes `lastActivityAt`; returning only the cached active query makes the dialog reopen.
- **Workout history has its own sidebar entry, and needs one.** `/workouts` `router.replace`s into the live session whenever one exists ([app/(protected)/workouts/page.tsx](<app/(protected)/workouts/page.tsx>)), and that page's "View History" button only renders in the no-session branch — so the `History` item in [Sidebar.tsx](features/shell/components/Sidebar.tsx) is the only route to `/workouts/history` while training. Don't fold it back into the `Workouts` item.
- **The service worker has no `/api/*` branch, on purpose.** One existed and was unreachable — the backend is cross-origin and the only same-origin `/api` route accepts POST/DELETE only, while the handler returns early on non-GET. Verified in the browser: `ss-api-*` never got created. See TD-13.
- **Service-worker activation only deletes caches it owns.** The generated worker removes the hand-written worker's `ss-{runtime,precache,pages}-vN` namespaces once (`isLegacyWorkerCache`) and Serwist's own outdated precache; never restore a "delete every cache except ours" filter: Cache Storage is origin-wide, so that could erase unrelated application caches. Updates do not call `skipWaiting()` from `install`; the page sends `SKIP_WAITING` to the waiting worker when it is safe to reload. See TD-25 and TD-44. **Known limitation, observed with the old worker too:** in headless Chromium the waiting worker sometimes does not take over when the user leaves a workout, because the active worker still reports work; the update then applies at the next launch. It never activates or reloads mid-workout.
- **Sync `node_modules` with `npm ci`, never `npm install` — any npm command that rewrites the lock on Windows can drop another platform's entries.** npm resolves only the win32 branch of optional platform packages and drops the `@emnapi/*` dependencies of the wasm32 variants whenever it rewrites the lock. A bare `npm install` after a pull did it on 2026-09-13. `npm ci` on Linux then fails with a bare `EUSAGE`, and `npm ci --dry-run` on Windows cannot reproduce it. `npm ci` never writes the lock. Three guards catch the rest. First, the root `postinstall` repairs the lock after a bare `npm install`: it restores the missing entries, unchanged, from `HEAD`, and never fails the install. Second, `npm install <pkg>` and `npm uninstall` skip `postinstall`, so the pre-commit hook in `.githooks/` (activated by `prepare`) refuses a staged lock that fails the check; run `npm run lock:repair` and re-stage. Third, `lock:check` is the first step of `verify` and of CI. Never delete the lock to regenerate it: that drifts the tree. See TD-33.

Active technical debt, with evidence and closure criteria, is tracked in [docs/roadmaps/technical-debt.md](docs/roadmaps/technical-debt.md). Read it before optimizing anything performance-related. Use the closed audit linked from that register only when older decision evidence is needed.

## Documentation rule

**Never create doc files at repo root** (except this file, `CLAUDE.md` and `README`). All project docs live under `docs/` (`docs/roadmaps/`, `docs/history/`, `docs/reference/`). The `docs/` tree was fully deleted at some point and is being rebuilt — check what exists before assuming a path. This ban covers **this repository's root only**. Cross-repository documents may live in the parent workspace folder (`sunsteel/`) as the deliberate exception documented below, and must never be moved into this repo.

Before proposing or implementing product features, read the shared [product vision](../PRODUCT_VISION.md) for enduring direction, then [docs/roadmaps/product-roadmap.md](docs/roadmaps/product-roadmap.md) for approved scope and delivery state. The roadmap is the canonical cross-repository registry for shipped capabilities, the active queue, dependencies, deferred work and retained product decisions. The vision does not approve or schedule features. Verify the code before changing a feature to `SHIPPED`; performance defects and cleanup remain in [docs/roadmaps/technical-debt.md](docs/roadmaps/technical-debt.md).

Several agents work in parallel, each in its own git worktree. Before starting a roadmap item, read the roadmap's **In-flight work** table on `main`: never start a claimed item or one whose modules overlap a claim, and claim yours on `main` (status plus a row with owner and branch) before creating the worktree. A status changed only inside a feature branch is invisible to the other agents.

## Shared workspace docs (monorepo parent folder)

`../PRODUCT_VISION.md` (enduring product direction), `../FEATURES.md` (product-facing shipped summary), `../TECH_STACK.md` (technical/portfolio-facing) and `../ARCHITECTURE.md` (topology, one request end to end, the auth flow and engineering decisions) live in the parent workspace folder (`sunsteel/`), **outside this repository**. `PRODUCT_VISION.md` guides trade-offs but is not a roadmap or implementation specification. The other three are derived documents: the code and [docs/roadmaps/product-roadmap.md](docs/roadmaps/product-roadmap.md) remain the sources of truth for implemented behavior and delivery state.

Change `PRODUCT_VISION.md` only when the owner deliberately changes the product direction; do not update it mechanically when a feature ships. Update the three derived documents in the same change when: a feature moves to `SHIPPED` in the roadmap, an active user-facing feature is removed or hidden, a dependency/CI/build script changes, or an architecture decision recorded in this file changes. Update `ARCHITECTURE.md` in the same change when the system topology changes (a node, data store, job, external service or dependency is added, removed, or starts or stops being used), when the request flow it traces changes (layering, guard/pipe order, caching defaults, mappers), when the auth flow changes (the `ss_session` marker or the per-request token verification in `SupabaseJwtGuard`), or when one of its recorded engineering decisions changes; never invent a decision's rationale — ask the owner when the code and docs do not record it. Keep `FEATURES.md` free of technical details, give every bullet its roadmap ID as an HTML comment (e.g. `<!-- LIVE-04 -->`) kept current when features are added, merged or re-scoped, and bump its "Last updated" date whenever it changes. Keep `TECH_STACK.md` and `ARCHITECTURE.md` free of unverified claims; update their "Last verified" dates only when actually verified against code. Do not sync them for refactors, fixes, or in-progress work with no user-visible or stack-visible effect.

`CLAUDE.md` is the Claude Code-facing twin of this file. **Keep the two in sync**: if you change one, mirror the change in the other.
