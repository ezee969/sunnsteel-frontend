# Sunnsteel Product Roadmap

Last verified against the frontend, backend, and shared contracts: **2026-09-13**.

This is the canonical product feature registry for Sunnsteel. It tells humans and
coding agents what the product already does, what is next, which ideas are only
candidates, and which foundations must exist before larger features are started.

This document is not a technical-debt log. Performance defects, cleanup, and
implementation hazards belong in [technical-debt.md](technical-debt.md). The code
remains the final source of truth when it disagrees with this file.

## How to use this document

Before proposing or implementing product work:

1. Read the current product snapshot and active queue.
2. Find the feature by its stable ID; do not create a duplicate entry.
3. Verify the relevant code before changing a status to `SHIPPED`.
4. Claim an item before implementation starts: on `main`, move it to
   `IN_PROGRESS` and add a row to [In-flight work](#in-flight-work). Never start
   an item that already has a claim there. See that section for the rules.
5. When it ships, add the completion date and links to the relevant code, PR, or
   migration in the completion log.
6. If a feature changes API shapes, update and publish `@sunsteel/contracts`
   before consuming the new version in the frontend or backend.

### Statuses

| Status        | Meaning                                                           |
| ------------- | ----------------------------------------------------------------- |
| `SHIPPED`     | Verified in the current code.                                     |
| `IN_PROGRESS` | Actively being implemented.                                       |
| `NEXT`        | Selected as the immediate product work.                           |
| `QUEUED`      | Approved direction with an established order.                     |
| `CANDIDATE`   | Worth retaining, but not committed to the queue.                  |
| `BLOCKED`     | Cannot proceed until a stated dependency or decision is resolved. |
| `DEFERRED`    | Intentionally postponed.                                          |
| `REJECTED`    | Evaluated and declined; retain the reason.                        |

Repository abbreviations: **FE** = `sunnsteel-frontend`, **BE** =
`sunnsteel-backend`, **CT** = `sunnsteel-contracts`.

### Sizes

Size is structural, not an estimate: it is derived from how far a change reaches,
not from how long anyone thinks it takes. It exists so the queue cannot hide a
quarter of work behind a group that looks like the one before it.

| Size | Reach                                                                                                             |
| ---- | ----------------------------------------------------------------------------------------------------------------- |
| `S`  | One repository, no contract publish, no migration, no new model.                                                  |
| `M`  | One or two repositories, may publish contracts, no data migration.                                                |
| `L`  | Cross-repository with a schema change, migration or backfill, or a new surface others depend on.                  |
| `XL` | A new subsystem, or work gated on policy, moderation, licensed content or infrastructure that does not exist yet. |

An `XL` is a signal to decompose before scheduling, not a licence to start.

## In-flight work

More than one agent works on this product at the same time, each in its own git
worktree. A status changed only inside a feature branch is invisible to everyone
else until it merges, so claims live here, on `main`.

- **Claim before you start.** On `main`, set the item's status to `IN_PROGRESS`
  (or `NEXT` when reserving it for your next slice) and add a row below. Do this
  before creating the worktree.
- **Never start a claimed item**, and avoid items whose repositories and modules
  overlap a claimed one. Pick something else or ask the owner.
- **Release the claim when the item ships or is abandoned.** Remove the row in
  the same change that marks it `SHIPPED`, or revert the status if the work
  stops.
- **`@sunsteel/contracts` versions are serialized.** When two claims both need a
  publish, the second to publish rebases on the first and takes the next version.

| ID       | Status        | Owner  | Branch / worktree                                  | Claimed    | Repositories |
| -------- | ------------- | ------ | -------------------------------------------------- | ---------- | ------------ |

## Current product snapshot

| ID       | Status    | Capability                                                                                                                                        | Evidence / notes                                                                                         |
| -------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| CORE-01  | `SHIPPED` | Email/password and Google authentication, protected routing, and recoverable session-marker cleanup                                               | FE auth provider, auth controller, middleware, and auth tests                                            |
| CORE-02  | `SHIPPED` | Installable PWA shell with production service worker, safe updates, generated icons, and development cleanup                                      | FE PWA provider, service-worker policy, and `public/sw.js`                                               |
| CORE-03  | `SHIPPED` | Responsive protected shell with collapsible navigation, global user search, profile menu, and light/dark themes                                   | FE protected layout and shell components                                                                 |
| PROF-01  | `SHIPPED` | Editable name, age, sex, height, weight, weight-unit preference, and cropped avatar upload                                                        | FE settings; BE users module; CT user contracts                                                          |
| PROF-02  | `SHIPPED` | Public profile lookup with follower/following counts                                                                                              | FE profile; BE `GET /users/:id`                                                                          |
| SOC-01   | `SHIPPED` | User search plus follow and unfollow relationships                                                                                                | FE search/follow hooks; BE users service; `UserFollow` model                                             |
| ROUT-01  | `SHIPPED` | Routine creation, editing, deletion, favorites, and list filtering; a completion field exists but is not presented as meaningful program progress | FE routine pages and wizard; BE routines module                                                          |
| ROUT-02  | `SHIPPED` | Routine days, exercise ordering, fixed/ranged reps, target weight/RIR, rest time, notes, and per-exercise progression                             | CT routine contracts and BE routine schema                                                               |
| LIVE-00  | `SHIPPED` | Start/resume a session, log completed sets, save per-set state, finish/abort, and apply configured progression                                    | FE workout session; BE workout services                                                                  |
| HIST-01  | `SHIPPED` | Paginated workout history with status, routine, date, text, and sort filters                                                                      | FE workout history; BE session listing                                                                   |
| DASH-01  | `SHIPPED` | Today's scheduled workouts, weekly statistics, recent sessions, personal records, streaks, and lifetime volume                                    | FE dashboard; BE stats/progress endpoints                                                                |
| PROG-01  | `SHIPPED` | Exercise strength trends by lift and rolling date range, with best-set load, estimated 1RM and exact record changes                               | FE `/progress`; BE indexed event read; CT trend response                                                 |
| PROG-02  | `SHIPPED` | Paginated per-exercise history with terminal sessions, completed sets, RPE, session and prescription notes, and progression changes               | FE `/progress`; BE indexed session read; CT history response                                             |
| PROG-03  | `SHIPPED` | Four-, eight- or twelve-week muscle-group heatmap with primary and secondary set weighting                                                        | FE `/progress`; BE projection rollup read; CT heatmap response                                           |
| PROG-04  | `SHIPPED` | Four-, eight- or twelve-week external-load volume comparisons by total, muscle, historical routine and exercise                                   | FE `/progress`; BE bounded aggregate read; CT volume response                                            |
| PROG-06  | `SHIPPED` | Latest-two comparison for any preserved routine day across session metrics, notes and exact exercise sets                                         | FE `/progress`; BE bounded snapshot comparison; CT response                                              |
| PROG-07  | `SHIPPED` | Cursor-paginated record and prescription-change timeline with reasons, historical routine context and session recap links                         | FE `/progress`; BE indexed event read; CT timeline response                                              |
| PROG-08  | `SHIPPED` | Up to eight private, measurable frequency, workload, streak, strength and current-body-weight goals with exact current/target/gap progress        | FE Settings/Progress; BE persisted goals and bounded aggregate; CT `@sunsteel/contracts@0.27.0`          |
| PROG-09  | `SHIPPED` | Plateau watch for recently trained lifts with no new best in four or more sessions, with the evidence and thresholds stated                       | FE `/progress` and exercise page; BE bounded plateau read; CT `@sunsteel/contracts@0.34.0`               |
| PREF-05  | `SHIPPED` | Per-account plateau sensitivity: three to eight sessions without a new best, default four                                                         | FE Plateau watch control; BE `User.plateauMinSessions`; CT `@sunsteel/contracts@0.35.0`                  |
| ROUT-11  | `SHIPPED` | Weekly or rotation routines with optional day names; a rotation offers its next day on any weekday                                                | FE wizard, routine card/detail and dashboard; BE `Routine.scheduleMode`; CT `@sunsteel/contracts@0.36.0` |
| ROUT-08  | `SHIPPED` | Named, immutable routine versions, compared with the routine day by day and restored with the replaced setup saved first                          | FE routine detail; BE `RoutineVersion`; CT `@sunsteel/contracts@0.38.0`                                  |
| SCHED-01 | `SHIPPED` | Weekly schedule of planned routine days beside completed, ended-early and live sessions                                                           | FE `/schedule` over the existing routines and sessions reads                                             |
| SCHED-03 | `SHIPPED` | Start today's planned days and the next rotation day, or resume the live session, from the Schedule page                                          | FE `/schedule` actions over the existing start flow                                                      |
| SCHED-07 | `SHIPPED` | Planned rest days on weekly routines, shown on the schedule instead of empty days                                                                 | FE wizard, schedule and routine card; BE `Routine.restDays`; CT `@sunsteel/contracts@0.37.0`             |
| SCHED-06 | `SHIPPED` | Rotations on chosen training weekdays, placed on the schedule in order from today instead of an undated note                                      | FE schedule, builder and dashboard; BE `Routine.rotationWeekdays`; CT `@sunsteel/contracts@0.39.0`       |
| SCHED-04 | `SHIPPED` | Move one planned weekly workout to another day, or put it back, without changing the routine                                                      | FE schedule, month and dashboard; BE `ScheduleOverride`; CT `@sunsteel/contracts@0.40.0`                 |
| SCHED-05 | `SHIPPED` | Skip a planned weekly workout, or postpone it to the next free day; a skip never reads as not logged                                              | FE schedule, month and dashboard; BE `ScheduleOverride` `SKIP`; CT `@sunsteel/contracts@0.41.0`          |
| SCHED-02 | `SHIPPED` | Monthly Schedule view beside the week, with the month's totals and trained days, opening any day's week                                           | FE `/schedule` month view over the existing routines and sessions reads                                  |
| PROG-05  | `SHIPPED` | Consistency calendar in Progress: completed, ended-early, not-logged, planned and rest days by month                                              | FE Progress section sharing the Schedule's month view and reads                                          |
| ACH-01   | `SHIPPED` | Verified milestones for completed sessions, sets, external-load volume, current record frontiers and best training streaks                        | FE `/achievements`; BE event-backed award/read; CT `@sunsteel/contracts@0.29.0`                          |
| ACH-02   | `SHIPPED` | Six attendance-based Renaissance ranks from Initiate to Laureate, with the current rank and exact next requirements                               | FE `/achievements`; BE active-week/session derivation; CT `@sunsteel/contracts@0.30.0`                   |
| ACH-04   | `SHIPPED` | One stable next milestone per achievement category with exact evidence, completed-category states and safety-conscious guidance                   | FE `/achievements`; BE verified category totals; CT `@sunsteel/contracts@0.32.0`                         |
| ACH-05   | `SHIPPED` | Comeback recognition after 14 full inactive days and three distinct return-training dates inside an inclusive 14-day window                       | FE `/achievements`; BE bounded completed-event derivation; CT `@sunsteel/contracts@0.42.0`               |
| NOTIF-01 | `SHIPPED` | Notification center: a top-bar bell, today's actions and read-state updates for achievements, session progress and followers                      | FE shell, notifications page; BE `Notification`; CT `@sunsteel/contracts@0.43.0`                         |
| EXER-02  | `SHIPPED` | Exercise catalog browsing by name, muscle, equipment or the default gym, movement pattern and personal training history                           | FE `/exercises`; existing BE catalog and performance reads                                               |
| EXER-01  | `SHIPPED` | Personal exercise page with routine usage, best set, estimated 1RM trend, recent performances and a per-exercise progression history              | FE `/exercises/[id]`; BE timeline `exerciseId` filter; CT `@sunsteel/contracts@0.31.0`                   |
| EXER-07  | `SHIPPED` | Private exercise stars, a starred catalog filter and starred/recent-first routine wizard pickers                                                  | FE catalog, exercise page and wizard pickers; BE `StarredExercise`; CT `@sunsteel/contracts@0.33.0`      |

## Immediate product-integrity work

These are current correctness or honesty gaps, not optional enhancements.

They are split by size on purpose. The first table is frontend-only work that
removes false statements from screens that already exist; it can ship as one
change with no contract publish and no backend. The second table holds gaps that
a feature owns instead: either the small honest fix is a step toward it, or --
as with `FIX-05` -- there is no honest short version and the surface stays
unfinished until the real capability lands. Do not let the first table wait for
the second.

The first table shipped on 2026-09-07 and is retained as a record of what was
corrected and where. `FIX-03` then closed the remaining actionable integrity gap;
`FIX-05` shipped with its owning public-profile capability in `PROF-12`.

### Honesty fixes — frontend only

| ID     | Status    | Size | Work item                          | Expected behavior                                                                                                                                                                                               | Repositories |
| ------ | --------- | ---- | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| FIX-01 | `SHIPPED` | S    | Remove fabricated profile content  | Profiles must not claim that every user has the same biography or lives in San Francisco. Show real optional data or an honest empty state.                                                                     | FE           |
| FIX-02 | `SHIPPED` | S    | Remove the unconditional Pro badge | Display a membership/title badge only when backed by a real account state.                                                                                                                                      | FE           |
| FIX-06 | `SHIPPED` | S    | Stop implying interactive counts   | Follower and Following counts must not render as interactive while they lead nowhere. Browsable relationship lists, and the paginated endpoint they need, belong to `SOC-02`.                                   | FE           |
| FIX-07 | `SHIPPED` | S    | Align search copy with behavior    | Do not claim username search until unique usernames exist; avoid presenting email as a public identity.                                                                                                         | FE           |
| FIX-08 | `SHIPPED` | S    | Replace fixed dashboard goals      | Label weekly and lifetime targets as generic milestones until `PROG-08` supplies real user goals.                                                                                                               | FE           |
| FIX-09 | `SHIPPED` | S    | Unify duration formatting          | `formatDuration` is now the single human-readable duration formatter across the active session, dashboard, history list and history detail; hour rollover keeps long sessions readable (for example, `22h 3s`). | FE           |
| FIX-10 | `SHIPPED` | S    | Fix day-count pluralisation        | Routine cards and the training-day wizard now share `formatDaysPerWeek`, so singular counts render as `1 day/week` while zero and multiple counts remain plural.                                                | FE           |

### Integrity gaps owned by a feature

| ID     | Status    | Size | Work item                          | Expected behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Repositories         |
| ------ | --------- | ---- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| FIX-03 | `SHIPPED` | M    | Correct kg/lb handling             | Canonical storage remains kilograms (`SetLog.weight`, `PersonalRecord.weight`, `WorkoutAnalyticsProjection.totalVolumeKg`); the frontend converts only at display and entry boundaries. **Shipped 2026-09-07 in the required order:** the shared conversion boundary landed before Settings exposed the already-persisted `weightUnit`. Active logging, previous performance, routine detail/editor, history, dashboard, profile and body-weight settings now display the preference, while editable values convert back to kilograms before validation or persistence. A bounded tolerance absorbs only two-decimal pound round-trip noise and prevents a unit transition from autosaving unchanged sets. No backend, contract publish or migration was needed. | FE                   |
| FIX-04 | `SHIPPED` | S    | Hide the Quick Workout entry point | Decided 2026-09-07: hide the button rather than seed it with a starting exercise. It currently creates a routine with an empty `exercises` array, so the session it opens has nothing loggable and no way to add anything. Hiding is reversible and costs nothing; the replacement flow is `LIVE-06`, whose design is still open.                                                                                                                                                                                                                                                                                                                                                                                                                                | FE                   |
| FIX-05 | `SHIPPED` | -    | Make Share Profile functional      | Shipped 2026-09-10 with `PROF-12`: Share Profile copies a real `/members/<username>` URL, confirms success, reports clipboard failure, and sends signed-out recipients to the privacy-filtered public profile instead of Login.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | FE, BE via `PROF-12` |
| FIX-11 | `SHIPPED` | S    | Make Forgot password functional    | Shipped 2026-09-13: Login's "Forgot password?" now opens a real reset. The request page emails a Supabase recovery link without revealing whether an account exists; the link returns through the existing `/auth/callback` to a page that sets the new password, and an expired or used link comes back to the request form with an explanation. The missing route had logged a 404 on every production Login visit.                                                                                                                                                                                                                                                                                                                                            | FE                   |

## Active queue

This records dependency order, not an estimate or a detailed implementation plan.

| Order | Queue group              | Size | Included feature IDs                                                                    | Why it precedes later work                                                                                                                                                            |
| ----- | ------------------------ | ---- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | Analytics foundation     | L    | Complete — DATA-01 through DATA-05 shipped                                              | The projection, snapshots, rollups, addressable event families and rebuild path are all in production. Groups 4, 6, 7, and 8 read what they produce.                                  |
| 2     | Product integrity        | S    | Complete — FIX-01 through FIX-04 and FIX-06 through FIX-10 shipped                      | Existing screens and flows must be truthful and usable first.                                                                                                                         |
| 3     | Daily session essentials | M    | LIVE-01 through LIVE-06, LIVE-19 (LIVE-01 through LIVE-05 and LIVE-19 shipped)          | `LIVE-05` is shipped; LIVE-06 remains product-blocked.                                                                                                                                |
| 4     | Visible workout payoff   | M    | Complete — LIVE-07 through LIVE-10 shipped                                              | Session completion now exposes live records, progression explanations, recovery and a reconstructible recap.                                                                          |
| 5     | Identity and privacy     | XL   | PROF-03 through PROF-10, PROF-12 (PROF-03 through PROF-06, PROF-09 and PROF-12 shipped) | The dependency-ready identity, privacy, discovery and public-sharing foundation is complete; PROF-07, PROF-08 and PROF-10 await achievements, routine sharing and a moderation model. |
| 6     | Progress destination     | L    | Complete — PROG-01 through PROG-08 shipped                                              | Progress combines record frontiers, session history, muscle distribution, bounded workload, same-day session comparisons, a global event timeline and private measurable goals.       |
| 7     | Achievements and ranks   | L    | ACH-01, ACH-02, ACH-04 and ACH-05 shipped; ACH-03 waits on PROF-07                      | Verified milestones, attendance ranks, safe next-milestone progress and comeback recognition are live; the remaining showcase needs the featured-accomplishment foundation.           |
| 8     | Social core              | XL   | SOC-02 through SOC-08 (SOC-02 and SOC-07 shipped)                                       | Builds safe discovery and activity before messaging.                                                                                                                                  |
| 9     | Schedule and push        | XL   | SCHED-01–07, NOTIF-01–05, NOTIF-08 (SCHED-01–07 and NOTIF-01 shipped)                   | Improves retention and makes the installed PWA materially useful.                                                                                                                     |
| 10    | Routine ecosystem        | XL   | ROUT-03 through ROUT-11 (ROUT-08, ROUT-10 and ROUT-11 shipped)                          | Sharing depends on truthful history, identity, and privacy.                                                                                                                           |
| 11    | Complex infrastructure   | XL   | OFFLINE-01, MSG-01 through MSG-05                                                       | Offline conflict resolution, realtime delivery, moderation, and unread state are substantial systems.                                                                                 |

Order is dependency order, not priority: group 1 leads because it is already in
flight, not because it outranks the honesty fixes in group 2, which were a day of
frontend work and proceeded in parallel -- the six frontend-only ones and
`FIX-03` shipped on 2026-09-07. Group 3 sits ahead of group 4 because nothing in
it waits on a production rollout, while group 4 cannot reach users until
projection reads are enabled.

## Feature catalog

### Live workout experience

| ID      | Status      | Size | Feature                        | User-facing behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Dependencies                                                     |
| ------- | ----------- | ---- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| LIVE-01 | `SHIPPED`   | M    | Rest timer                     | Completing a set starts the exercise's stored rest duration with add 15 seconds, skip, sound/vibration, and a deadline that survives backgrounding.                                                                                                                                                                                                                                                                                                                                                                                                | Existing `restSeconds`; NOTIF-02 for locked-screen alerts        |
| LIVE-02 | `SHIPPED`   | S    | Screen wake lock               | Keep the display awake while an active session is in use, with graceful fallback where unsupported.                                                                                                                                                                                                                                                                                                                                                                                                                                                | None                                                             |
| LIVE-03 | `SHIPPED`   | M    | Previous performance inline    | Show the matching set from the most recent comparable workout beneath the current input and highlight improvement.                                                                                                                                                                                                                                                                                                                                                                                                                                 | Efficient previous-session query                                 |
| LIVE-04 | `SHIPPED`   | S    | RPE capture                    | Let users enter the RPE already represented by set logs and displayed in history.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Existing set-log field                                           |
| LIVE-05 | `SHIPPED`   | M    | Plate calculator               | The active session exposes one calculator per weighted exercise. It starts from the next incomplete set's target and the default training location, allows both to change, and returns the exact bar-plus-plates load or the closest inventory-bounded load without exceeding the target. The result is expressed per side in the account's kg/lb unit, handles a bare bar and warns when the bar itself exceeds the target. Shipped 2026-09-08 as a frontend-only consumer of PREF-01.                                                            | PREF-01                                                          |
| LIVE-06 | `BLOCKED`   | L    | Ad-hoc workout replacement     | Blocked on a product decision, not on code: what replaces Quick Workout has not been designed. The direction under consideration is letting the user pick a template (full body, upper/lower, filtered by session duration and experience level) instead of starting from an empty session, which overlaps `ROUT-03` and needs those templates authored. Resolve the design before scheduling.                                                                                                                                                     | FIX-04; design decision pending; likely `ROUT-03`                |
| LIVE-07 | `SHIPPED`   | M    | Live PR detection              | Completing a set now celebrates every weight, rep, volume and estimated-1RM frontier it crosses. The result is calculated against completed history plus other completed sets in the active session; identical autosaves do not repeat it, bodyweight work can earn only the truthful rep category, and weight-derived values display in the account's kg/lb preference. Shipped 2026-09-08 through `@sunsteel/contracts@0.9.0` and the BE/FE set-log mutation path.                                                                               | DATA-01; `@sunsteel/contracts@0.9.0`; BE/FE mutation path        |
| LIVE-08 | `SHIPPED`   | M    | Progression transparency       | Finishing a workout now pauses before Dashboard when prescriptions advanced and names each exercise, the all-sets or per-set rule, performed/target reps, and old → new load in the account's kg/lb unit. Only completed sets can satisfy a progression rule. The finish response is retry-stable because each change is stored as an idempotent `PROGRESSION_CHANGED` event. Shipped 2026-09-09 through `@sunsteel/contracts@0.11.0`, BE migration/event writer and FE result dialog.                                                             | DATA-02 progression slice; `@sunsteel/contracts@0.11.0`          |
| LIVE-09 | `SHIPPED`   | M    | Session recap                  | Completing a workout now pauses on a recap with duration, unit-aware volume, completed sets, the session's final record frontiers, persisted progression changes, session notes and duration/volume/set deltas against the latest completed session for the same preserved routine day. The same stable recap can be reopened from workout history; first sessions and absent notes use honest empty states. Shipped 2026-09-09 through `@sunsteel/contracts@0.12.0`, a reconstructible BE recap read/finish response and FE finish/history views. | DATA-01; DATA-02 progression slice; `@sunsteel/contracts@0.12.0` |
| LIVE-10 | `SHIPPED`   | M    | Abandoned-session recovery     | After 48 hours without activity, require the owner to Resume, finish using completed sets, or discard the session. Sessions are no longer auto-aborted in the background; Resume refreshes `lastActivityAt`, and the detailed set log is fetched only when the recovery prompt is needed. Shipped 2026-09-07 across FE/BE without a contract publish.                                                                                                                                                                                              | Existing `lastActivityAt`; FE/BE behavior change                 |
| LIVE-11 | `SHIPPED`   | L    | Exercise substitution          | Recommend alternatives by muscles and equipment and apply them to this session or optionally to the routine.                                                                                                                                                                                                                                                                                                                                                                                                                                       | EXER-05, DATA-04                                                 |
| LIVE-12 | `CANDIDATE` | L    | Set classifications            | Distinguish warm-up, working, drop, failure, and optional sets so only intended work affects progression and analytics.                                                                                                                                                                                                                                                                                                                                                                                                                            | BE/CT schema change                                              |
| LIVE-13 | `CANDIDATE` | M    | Warm-up ramp generator         | Generate non-counting ramp sets from the working weight, bar, and available plates.                                                                                                                                                                                                                                                                                                                                                                                                                                                                | LIVE-05, LIVE-12                                                 |
| LIVE-14 | `CANDIDATE` | L    | Supersets and circuits         | Group exercises, rotate through them by round, and apply rest at the correct point.                                                                                                                                                                                                                                                                                                                                                                                                                                                                | ROUT-12, LIVE-01                                                 |
| LIVE-15 | `CANDIDATE` | M    | Faster set editing             | Duplicate a set, copy previous values, insert another set, or reorder session-only work.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | LIVE-06                                                          |
| LIVE-16 | `CANDIDATE` | M    | Session notes                  | Capture visible workout-level context and exercise-specific observations without overwriting routine instructions.                                                                                                                                                                                                                                                                                                                                                                                                                                 | CT/BE session note contracts                                     |
| LIVE-17 | `CANDIDATE` | M    | Post-workout correction window | Correct mistaken weight, reps, RPE, or completion status while preserving an audit trail.                                                                                                                                                                                                                                                                                                                                                                                                                                                          | DATA-02                                                          |
| LIVE-18 | `CANDIDATE` | M    | Gym mode                       | Offer larger controls, stronger contrast, and fewer secondary actions during training.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A11Y-02                                                          |
| LIVE-19 | `SHIPPED`   | S    | Discard an in-progress session | The active-session action card now lets the owner discard at any time. A dedicated destructive confirmation clearly distinguishes discard from finish, warns that the session and saved sets will not enter history, disables both actions while the existing `ABORTED` mutation is pending, and reports success or failure before returning to the dashboard. Shipped 2026-09-08 without backend or contract changes because the endpoint, status and cache invalidation already existed.                                                         | FE                                                               |

### Analytics and historical-data foundation

| ID      | Status    | Size | Feature                      | User-facing or architectural outcome                                                                                      | Dependencies                 |
| ------- | --------- | ---- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| DATA-01 | `SHIPPED` | L    | Persistent personal records  | Store record events when they occur instead of deriving all records from the user's lifetime set-log table on every read. | BE/CT migration and backfill |
| DATA-02 | `SHIPPED` | L    | Training-event stream        | Persist session completions, PRs, streak milestones, progression changes, and achievements as addressable events.         | BE/CT schema                 |
| DATA-03 | `SHIPPED` | L    | Daily and weekly rollups     | Store volume, completed sets, active days, and muscle-group totals for fast progress views.                               | BE schema and backfill       |
| DATA-04 | `SHIPPED` | L    | Routine snapshot per session | Preserve the routine/day/exercise prescription used at training time so future edits cannot rewrite history.              | BE/CT schema                 |
| DATA-05 | `SHIPPED` | L    | Historical backfill          | Generate initial records, events, rollups, and snapshots from existing sessions with repeatable migration logic.          | DATA-01 through DATA-04      |

Together, `DATA-01` through `DATA-05` provide the durable data model and
backfill needed to close
[`TD-27`](technical-debt.md#td-27).
The technical-debt entry owns the query-cost evidence and closure criteria;
this roadmap owns the product and cross-repository delivery sequence.

**Delivery verified 2026-09-14.** DATA-01, DATA-03, DATA-04 and DATA-05 first
shipped on 2026-09-07. Production comparison passed for all seven accounts;
repeated backfill was idempotent, snapshot-safe relations were migrated and the
API legacy fallback was removed. See the
[TD-27 closure record](../history/td27-closure-2026-09-07.md). DATA-02 then
completed through `LIVE-08` and `ACH-01`: `TrainingEventType` now persists
`SESSION_COMPLETED`, `PERSONAL_RECORD`, `PROGRESSION_CHANGED`,
`STREAK_MILESTONE` and `ACHIEVEMENT_UNLOCKED`. New sessions award milestones
inside the idempotent projection writer, while the owner achievement read
reconciles pre-feature history into immutable, explicitly backfilled events.

### Progress

| ID      | Status      | Size | Feature                         | User-facing behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Dependencies                                   |
| ------- | ----------- | ---- | ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| PROG-01 | `SHIPPED`   | L    | Exercise strength trends        | Progress now selects among trained lifts and plots the best-set load and estimated-1RM record frontier over 30 days, 90 days, 6 months, 1 year or all time. A carried-in baseline keeps bounded ranges truthful; exact record rows retain date, load and reps. The read reconstructs persisted `PERSONAL_RECORD` events through a partial exercise/date index and caps responses at 500 changes instead of scanning lifetime set logs. Shipped 2026-09-10 through `@sunsteel/contracts@0.18.0`, a BE index migration and hand-rolled FE SVG charts.                                                                                                                                                                                                                           | DATA-01, DATA-03; `@sunsteel/contracts@0.18.0` |
| PROG-02 | `SHIPPED`   | L    | Exercise performance history    | Progress now lists every terminal session containing completed work for the selected exercise, paginated across the chosen range. Each card preserves its historical routine/day, actual sets and RPE, session note, prescription note and any automatic progression change; aborted sessions with real completed work remain visible, while incomplete sets do not become performance. Shipped 2026-09-11 through `@sunsteel/contracts@0.19.0`, indexed BE reads and the existing FE `/progress` destination.                                                                                                                                                                                                                                                                | DATA-04; `@sunsteel/contracts@0.19.0`          |
| PROG-03 | `SHIPPED`   | M    | Muscle-group heatmap            | Progress maps every canonical muscle across 4, 8 or 12 Monday-based weeks using completed-set equivalents: primary muscles count 1.0 and secondary muscles 0.5. Zero-work cells stay visible, the current partial week is explicit, and the top three muscles summarize the selected range. Shipped 2026-09-11 through `@sunsteel/contracts@0.20.0` and the existing `DATA-03` projection, with no new migration or historical rebuild.                                                                                                                                                                                                                                                                                                                                       | DATA-03; `@sunsteel/contracts@0.20.0`          |
| PROG-04 | `SHIPPED`   | M    | Volume trends                   | Progress compares external-load volume and completed work across the total, muscles, historical routines and exercises for 4, 8 or 12 Monday-based weeks. The current partial week is explicit and excluded from the latest-versus-previous completed-week delta. Overall and muscle data use the active timezone-matched `DATA-03` projection; routine and exercise dimensions use only the same bounded completed-session window. Bodyweight work contributes sets but no invented load, and muscle sets retain primary 1.0 / secondary 0.5 weighting. Shipped 2026-09-12 through `@sunsteel/contracts@0.21.0` without a migration or rebuild.                                                                                                                              | DATA-03; `@sunsteel/contracts@0.21.0`          |
| PROG-05 | `SHIPPED`   | M    | Consistency calendar            | Progress shows a monthly consistency calendar built from the Schedule's week rules. Each day carries one mark, the strongest of its entries: completed, in progress, ended early, not logged (never "missed"), planned or rest day. The month states its totals and on how many of its days so far a workout was completed. Recovery days are the planned rest days of `SCHED-07`; no day is judged against an invented target. Previous and next months page through history, and the section links to the Schedule. Shipped 2026-09-15 in the frontend over existing reads.                                                                                                                                                                                                 | DATA-02, SCHED-01                              |
| PROG-06 | `SHIPPED`   | M    | Session comparison              | Progress selects any preserved routine day with completed history and compares its latest two completions: duration, unit-aware load volume, completed sets, session notes and exercise-by-exercise actual sets. Exercises and sets match through stable snapshot identities rather than mutable names; skipped work plus exercises added or removed between executions stay explicit, while one-session days and absent notes use honest empty states. The read caps routine-day discovery at 100 and loads only the two selected sessions. Shipped 2026-09-13 through `@sunsteel/contracts@0.22.0` without a migration or backfill.                                                                                                                                         | DATA-04; `@sunsteel/contracts@0.22.0`          |
| PROG-07 | `SHIPPED`   | M    | Record and progression timeline | Progress now interleaves persisted personal-record and automatic-progression events in a stable cursor-paginated feed, filterable by event type. Each row preserves its historical routine/day and session recap, explains whether a best set was the first recorded frontier, a heavier load or more reps at the same load, and expands exact per-set prescription changes with the stored rule evidence. The owner-scoped read defaults to 20 events, caps pages at 50 and never scans lifetime set logs. Shipped 2026-09-13 through `@sunsteel/contracts@0.23.0` without a migration or backfill. The four transient LIVE-07 celebration categories are not reconstructed as separate historical events because only the best-set `PERSONAL_RECORD` frontier is persisted. | DATA-01, DATA-02; `@sunsteel/contracts@0.23.0` |
| PROG-08 | `SHIPPED`   | L    | Personal goals                  | Settings stores up to eight private targets for weekly sessions, weekly external-load volume, active streak, per-exercise estimated 1RM and current body weight. Progress evaluates them from bounded current-week rollups, the active streak projection, persisted records and the owner's body weight, showing exact current, target and remaining gap in the account's unit. At-most body-weight goals intentionally omit a percentage when a bounded bar would misrepresent direction. Shipped 2026-09-13 through `@sunsteel/contracts@0.27.0`, a required BE migration and FE Settings/Progress views.                                                                                                                                                                   | PREF-02, DATA-03; `@sunsteel/contracts@0.27.0` |
| PROG-09 | `SHIPPED`   | M    | Plateau detection               | Progress lists recently trained lifts that have gone at least four sessions without a new best, with a best at least three weeks old, from the persisted best-set frontier and an 8-week window of completed loaded sets. Each row states the best set, its estimated 1RM and the closest set since; a lift whose estimated 1RM rose is never flagged, the thresholds are printed above the list, and the exercise page repeats the note. Numbers only, never a cause. Shipped 2026-09-14 through `@sunsteel/contracts@0.34.0` without a migration.                                                                                                                                                                                                                           | PROG-01, LIVE-04; `@sunsteel/contracts@0.34.0` |
| PROG-10 | `CANDIDATE` | M    | Fatigue indicators              | Combine RPE, missed targets, recent decline, and frequency to flag possible fatigue without diagnosing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | LIVE-04, DATA-03                               |
| PROG-11 | `CANDIDATE` | L    | Training-block comparison       | Compare performance and adherence between program phases.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ROUT-09, DATA-03                               |
| PROG-12 | `CANDIDATE` | L    | Body measurements               | Track weight and optional measurements privately with trends and goal context.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | PROF-06, FIX-03                                |
| PROG-13 | `CANDIDATE` | L    | Progress photos                 | Store private dated photos and support side-by-side comparison with explicit sharing controls.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | PROF-06, storage policy                        |
| PROG-14 | `CANDIDATE` | M    | Exportable progress report      | Produce a selected, shareable summary for personal review or a coach.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | EXPORT-01                                      |

Use lightweight CSS or hand-rolled SVG for initial progress visuals. Do not
restore a large charting dependency without measuring the installed PWA's
cold-start impact and showing that simpler rendering is insufficient.

### Exercise library

| ID      | Status      | Size | Feature                        | User-facing behavior                                                                                                                                                                   | Dependencies                   |
| ------- | ----------- | ---- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| EXER-01 | `SHIPPED`   | M    | Personal exercise page         | Show routines using the exercise, recent performances, best set, estimated 1RM, and progression history.                                                                               | PROG-01, PROG-02               |
| EXER-02 | `SHIPPED`   | M    | Catalog browsing               | Filter the catalog by name, muscle, equipment (one item or everything listed at the default gym), movement pattern and training history. Stars arrived with `EXER-07`.                 | EXER-09                        |
| EXER-03 | `CANDIDATE` | XL   | Instructions and cues          | Provide setup, execution, common mistakes, and appropriate safety notes.                                                                                                               | Content-authoring process      |
| EXER-04 | `CANDIDATE` | XL   | Muscle visualization and media | Display targeted muscles and concise demonstrations where reliable assets exist.                                                                                                       | Curated assets and licenses    |
| EXER-05 | `SHIPPED`   | M    | Alternatives                   | Suggest substitutions based on movement, primary muscles, and available equipment.                                                                                                     | EXER-09                        |
| EXER-06 | `CANDIDATE` | L    | Custom exercises               | Let users create private catalog entries with muscles, equipment, and notes.                                                                                                           | BE/CT ownership model          |
| EXER-07 | `SHIPPED`   | L    | Favorites and recents          | Star exercises privately and see starred, then recently trained, exercises first in the routine wizard's pickers; the catalog filters by stars. Quick Workout stays hidden (`FIX-04`). | Starred-exercise model         |
| EXER-08 | `CANDIDATE` | L    | Catalog moderation             | Review duplicates, naming, instructions, and promoted user submissions.                                                                                                                | Administrative tooling         |
| EXER-09 | `SHIPPED`   | L    | Exercise metadata expansion    | Extend the catalog with movement pattern, equipment detail, substitution grouping and instruction/media fields so dependent features stop blocking on undefined data.                  | BE/CT schema; catalog backfill |

The first Exercises release should emphasize the user's existing training data.
Instructions and media make this a content project as well as an engineering one.

`EXER-09` shipped the data those items were waiting on: every catalog exercise
now carries a movement pattern, a compound/isolation mechanic, the equipment it
needs (in the same lowercase vocabulary as `PREF-01` location equipment) and a
substitution group of near-identical exercises that never crosses movement
patterns. `EXER-02`, `EXER-05` and `ROUT-10` are no longer blocked on catalog
data. `instructions` and `mediaUrl` exist but stay empty until `EXER-03` and
`EXER-04` supply real content; nothing should render them before then.

`EXER-02` shipped the first Exercises page on 2026-09-14 as catalog browsing
only: filters for name, muscle, equipment, movement pattern and "Trained by me",
with each exercise's last training date. It adds no backend read, because the
existing performance history already lists every exercise with completed work.
The favorites filter in the original description was deliberately left out:
it needs the user-exercise preference model owned by `EXER-07`, which does not
exist. The five favorite exercises on the `PROF-05` training identity are
public-facing profile metadata, not a browsing preference, and were not reused
as a substitute.

`EXER-01` shipped the personal exercise page the same day. It composes the
existing reads: catalog facts, the owner's routine days that program the
exercise, the record frontier for the best set and estimated 1RM, paginated
performance history, and the progress timeline narrowed to the exercise. The
narrowing was the one missing read, so `GET /workouts/progress/timeline` gained
an optional `exerciseId` (`@sunsteel/contracts@0.31.0`) instead of the page
reconstructing progression from whichever performance pages happened to load.

`EXER-07` then shipped favorites as private **stars**. The name is deliberate:
the `PROF-05` training identity already has public "favorite exercises", and
two different favorites would blur a private working preference into profile
metadata. Stars need their own `StarredExercise` model and migration, so the
item grew from `M` to `L`. Recents need no storage: they come from the
trained-exercise list the catalog already reads. Quick Workout, the second
surface in the original description, stays hidden under `FIX-04` until
`LIVE-06` designs it.

### Routines and programming

| ID      | Status      | Size | Feature                        | User-facing behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Dependencies                         |
| ------- | ----------- | ---- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| ROUT-03 | `QUEUED`    | M    | Starter templates              | Open curated programs in the existing wizard as fully editable drafts. Candidate to also serve as the Quick Workout replacement path (`LIVE-06`), which would extend selection with session duration and experience level.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Curated templates (no owning ID yet) |
| ROUT-04 | `QUEUED`    | L    | Routine sharing                | Publish, share with followers, or expose through a private link under explicit visibility controls.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | PROF-06                              |
| ROUT-05 | `QUEUED`    | M    | Routine cloning                | Copy a shared routine into an independent editable version.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | ROUT-04                              |
| ROUT-06 | `QUEUED`    | M    | Routine lineage                | Preserve original authorship and identify the source of a clone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ROUT-05, PROF-03                     |
| ROUT-07 | `QUEUED`    | L    | Routine discovery              | Browse programs by goal, days, experience, equipment, duration, and muscle focus.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | ROUT-04, EXER-02                     |
| ROUT-08 | `SHIPPED`   | L    | Routine versions               | The routine detail page saves the routine as it is now under an optional name, compares any saved version with the current routine day by day (days by weekday or position, exercises by catalog exercise) and restores it after saying what would change. A version is an immutable copy of the whole setup, loads included; restoring first saves the setup it replaces, so it can be undone, keeps the routine and its history, and is refused while a session of the routine is in progress. A routine keeps up to 20 versions. Shipped 2026-09-15 through `@sunsteel/contracts@0.38.0` and migration `20260915150000_routine_versions`.                                                                                           | DATA-04                              |
| ROUT-09 | `QUEUED`    | XL   | Training blocks and deloads    | Organize dated program phases and apply temporary deload prescriptions without destroying the normal routine.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | ROUT-08, SCHED-01                    |
| ROUT-10 | `SHIPPED`   | M    | Quality summary                | Before saving, show weekly muscle-group sets, equipment, estimated duration, and likely imbalances.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | EXER-09                              |
| ROUT-11 | `SHIPPED`   | L    | Flexible day identity          | A routine is weekly (each day on its weekday, as before) or a rotation of up to seven days that runs in order on any weekday, and every day may carry its own name. The builder offers rotation presets, reordering and a name per day, and switching modes keeps every day and its exercises. A rotation offers the day after the last completed session (an aborted one does not advance it) on the routine card, the detail page and the dashboard, and any of its days can still be started. Days read by their name, weekday or rotation letter everywhere, history included, while older snapshots keep their weekday. Shipped 2026-09-14 through `@sunsteel/contracts@0.36.0` and migration `20260914230000_rotation_routines`. | BE/CT routine model                  |
| ROUT-12 | `CANDIDATE` | M    | Superset/circuit configuration | Group routine exercises and define order within a round.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | BE/CT schema; LIVE-14                |
| ROUT-13 | `CANDIDATE` | M    | Routine import/export          | Back up and exchange programs through a stable versioned format.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | EXPORT-01                            |
| ROUT-14 | `DEFERRED`  | XL   | Collaborative editing          | Allow an invited coach or collaborator to suggest or make changes with an audit trail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | COACH-01, DATA-02                    |

### Schedule

| ID       | Status      | Size | Feature                    | User-facing behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Dependencies            |
| -------- | ----------- | ---- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| SCHED-01 | `SHIPPED`   | L    | Weekly calendar            | The Schedule page shows one Monday-based week at a time: weekly routine days as planned from today on or "not logged" before it (never "missed", and only from each routine's creation date), completed, ended-early and live sessions on the day they started, and each rotation's next day as an undated note. Rest days are not inferred; intentional rest belongs to `SCHED-07`. The page states that planned days follow the routines as they are now. Shipped 2026-09-14 in the frontend over existing reads.                                 | ROUT-11                 |
| SCHED-02 | `SHIPPED`   | M    | Monthly calendar           | The Schedule page switches between the week and a Monday-based month of the same rules, one mark per day, with the month's totals and how many of its days so far had a completed workout. Months page back and forward, and choosing a day opens its week. Shipped 2026-09-15 in the frontend with `PROG-05`, over existing reads.                                                                                                                                                                                                                 | SCHED-01, DATA-02       |
| SCHED-03 | `SHIPPED`   | S    | Start from calendar        | The Schedule page starts today's planned weekly days and each rotation's next day in the current week, and resumes the workout in progress. Past and future days never start, matching the routine card and dashboard, and nothing starts while a session is live, since that would resume the live one instead. Shipped 2026-09-14 in the frontend.                                                                                                                                                                                                | SCHED-01                |
| SCHED-04 | `SHIPPED`   | L    | Reschedule one occurrence  | A planned weekly workout moves to another day up to six days away, never a past day nor one the routine already trains on, and the move can be changed or undone; the routine itself does not change. The planned day reads "Moved to <day>" and the new day carries the workout, with Start when it is today; a session on either date completes it. Rotations re-flow on their own. Shipped 2026-09-15 through `@sunsteel/contracts@0.40.0` and migration `20260915190000_schedule_overrides`.                                                    | Schedule-instance model |
| SCHED-05 | `SHIPPED`   | M    | Skip and postpone          | A planned weekly workout can be skipped ahead of time or, for a day that passed, up to six days back; it reads "Skipped", never "not logged", can be undone, and a session that day still counts. Postpone moves it in one tap to the first free day after where it sits. A skipped day is free for another workout of the routine. Rotations re-flow on their own. Shipped 2026-09-15 through `@sunsteel/contracts@0.41.0` and migration `20260915210000_schedule_skip`.                                                                           | SCHED-04                |
| SCHED-06 | `SHIPPED`   | L    | Rotation training weekdays | Weekday schedules and rotations came with `ROUT-11`. A rotation can now pick training weekdays in the builder, and the Schedule places its days on them from today: the next day on the first one without a session of the routine, the rest in order. A past one without a session reads "not logged", with no day name; days still start on any weekday. Shipped 2026-09-15 through `@sunsteel/contracts@0.39.0` and migration `20260915170000_rotation_weekdays`.                                                                                | ROUT-11                 |
| SCHED-07 | `SHIPPED`   | L    | Rest-day planning          | A weekly routine can mark rest days among the weekdays it does not train on, in the builder's schedule step. The Schedule page shows them as rest days before and after today, never as "not logged", and a workout of that routine on the day replaces the rest; the routine card and review list them. Rotations have no rest days: they advance only on completed workouts, so rest in a rotation is simply the days you do not train. Shipped 2026-09-15 through `@sunsteel/contracts@0.37.0` and migration `20260915090000_routine_rest_days`. | SCHED-01                |
| SCHED-08 | `CANDIDATE` | M    | Travel or alternate gym    | Temporarily change available equipment and substitutions for a date range.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | PREF-01, EXER-05        |
| SCHED-09 | `CANDIDATE` | M    | External calendar export   | Optionally expose scheduled workouts to common calendar applications.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Stable schedule model   |

### Profiles, identity, and privacy

| ID      | Status      | Size | Feature                           | User-facing behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Dependencies                                            |
| ------- | ----------- | ---- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- |
| PROF-03 | `SHIPPED`   | L    | Unique username                   | Every account has a normalized unique handle with shared format and reserved-name rules. Settings edits it, search accepts names or `@username` without exposing email, and authenticated profile URLs use the handle while old UUID links remain valid.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Shipped 2026-09-09                                      |
| PROF-04 | `SHIPPED`   | L    | Real biography and location       | Settings edits an optional biography (500 characters) and location (100 characters). Blank values normalize to empty state, each field has its own private-by-default visibility, and profiles distinguish real, empty and private content without querying denied fields. Shipped 2026-09-09 through `@sunsteel/contracts@0.15.0`, a required BE migration and FE settings/profile views.                                                                                                                                                                                                                                                                                                                                                             | PROF-06; `@sunsteel/contracts@0.15.0`                   |
| PROF-05 | `SHIPPED`   | L    | Training identity                 | Settings manages optional broad goals (up to 3), experience, disciplines (up to 3), preferred routine style and catalog-backed favorite exercises (up to 5). Profiles render honest empty/private states or the structured identity allowed by one private-by-default visibility rule. These labels describe training identity; measurable targets remain with `PROG-08`. Shipped 2026-09-10 through `@sunsteel/contracts@0.16.0`, a required BE migration and FE settings/profile views.                                                                                                                                                                                                                                                              | PROF-06; `@sunsteel/contracts@0.16.0`                   |
| PROF-06 | `SHIPPED`   | XL   | Privacy model                     | Settings controls `public`, `followers`, or `private` visibility independently for biography, location, training identity, workout history, records, routines, achievements, and body metrics. All eight default to private; email is never part of the member-profile response. The authenticated profile read enforces owner/follower/public access before querying sensitive profile, training or body data, and currently renders allowed About content, training identity, training summaries, records and body metrics. Routine and achievement rules are persisted for their future profile surfaces. The five-domain foundation shipped 2026-09-09 through `@sunsteel/contracts@0.14.0`; `PROF-04` and `PROF-05` extended it through `0.16.0`. | BE/CT authorization rules; `@sunsteel/contracts@0.16.0` |
| PROF-07 | `QUEUED`    | L    | Featured records and achievements | Add ordered, typed featured-accomplishment slots and a privacy-filtered public-profile payload, backed by a new per-user selection model. Owners first select record frontiers; the storage/API accepts earned achievement references so ACH-03 can add the medal/title picker without another migration. Whole records and achievements sections keep their existing independent privacy rules, and a stale or no-longer-earned reference is omitted rather than serialized. Evaluation: [PROF-07 scope](../history/prof07-evaluation-2026-09-15.md).                                                                                                                                                                                                 | DATA-01, ACH-01, PROF-06                                |
| PROF-08 | `QUEUED`    | M    | Routine showcase                  | Display selected public routines on a profile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ROUT-04, PROF-06                                        |
| PROF-09 | `SHIPPED`   | L    | Account discovery controls        | Settings independently controls discovery by first/last name, public `@username`, and future contact matching. Name and username remain enabled by default to preserve existing search; contact matching is explicit opt-in and is not active today. Search enforces each current control without affecting direct profile URLs or section privacy. Shipped 2026-09-10 through `@sunsteel/contracts@0.17.0`, a required BE migration and FE settings card.                                                                                                                                                                                                                                                                                             | PROF-03, PROF-06; `@sunsteel/contracts@0.17.0`          |
| PROF-10 | `QUEUED`    | L    | Block and report                  | Give users immediate control over unwanted discovery and interaction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | BE/CT moderation model                                  |
| PROF-11 | `CANDIDATE` | M    | Branded profile card              | Generate a shareable image containing selected public identity, rank, and accomplishments.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | PROF-03, PROF-07, PROF-12                               |
| PROF-12 | `SHIPPED`   | L    | Public profile route and sharing  | `/members/<username>` sits outside the protected shell and reads an unguarded, no-store backend endpoint that evaluates every section as a signed-out visitor: only `PUBLIC` data is queried and returned, while `FOLLOWERS` and `PRIVATE` remain hidden. Authenticated `/profile/<identifier>` retains owner/follower access, UUID compatibility and social controls. Share Profile copies the public URL with explicit success or failure feedback. Shipped 2026-09-10 without a contract publish or database migration.                                                                                                                                                                                                                             | PROF-03, PROF-06                                        |

### Social and community

| ID     | Status      | Size | Feature                    | User-facing behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Dependencies                                   |
| ------ | ----------- | ---- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| SOC-02 | `SHIPPED`   | M    | Relationship lists         | Signed-in members open any profile's followers, following and viewer-relative mutuals (people you follow who also follow them; mutual follows on your own profile) from the profile counts, paginated newest first with Follow/Unfollow on every row. Search suggests up to ten members: people who already follow you first, then accounts followed by people you follow that allow name or username discovery. Entries carry identity only, never email or privacy-governed sections, and the signed-out `/members` route keeps plain counts. Shipped 2026-09-13 through `@sunsteel/contracts@0.24.0` without a migration.                                                                                                         | FIX-06; `@sunsteel/contracts@0.24.0`           |
| SOC-03 | `QUEUED`    | L    | Generated activity feed    | Automatically create optional entries for completed sessions, PRs, achievements, routines, and streak milestones.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | DATA-02, PROF-06                               |
| SOC-04 | `QUEUED`    | M    | Selective activity sharing | Configure sharing defaults by event type and override visibility for one entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | SOC-03, PROF-06                                |
| SOC-05 | `QUEUED`    | M    | Themed reactions           | Encourage activity with a small Sunnsteel-specific reaction set rather than a generic like counter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | SOC-03, PROF-10                                |
| SOC-06 | `QUEUED`    | L    | Comments                   | Discuss shared activity with deletion, reporting, and visibility enforcement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | SOC-03, PROF-10, moderation tools              |
| SOC-07 | `SHIPPED`   | L    | Structured workout sharing | From a completed session's recap in history, the owner creates a public link and chooses which parts it reveals: duration, volume, completed sets and personal records by default; progression changes and session notes opt-in. Anyone with the link sees only those parts, in the owner's kg/lb unit, with the owner's name, handle and avatar and a link to the privacy-filtered public profile; the previous-session comparison is never shared. Links are unguessable, capped at ten active per session and revocable at any time, after which they stop working immediately. Shipped 2026-09-13 through `@sunsteel/contracts@0.25.0` and a `SessionShare` migration; the size is corrected from `M` to `L` for that migration. | LIVE-09, PROF-06; `@sunsteel/contracts@0.25.0` |
| SOC-08 | `QUEUED`    | L    | Training partners          | Establish a mutual relationship with explicit shared schedule and progress permissions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | PROF-06, SOC-02                                |
| SOC-09 | `CANDIDATE` | M    | Partner encouragement      | Send lightweight prompts such as Ready to train or Strong session without opening full messaging.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | SOC-08, NOTIF-01                               |
| SOC-10 | `CANDIDATE` | L    | Private challenges         | Invite friends to frequency, consistency, or relative-improvement goals.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | DATA-02, PROF-06                               |
| SOC-11 | `CANDIDATE` | L    | Public challenges          | Join inclusive community campaigns scored by consistency or relative progress.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | SOC-10, moderation                             |
| SOC-12 | `DEFERRED`  | XL   | Training groups            | Create clubs with a shared feed, routines, challenges, roles, and moderation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | SOC-03, SOC-10, PROF-10                        |

### Achievements and motivation

| ID     | Status      | Size | Feature                | User-facing behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Dependencies      |
| ------ | ----------- | ---- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| ACH-01 | `SHIPPED`   | L    | Milestone achievements | Achievements now lists 25 fixed, verified milestones across sessions, completed sets, external-load volume, current record frontiers and best streaks. Existing history is recognized honestly without inventing an earned date; future unlocks retain their session and time. Shipped 2026-09-14 through `@sunsteel/contracts@0.29.0`, idempotent BE event writers and the protected FE ledger.                                                                                                               | DATA-01, DATA-02  |
| ACH-02 | `SHIPPED`   | M    | Renaissance ranks      | Six stable ranks now progress from Initiate through Apprentice, Artisan, Maestro and Virtuoso to Laureate. Rank is derived from completed sessions plus distinct active training weeks, so it rewards participation and sustained attendance without using weight or encouraging consecutive-day training. Achievements shows the current rank, attendance evidence and exact remaining requirements for the next rank. Shipped 2026-09-14 through `@sunsteel/contracts@0.30.0` with no migration or backfill. | ACH-01            |
| ACH-03 | `QUEUED`    | M    | Achievement showcase   | Let owners choose earned medals and one rank title for the ordered public-profile slots delivered by PROF-07; validate against the live catalog/events, preserve achievement privacy, and show only still-earned selections.                                                                                                                                                                                                                                                                                   | PROF-06, PROF-07  |
| ACH-04 | `SHIPPED`   | M    | Achievement progress   | Achievements now shows exactly one next fixed milestone for each category, in stable catalog order, with exact current and target values rather than percentages, deadlines or proximity ranking. Completed categories say so explicitly. Volume and streak copy keeps planned loads and recovery authoritative. Shipped 2026-09-14 through `@sunsteel/contracts@0.32.0`, the existing protected achievement read and the responsive FE ledger; no migration or one-off backfill.                              | ACH-01            |
| ACH-05 | `SHIPPED`   | M    | Comeback recognition   | Achievements derives comeback evidence from the 500 most recent immutable completed-session events. After 14 full inactive local-calendar days, three distinct active dates inside an inclusive 14-day return window record a comeback; same-day sessions count once. The page explains the rule, links each recognition to its third return session, and discloses truncated history. Shipped 2026-09-15 through `@sunsteel/contracts@0.42.0`; no migration or backfill.                                      | DATA-02           |
| ACH-06 | `CANDIDATE` | M    | Personal quests        | Offer short private targets based on the user's real schedule and current ability.                                                                                                                                                                                                                                                                                                                                                                                                                             | PROG-08, SCHED-01 |
| ACH-07 | `CANDIDATE` | M    | Seasonal campaigns     | Run optional themed challenges without resetting permanent progress.                                                                                                                                                                                                                                                                                                                                                                                                                                           | SOC-11            |

### Dashboard and post-login experience

| ID         | Status      | Size | Feature                   | User-facing behavior                                                                                                                                                                                                                                                                                                                  | Dependencies                  |
| ---------- | ----------- | ---- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- |
| DASH-02    | `SHIPPED`   | S    | Adaptive primary action   | Today's card fills exactly one action chosen by state: Resume an open session (even one not scheduled today), Start the first workout that can start today, Review today's finished session, or Browse routines. Other scheduled rows keep outline controls. Shipped 2026-09-13, frontend only.                                       | Existing active/today queries |
| DASH-03    | `CANDIDATE` | M    | Weekly training strip     | Summarize the current week and open the relevant workout or schedule entry.                                                                                                                                                                                                                                                           | SCHED-01                      |
| DASH-04    | `CANDIDATE` | M    | Goal-based dashboard      | Emphasize statistics related to the user's selected objective.                                                                                                                                                                                                                                                                        | PROG-08                       |
| DASH-05    | `CANDIDATE` | M    | Configurable cards        | Let users reorder or hide schedule, records, streak, activity, and progress modules.                                                                                                                                                                                                                                                  | PREF-03                       |
| DASH-06    | `CANDIDATE` | M    | Stronger visual hierarchy | Replace repetitive cards with compact progress visuals, milestone callouts, and clearer mobile prioritization.                                                                                                                                                                                                                        | Progress data                 |
| DASH-07    | `CANDIDATE` | M    | Personalized insights     | Surface concise facts such as recent consistency changes or a lift approaching a record.                                                                                                                                                                                                                                              | DATA-01, DATA-03              |
| DASH-08    | `CANDIDATE` | M    | Social preview            | Show a small set of relevant followed-user updates without displacing training actions.                                                                                                                                                                                                                                               | SOC-03                        |
| DASH-09    | `CANDIDATE` | M    | Upcoming milestones       | Preview the next rank, record, streak, or session milestone.                                                                                                                                                                                                                                                                          | ACH-01                        |
| DASH-10    | `SHIPPED`   | S    | Contextual empty states   | Recent Activity, Personal Records and Workout History name the next step when empty: browse or create a routine, complete a set, or clear the filters hiding sessions. Clear filters now returns history to All instead of Completed. Progress empty states stay with their owning `PROG-*` items. Shipped 2026-09-13, frontend only. | None                          |
| ONBOARD-01 | `CANDIDATE` | L    | First-login onboarding    | Gather goals, experience, training days, equipment, and units, then recommend a template or first action.                                                                                                                                                                                                                             | ROUT-03, PREF-01, PROG-08     |

### Search and navigation

| ID     | Status      | Size | Feature                   | User-facing behavior                                                                                                                            | Dependencies              |
| ------ | ----------- | ---- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| NAV-01 | `CANDIDATE` | L    | Unified search            | Search users, exercises, routines, and workout history from the header.                                                                         | EXER-02, ROUT-07, PROF-03 |
| NAV-02 | `CANDIDATE` | M    | Grouped suggestions       | Show categorized instant results before opening the full results page.                                                                          | NAV-01                    |
| NAV-03 | `CANDIDATE` | S    | Recent searches           | Reopen recently viewed users, exercises, and routines.                                                                                          | NAV-01                    |
| NAV-04 | `CANDIDATE` | M    | Mobile quick actions      | Start or resume a workout, create a routine, or open the timer from a compact menu.                                                             | LIVE-01                   |
| NAV-05 | `CANDIDATE` | S    | Navigation badges         | Display restrained unread or upcoming indicators for notifications and schedule.                                                                | NOTIF-01, SCHED-01        |
| NAV-06 | `SHIPPED`   | S    | Persistent history access | Keep workout history directly reachable even when Workouts redirects to an active session. Shipped as the dedicated `History` navigation entry. | None                      |

### Notifications and retention

| ID       | Status        | Size | Feature                    | User-facing behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Dependencies                           |
| -------- | ------------- | ---- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| NOTIF-01 | `SHIPPED`     | L    | In-app notification center | A bell in the top bar leads to a Notifications page; unread notifications change its glyph and accessible name. Today lists the live session to resume or today's planned workouts to start, without read state. Updates lists, newest first, achievements earned live (never ones recognized from history), one note per finished session that set records or changed loads, and new followers from the last 30 days, gathered on read from records that already exist. Opening one marks it read, and all can be marked read at once. Shipped 2026-09-15 through `@sunsteel/contracts@0.43.0` and migration `20260915230000_notifications`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | DATA-02, PROF-06                       |
| NOTIF-02 | `QUEUED`      | M    | Web Push foundation        | Request permission, register the subscription and render authorized notifications on supported installed PWAs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | NOTIF-08; service-worker push handlers |
| NOTIF-03 | `QUEUED`      | S    | Rest-timer alert           | Notify when rest ends while the app is backgrounded or the screen is locked. **The alert is a notification, not the in-app tone.** `LIVE-01` synthesises its tone with WebAudio inside the page, and a backgrounded or locked PWA has its timers throttled or suspended and its audio context suspended, so that tone cannot fire -- this is the execution model, not a defect to repair in `LIVE-01`. The OS must play the sound, which means a notification, which on iOS means Web Push. **Scope limit recorded 2026-09-07: a live countdown on the lock screen is not achievable and must not be promised.** iOS Live Activities are native-only with no web equivalent, and the Web Notifications API has no chronometer or countdown field, so the most either platform can show is a static line such as `Rest ends at 14:32`, never a number that ticks down. Two further conditions: on iOS, Web Push requires the PWA installed to the Home Screen (16.4+) and does nothing in a browser tab; and a rest alert means scheduling one push per completed set at second-level precision, which is heavier than a training reminder and is what the `S` size assumes `NOTIF-08` already provides. The Chromium Notification Triggers experiment would allow a local scheduled notification with no server, but it is unverified, Chromium-only at best and absent on iOS; confirm its status before any plan depends on it. | LIVE-01, NOTIF-02                      |
| NOTIF-04 | `QUEUED`      | M    | Training reminders         | Remind users before scheduled sessions using their time zone and quiet hours.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | SCHED-01, PREF-04, NOTIF-02            |
| NOTIF-05 | `QUEUED`      | M    | Notification controls      | Configure categories, channels, quiet hours, and optional digests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | NOTIF-01, NOTIF-02                     |
| NOTIF-06 | `CANDIDATE`   | M    | Streak-at-risk reminder    | Offer an optional nudge based on the real training schedule rather than generic daily pressure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | SCHED-01, DATA-02                      |
| NOTIF-07 | `CANDIDATE`   | M    | Partner activity alerts    | Notify only for selected partner events and respect both users' privacy preferences.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | SOC-08, NOTIF-05                       |
| NOTIF-08 | `QUEUED`      | L    | Push delivery service      | Store per-device push subscriptions, hold VAPID credentials, send authorized payloads and retire expired or rejected subscriptions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | BE/CT subscription model               |

### Preferences, accessibility, and trust

| ID        | Status      | Size | Feature                               | User-facing behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Dependencies                          |
| --------- | ----------- | ---- | ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| PREF-01   | `SHIPPED`   | M    | Equipment and plate preferences       | Store named training locations with one default, canonical bar weight, available plate pairs and quantities, and normalized equipment. Settings displays and edits weights in the account's kg/lb preference while persisting kg. Shipped 2026-09-08 through `@sunsteel/contracts@0.10.0`, a normalized backend model and authenticated Settings editor.                                                                                                                                                                                                             | FIX-03                                |
| PREF-02   | `SHIPPED`   | M    | Training-goal preferences             | Store up to eight private measurable targets with stable IDs, canonical kilogram values, one non-exercise target per kind and one strength target per exercise. Shipped 2026-09-13 with `PROG-08`; Progress consumes them now and a future dashboard may reuse the same preferences.                                                                                                                                                                                                                                                                                 | PROG-08; `@sunsteel/contracts@0.27.0` |
| PREF-03   | `CANDIDATE` | S    | Dashboard preferences                 | Persist card order and visibility.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | DASH-05                               |
| PREF-04   | `CANDIDATE` | M    | Locale and time preferences           | Configure time zone, week start, language, and formatting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | BE/CT preferences model               |
| PREF-05   | `SHIPPED`   | L    | Plateau sensitivity                   | Plateau watch lets each account choose how many sessions without a new best list a lift, from three to eight (default four). The choice is saved to the account, so Progress and the exercise pages agree on every device; the rule above the list always states the value in use, a save keeps the control on the new value until the list has refetched, and a failed save says which value still applies. The other `PROG-09` thresholds stay fixed. Shipped 2026-09-14 through `@sunsteel/contracts@0.35.0` and migration `20260914210000_plateau_min_sessions`. |
| A11Y-01   | `SHIPPED`   | S    | Reduced motion                        | The OS reduced-motion setting and a per-device Settings choice both remove sliding, scaling, spinning, pulsing and growing motion across the app, the splash and toasts, and shorten fades to 120ms without hiding anything. The device choice applies before first paint, including on signed-out pages, and can add reduction but never override the OS. Shipped 2026-09-13, frontend only.                                                                                                                                                                        | None                                  |
| A11Y-02   | `CANDIDATE` | M    | High contrast and larger controls     | Improve bright-gym readability and one-handed training use.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | None                                  |
| A11Y-03   | `CANDIDATE` | L    | Keyboard and screen-reader completion | Make routine building and set logging fully understandable and operable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Accessibility audit                   |
| EXPORT-01 | `CANDIDATE` | M    | User data export                      | Download routines, sessions, records, and optional measurements in stable formats.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Data inventory                        |
| TRUST-01  | `CANDIDATE` | L    | Account deletion                      | Clearly explain and complete removal of user data and stored media.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | BE deletion workflow                  |
| TRUST-02  | `SHIPPED`   | M    | Privacy dashboard                     | Settings summarises, from saved settings, which live profile sections each audience sees and where: Everyone on the public `/members` link and inside the app, Followers only inside the app, Only me on the own profile. It also lists always-visible identity, member-search discovery and the rules stored for routines and achievements, and previews the signed-out profile. Shipped 2026-09-13, frontend only.                                                                                                                                                 | PROF-06                               |
| TRUST-03  | `CANDIDATE` | M    | Connection and sync state             | Clearly distinguish offline, pending, saving, synchronized, and failed data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | OFFLINE-01                            |

### Deferred infrastructure-heavy features

| ID         | Status     | Size | Feature                   | User-facing behavior                                                                                      | Why deferred / dependencies                                                         |
| ---------- | ---------- | ---- | ------------------------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| OFFLINE-01 | `DEFERRED` | XL   | Offline workout logging   | Keep active sessions writable without connectivity and synchronize later with explicit conflict handling. | Requires durable local queue, idempotent mutations, reconciliation, and recovery UI |
| MSG-01     | `DEFERRED` | XL   | Direct messages           | Support text plus embedded workouts, routines, exercises, and challenges.                                 | Realtime/polling strategy, moderation, notifications, retention policy              |
| MSG-02     | `DEFERRED` | L    | Message requests          | Keep unsolicited messages outside the main inbox and enforce blocks.                                      | MSG-01, PROF-10                                                                     |
| MSG-03     | `DEFERRED` | M    | Unread and delivery state | Track unread counts and reliable delivery without implying end-to-end encryption.                         | MSG-01, NOTIF-01                                                                    |
| MSG-04     | `DEFERRED` | L    | Group discussion          | Add moderated conversation to training groups.                                                            | MSG-01, SOC-12                                                                      |
| MSG-05     | `DEFERRED` | L    | Media attachments         | Share approved images or media with storage quotas and abuse controls.                                    | MSG-01, storage/moderation policy                                                   |
| COACH-01   | `DEFERRED` | XL   | Coach-athlete mode        | Grant a coach explicit access to routines, selected progress, completed sessions, and notes.              | PROF-06, ROUT-14, audit events                                                      |
| COACH-02   | `DEFERRED` | XL   | Coach marketplace         | Allow verified coaches to publish services or programs.                                                   | COACH-01, identity verification, payments, disputes                                 |
| GROUP-01   | `DEFERRED` | XL   | Clubs and teams           | Shared feed, routine library, challenges, roles, and moderation.                                          | SOC-12, MSG-04                                                                      |

### Longer-term intelligent and integration candidates

| ID       | Status      | Size | Feature                          | User-facing behavior                                                                              | Dependencies                         |
| -------- | ----------- | ---- | -------------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------ |
| INTEL-01 | `CANDIDATE` | L    | Adaptive progression suggestions | Recommend changes using comparable performance and RPE while requiring user confirmation.         | LIVE-04, DATA-01, DATA-04            |
| INTEL-02 | `CANDIDATE` | M    | Deload suggestions               | Propose a lighter period from sustained fatigue signals without making medical claims.            | PROG-10, ROUT-09                     |
| INTEL-03 | `CANDIDATE` | L    | Natural-language routine draft   | Turn goals and constraints into an editable wizard draft, never directly publish a hidden plan.   | ROUT-03, EXER metadata               |
| INTEL-04 | `CANDIDATE` | L    | Workout review assistant         | Explain patterns using the user's selected training data and cite the sessions behind each claim. | DATA-01 through DATA-04              |
| INTEG-01 | `CANDIDATE` | L    | Wearable integration             | Import useful workout duration, heart-rate, or recovery signals with explicit provenance.         | Provider selection and consent model |
| INTEG-02 | `CANDIDATE` | XL   | Health-platform integration      | Optionally synchronize workouts and measurements with Apple Health or Health Connect.             | Mobile/PWA feasibility study         |
| INTEG-03 | `CANDIDATE` | M    | External calendar integration    | Synchronize scheduled sessions after the internal schedule model is stable.                       | SCHED-09                             |

## Product rules and retained decisions

- Privacy is designed before activity feeds, public routines, comments, coaching,
  or messaging. Sensitive profile and body data is private by default.
- Progress, achievements, feeds, and notifications build on persistent records,
  events, rollups, and truthful historical snapshots—not repeated lifetime scans.
- Estimated 1RM per lift is the headline strength trend. Total volume is useful
  mainly in exercise and muscle-group context.
- Ranks and leaderboards favor consistency and relative improvement rather than
  absolute load so they remain meaningful across body sizes and experience.
- The active-session screen prioritizes speed, legibility, offline resilience,
  and one-handed use over decorative complexity.
- Social activity is generated from training events with explicit sharing
  controls; users are not required to author conventional social posts.
- Messaging is not an early social feature. It remains deferred until identity,
  privacy, blocking, moderation, notifications, and delivery infrastructure exist.
- Exercise instructions and demonstrations require a maintained content and
  licensing process; engineering alone is not sufficient.
- Automated training recommendations explain their evidence and require user
  confirmation. They do not diagnose injuries, recovery state, or health issues.
- Catalog stars (`EXER-07`) are private working preferences in their own
  model. The `PROF-05` profile favorites are identity metadata governed by
  profile privacy and must not be repurposed as a browsing or wizard
  preference, and stars must never appear on a profile.
- An interim fix that replaces one false promise with another is not an
  improvement. When the honest short version of a feature would still mislead,
  leave the surface visibly unfinished and wait for the real capability. This
  rule comes from the `FIX-05` decision on 2026-09-06 and applies to any
  half-shipped social or sharing surface.

## Rejected features

No product feature has been formally rejected yet. When one is rejected, retain
its stable ID here with the date and reason so a future session does not propose
it again without addressing the original decision.

## Completion log

| Date       | Feature IDs                                                                                                  | Evidence                                                                                                                                                                                                                                                                                                             | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-06 | CORE-01 through CORE-03, PROF-01 through PROF-02, SOC-01, ROUT-01 through ROUT-02, LIVE-00, HIST-01, DASH-01 | Verified against the three local repositories                                                                                                                                                                                                                                                                        | Initial product snapshot and roadmap created.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-09-07 | LIVE-01                                                                                                      | FE `hooks/use-rest-timer.ts`, `lib/utils/rest-timer.utils.ts` (+ tests), `lib/utils/rest-alert.ts`, `features/workout/rest-timer-bar.tsx`, and the restSeconds/completion wiring in `session-progress.utils.ts`, `exercise-group.tsx`, `set-log-input.tsx`, `use-set-log-form.ts`; `npm run verify` clean, 104 tests | Ticking a set starts its exercise's stored `restSeconds`, with +15s, skip and an end-of-rest tone plus vibration. The countdown derives every reading from an absolute deadline rather than accumulating ticks, so a backgrounded tab, a throttled interval or a sleeping phone cannot make it drift -- the case the roadmap called out, and what the ten pure tests in `rest-timer.utils.test.ts` cover. The audio context is primed inside the tap that starts rest, because browsers only allow one to start from a user gesture and the alert fires minutes later with no gesture of its own. `restSeconds` had to be carried through `groupSetLogsByExercise`, which dropped it exactly as it dropped `rpe`. Locked-screen alerts are out of scope and remain with `NOTIF-02`; unticking a set does not restart rest. **Device-verified 2026-09-07:** the end-of-rest tone plays on the owner's phone with the app in the foreground and the screen unlocked, which confirms the AudioContext priming works from the set-completion tap. Neither hook is unit-tested -- Vitest is Node-only with no jsdom or RTL -- so this device pass is the only evidence for the audio path; the countdown arithmetic is separately covered by ten tests.                                                                                                                |
| 2026-09-07 | LIVE-02                                                                                                      | FE `hooks/use-screen-wake-lock.ts`, `app/(protected)/workouts/sessions/[id]/page.tsx`; `npm run verify` clean                                                                                                                                                                                                        | Gated on `session.status === 'IN_PROGRESS'` rather than on the route, so finishing or aborting drops the lock without waiting for the redirect. The browser releases a wake lock whenever the document is hidden and does not restore it, so the hook re-acquires on `visibilitychange` instead of assuming one request lasts the session. Unsupported browsers (iOS Safari before 16.4, Firefox) and a refusal under battery saver both resolve to a status value and never surface an error. Not covered by tests: the suite is Node-only with no jsdom or RTL, so hooks cannot be rendered -- see the accepted limitation in technical-debt.md. **Device-verified 2026-09-07 on iPhone:** the screen did not auto-lock while an in-progress session was open, so the lock is acquired and held on iOS Safari 16.4+.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-09-07 | LIVE-04                                                                                                      | FE `features/workout/set-log-input.tsx`, `hooks/use-set-log-form.ts`, `lib/utils/session-progress.utils.ts`, `lib/utils/session-validation.utils.ts`, `lib/utils/workout-session.types.ts`; `npm run verify` clean                                                                                                   | RPE capture was frontend-only: the column, the DTO and the write were already in place (`SetLog.rpe`, `upsert-set-log.dto.ts`, `workout-session-log.service.ts`), and history already rendered `n/10`. Three frontend links were missing, so the history column could never be anything but `-`: no input existed, `UpsertSetLogPayload` had no `rpe` field, and `groupSetLogsByExercise` dropped the stored value (its `as GroupedExerciseLogs` cast hid that from the type checker). Validation bounds RPE to the 0-10 scale the display already asserts; an empty box saves as `undefined`, never 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-09-07 | LIVE-03                                                                                                      | CT `@sunsteel/contracts@0.8.1`; BE `GET /workouts/sessions/:id/previous-performance`, `workout_previous_performance` index and three focused tests; FE `usePreviousPerformance`, `previous-performance.utils.ts` (+ five tests), `exercise-group.tsx` and `set-log-input.tsx`; all three repository gates clean      | The current session now loads one compact comparison instead of paginating workout history: the backend selects the latest earlier `COMPLETED` session with the same immutable `sourceRoutineDayId`, returns only completed sets, and keys them by preserved `routineExerciseId` plus set number so duplicate uses of one exercise cannot cross-wire. The UI shows `Last time` below each matching input and labels weight-then-reps improvements with text and colour; bodyweight/zero-load rows omit a false `0 kg`. Browser-verified in an authenticated session at 541 px and an emulated 390 px viewport with both improved and non-improved rows visible and no console warnings/errors. **Owner-confirmed 2026-09-07:** the completed flow works end to end. The hook and components are not rendered by tests because Vitest remains Node-only; the owner's confirmation did not identify a specific physical device.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-09-07 | DATA-01, DATA-03, DATA-04, DATA-05                                                                           | [TD-27 closure record](../history/td27-closure-2026-09-07.md); BE `20260906180000_analytics_expand` and `20260907090000_analytics_fk_cutover`, `src/workouts/analytics/`; CT `@sunsteel/contracts@0.7.0`                                                                                                             | The analytics foundation reached production. All seven accounts READY with a 7/7 legacy/projection match; a repeat rebuild preserved totals, rollups, record frontiers and checksum with the event count steady at 94. The FK cutover applied with all three relations `SET NULL` and 39 sessions/39 snapshots verified. `legacy-workout-progress.service.ts` and the `WORKOUT_PROGRESS_PROJECTION_READS` branch were then removed, which closes `TD-27`. **`DATA-02` deliberately stays `IN_PROGRESS`:** `TrainingEventType` still emits only `SESSION_COMPLETED` and `PERSONAL_RECORD`, so `LIVE-08`, `ACH-01` and `ACH-05` must extend the enum and its writers before they can read what they need. `WORKOUT_ANALYTICS_JOBS` is not a rollout flag and remains required for new accounts and rebuilds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-09-07 | FIX-01, FIX-02, FIX-04, FIX-06, FIX-07, FIX-08                                                               | FE `app/(protected)/profile/[[...userId]]/page.tsx`, `app/(protected)/workouts/page.tsx`, `app/(protected)/search/page.tsx`, `app/(protected)/dashboard/components/StatsOverview.tsx`; `npm run verify` clean                                                                                                        | The frontend-only honesty group, shipped as one change with no contract publish, no backend and no migration. `FIX-01` leaves an honest `No bio yet.` empty state and drops the invented location; `PROF-04` still owns the real editable fields. `FIX-04` hides the entry point behind `SHOW_QUICK_WORKOUT_ENTRY` and leaves `handleStartEmptyWorkout` wired for `LIVE-06`. `FIX-08` also corrects a defect: the lifetime card showed `0 left` forever past 50 sessions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-09-07 | FIX-03                                                                                                       | FE `lib/utils/weight-unit.ts` (+ seven tests), `hooks/use-weight-unit.ts`, active-session, routine, history, dashboard, profile and Settings consumers; local lint, typecheck and 117 tests clean                                                                                                                    | The conversion boundary shipped before the selector and leaves every API payload canonical in kilograms. An authenticated browser pass covered lb and kg across the active session, previous-performance comparisons, dashboard, profile and routine editor; Settings persisted both directions, returning the owner's account to its original kg preference. A first browser pass exposed rounded-pound mount noise that tried to autosave unchanged sets; the final tolerance and transition guard remove those writes, and a repeat pass saw no set-log requests or console warnings/errors. Components and hooks are not rendered in Vitest because the suite intentionally has no jsdom or RTL. **Device-verified 2026-09-07 on the owner's phone:** the kg/lb flow worked without a reported issue.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-09-07 | LIVE-10                                                                                                      | BE removal of `WorkoutMaintenanceService`, heartbeat on the reused start path and two focused recovery tests; FE global `stale-session-recovery-dialog.tsx`, `session-recovery.ts` (+ five tests) and resume/cache-heartbeat wiring; BE verify/CI clean, FE lint/typecheck/122 tests and CI build clean              | The old maintenance timer silently changed an `IN_PROGRESS` session to `ABORTED` after 48 hours, which made recovery impossible. Active sessions now remain recoverable until their owner explicitly resolves them. At the same threshold, the protected shell offers Resume, finish saved work, or discard; it conditionally fetches session detail because the compact active-session response does not carry set logs. Resume re-enters through the idempotent start endpoint so the backend records fresh activity. Successful set writes also refresh the cached activity deadline, preventing a long-open tab from raising a stale prompt after real work. No shared shape changed, so no contract publish or migration was needed. **Authenticated-browser verified 2026-09-08 on a test account:** a temporarily shortened local threshold exercised all three choices without console errors. Resume heartbeated and reopened the intact 5/12-set session; Finish saved work returned 200, preserved those five completed sets in history and cleared the active session; a new 0/1-set session then exercised Discard, returned 200 and disappeared from active sessions without entering completed history. The 48-hour production threshold was restored after each pass. The component itself remains outside Vitest's Node-only rendering boundary. |
| 2026-09-08 | LIVE-07                                                                                                      | CT `@sunsteel/contracts@0.9.0`; BE live frontier calculation and five focused tests; FE typed mutation result, unit-aware celebration formatter (+ three tests) and toast integration; all three repository gates and clean CI builds green                                                                          | The set-log mutation now returns the serialized saved set plus every independently crossed record category. The backend compares by stable exercise ID against completed history and other completed sets in the active session, excludes the row being edited, and also compares its pre-mutation values so RPE-only or identical autosaves cannot celebrate twice. Reps remain valid for bodyweight work; weight, volume and Epley estimated 1RM require positive load and reps. No migration was needed and DATA-01's completion-time analytics frontier remains unchanged. **Authenticated-browser verified 2026-09-08 on the test account:** completing a synthetic Barbell Curl set produced one grouped four-record celebration with canonical kg values; an RPE-only autosave returned 200 without a second celebration, no console warnings/errors occurred, and the synthetic set was marked incomplete before the session was finished so it did not replace the persisted personal record. The hook/toast is not rendered by Vitest because the suite remains Node-only.                                                                                                                                                                                                                                                                              |
| 2026-09-08 | FIX-09                                                                                                       | FE `lib/utils/time-format.utils.ts` (+ eight tests), dashboard recent activity, history list and history-detail consumers; `npm run verify` clean in an isolated Node 22 worktree; frontend CI green                                                                                                                 | Removed the competing `formatTimeReadable` implementation and routed every human-readable workout duration through the hour-aware formatter already used by the active-session header. **Authenticated-browser verified 2026-09-08:** the same 22-hour session now renders as `22h 3s` in both Dashboard and Workout History, while short and one-hour sessions retain compact output. No backend, contract or migration change was needed. Components remain outside Vitest's Node-only rendering boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-09-08 | FIX-10                                                                                                       | FE `lib/utils/routine-format.ts` (+ three tests), `RoutineMetaBadges.tsx` and `SelectedDaysSummary.tsx`; focused lint, typecheck and 136 tests clean; frontend CI green                                                                                                                                              | The two incorrect hardcoded labels now use one pure singular/plural rule. **Authenticated-browser verified 2026-09-08:** a one-day routine card renders `1 day/week`, and the creation wizard transitions from `0 days/week` to `1 day/week` after selecting one day without saving a routine. No backend, contract or migration change was needed. Components remain outside Vitest's Node-only rendering boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-09-08 | LIVE-19                                                                                                      | FE active-session action card, status-aware confirmation dialog, `use-session-management.ts` and `session-resolution.ts` (+ three tests); isolated `npm run verify` clean with 139 tests                                                                                                                             | Reused the existing `ABORTED` finish-session path instead of adding an endpoint or publishing contracts. The owner can now discard from any active session, with explicit permanent-action copy and mutation feedback. **Authenticated-browser verified 2026-09-08 on the test account:** opening and cancelling preserved the 0/1-set session; the confirmed discard then showed `Workout discarded`, routed to Dashboard and left `/workouts` at `No Active Workout`. The component and hook remain outside Vitest's Node-only rendering boundary; their wiring was covered by the authenticated browser pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

| 2026-09-08 | PREF-01 | CT `@sunsteel/contracts@0.10.0`; BE `TrainingLocationPreference`, `GET/PUT /users/training-locations`, migration and three focused tests; FE Settings editor, query/mutation hooks and `training-location-preferences.ts` (+ three tests); all repository gates and CI green | Equipment preferences now have stable per-location identities, exactly one default when any exist, canonical kilogram storage, bounded plate-pair quantities and normalized equipment. Replacement is transactional, rejects foreign IDs before writing and supports rename swaps without uniqueness collisions. The migration is applied in Neon. **Authenticated-browser verified 2026-09-08 using the gitignored state produced by `npm run ui:login`:** saved and reloaded Home Gym with a 20 kg bar, one 20 kg plate pair and barbell/bench/rack; GET and PUT returned 200, changing the unsaved account unit rendered both weights as 44.09 lb, and no console errors occurred. Components and hooks remain outside Vitest's Node-only rendering boundary. |
| 2026-09-08 | LIVE-05 | FE `plate-calculator-dialog.tsx`, finite-inventory `plate-calculator.ts` (+ five tests) and the weighted-exercise entry point; isolated `npm run verify` clean with 147 tests | Uses the published PREF-01 read path without a backend or contract change. The bounded calculator preserves pair quantities and finds the best valid combination rather than assuming a greedy plate order; the UI defaults to the next incomplete target and default location while keeping both editable. **Authenticated-browser verified 2026-09-08 using `ui:login` state at a 390 × 844 viewport:** Home Gym's 20 kg bar plus its single 20 kg plate pair resolved 60 kg exactly; a 150001 kg target stopped truthfully at the 60 kg inventory maximum, a 15 kg target warned that the bar was 5 kg too heavy, and no console errors or Next.js overlay occurred. The temporary 0/1-set session was discarded after the pass. The dialog remains outside Vitest's Node-only rendering boundary. |
| 2026-09-09 | LIVE-08 | CT `@sunsteel/contracts@0.11.0`; BE `PROGRESSION_CHANGED` migration, pure progression outcome (+ four tests), idempotent event writer and typed finish response; FE result dialog and unit-aware presentation (+ three tests); all repository gates, integration CI and deployments green | Session finish now returns the completed session plus every prescription change and stores one stable event per session/exercise. Double progression requires every prescribed set to be completed at target; dynamic progression reports only the completed sets that individually reached target. Typed-but-incomplete sets can no longer advance load. Repeating the same finish reads the original event instead of returning an empty or duplicated result. **Authenticated-browser verified 2026-09-09 on the test account:** one completed Incline Dumbbell Press set at 10/10 and 40 kg produced `SET_REACHED_TARGET`, displayed `40 → 42 kg`, and the Continue action returned to Dashboard with no console errors. A repeated finish against the deployed Railway API returned the same change with HTTP 200, and the deployed routine read confirmed the next prescription is 42 kg. The frontend hook/dialog remains outside Vitest's Node-only rendering boundary. |
| 2026-09-09 | LIVE-09 | CT `@sunsteel/contracts@0.12.0`; BE reconstructible recap service, `GET /workouts/sessions/:id/recap`, typed finish payload and four recap tests; FE finish dialog, history panel, query/cache integration and recap formatters (+ two tests); isolated repository gates, CI and Railway/Vercel deployments green | No migration was required: completed-set history reconstructs the final per-session record frontier, `PROGRESSION_CHANGED` events reconstruct prescription changes, and the preserved routine-day identity selects the previous comparable session. Finish returns the recap immediately while the owner-scoped read makes it stable and reopenable. **Authenticated-browser verified 2026-09-09 on the test account at 390 × 844:** finishing one Incline Dumbbell Press set at 10 × 42 kg displayed 1m 35s, 420 kg, one completed set, three records, `42 → 44 kg`, the previous-session deltas and a test note before Continue returned to Dashboard; reopening the same session from history returned 200 with every block present and no console, page or failed-network errors. The note was added by the test harness because note entry remains separately owned by `LIVE-16`. Production API reconstruction was also verified directly. `DATA-02` remains `IN_PROGRESS` only for future milestone and achievement event families. |
| 2026-09-09 | PROF-03 | CT `@sunsteel/contracts@0.13.0` username shapes and shared format/reserved-name constants; BE required unique column migration, normalized writes, private search response and handle-or-UUID profile resolution (+ seven focused tests); FE Settings editor, handle search/results, readable profile routes and shared validation (+ two tests); isolated repository gates, PostgreSQL integration CI and Railway/Vercel deployments green | Existing accounts received deterministic opaque `member_<hash>` handles; new accounts derive a readable base plus a non-email hash, and owners can choose a lowercase 3–30 character handle. Database uniqueness and format constraints protect persistence while the service rejects reserved product/route names and maps collisions to 409. Search no longer selects or returns email. **Authenticated-browser verified 2026-09-09 in production:** `settings` was rejected before submission, `codex-prof03-0909` saved with HTTP 200, the own profile opened at `/profile/codex-prof03-0909`, exact `@username` search opened another account by handle with the follow action intact, and its legacy UUID URL still resolved to the same profile; no console errors occurred. `PROF-12` still owns a signed-out public route and truthful Share Profile action. |
| 2026-09-09 | PROF-06 | CT `@sunsteel/contracts@0.14.0` visibility enum, five-domain settings/access maps and typed private/public profile shapes; BE required five-column private-default migration, validated replacement endpoint and centralized owner/follower/public resolver (+ five focused tests); FE privacy settings plus authorized training-summary, record and body-metric profile states; isolated repository gates, CI and Railway/Vercel deployments green | Authorization is enforced before sensitive reads: denied viewers do not trigger analytics, personal-record or body-metric queries, and email is absent from every member-profile response. Owners always retain access; follower visibility uses the existing one-way follow relation. Routine and achievement policies are stored but expose no content until their dedicated features ship. **Authenticated-browser verified 2026-09-09 in production:** all five settings began at `Only me`; a mixed public/follower/private selection saved with HTTP 200 and persisted after reload; an existing followed member with private defaults returned no training summary, records, body metrics or email and rendered private states without console errors. The test account was restored to all-private. `PROF-12` still owns signed-out access. |
| 2026-09-09 | PROF-04 | CT `@sunsteel/contracts@0.15.0` optional biography/location fields, shared limits and two added privacy domains; BE required nullable-field/private-default migration, normalization and authorization-before-read behavior with two new focused tests plus expanded access assertions; FE settings editors, counters, two privacy selectors and honest About states; isolated repository gates, CI and Railway/Vercel deployments green | Biography is limited to 500 characters and location to 100; writes trim surrounding whitespace and normalize blanks to null. Both visibility settings default to private and may be set independently to followers or everyone. Denied reads do not query or serialize the values. **Authenticated-browser verified 2026-09-09 in production:** biography/location saved with HTTP 200, their mixed public/follower policies saved with HTTP 200, all four values persisted after reload, and the own profile rendered the saved multi-line biography and location. A followed member with private defaults returned both access flags false, omitted both values from JSON and rendered distinct private states; no console errors occurred. The test account's original values and policies were restored. Signed-out access remains with `PROF-12`. |
| 2026-09-10 | PROF-05 | CT `@sunsteel/contracts@0.16.0` structured training-identity types, shared limits and an added privacy domain; BE enum columns, ordered favorite-exercise relation, private-default migration, validated writes and authorization-before-read behavior with expanded focused tests; FE settings editor, catalog search, privacy selector, label utilities/tests and honest profile states; isolated gates, CI and Railway/Vercel deployments green | Broad identity labels stay separate from measurable `PROG-08` targets: goals and disciplines are capped at three, favorite exercises at five, and favorites reference real catalog rows. Denied profile reads neither query nor serialize identity. **Authenticated-browser verified 2026-09-10 in production:** a complete identity including Bench Press saved with HTTP 200, persisted after reload, its privacy policy saved with HTTP 200, the allowed response and own profile rendered every field, and an existing private member omitted the payload and rendered the private state. Desktop/mobile captures showed no overflow; console errors and failed requests were empty. The test account's original identity and policy were restored. Signed-out access remains with `PROF-12`. |
| 2026-09-10 | PROF-09 | CT `@sunsteel/contracts@0.17.0` discovery settings and replacement request; BE three-column migration, validated owner update endpoint and independently gated name/username search (+ two focused tests); FE Account Discovery settings card, typed service/hook and query-cache reconciliation; isolated gates, CI and Railway/Vercel deployments green | Existing name and username discovery stays enabled by default, while future contact matching defaults to explicit opt-in and the UI states that Sunnsteel does not upload or match contacts today. The current search applies name and username controls independently; direct profile URLs remain available and section privacy remains the authorization boundary. **Authenticated-browser verified 2026-09-10 in production:** mixed settings saved with HTTP 200 and persisted after reload; with both current discovery paths disabled, the own handle URL still opened; the 390 x 844 view rendered without overflow or console/page errors. The account was restored to name and username enabled with contact matching disabled. A live hidden-result check from a second account was not available; backend query-shape tests cover both filters. The roadmap size is corrected from `M` to `L` because the delivery required a schema migration. |
| 2026-09-10 | PROF-12, FIX-05 | BE unguarded `GET /profiles/:identifier`, anonymous privacy evaluation and no-store response (+ focused privacy assertions); FE public `/members/<username>` shell, shared profile presentation, unauthenticated query and resilient copy-link action (+ two utility tests); no CT publish or migration; isolated repository gates, CI and Railway/Vercel deployments green | The public route never depends on the routing cookie or authenticated shell. Its backend read reuses `PublicUserProfile` but supplies no viewer, so `FOLLOWERS` and `PRIVATE` sections are denied before their sensitive queries run; email remains absent. The authenticated route and follow controls are unchanged. **Production-browser verified 2026-09-10 at 390 x 844:** a signed-out visit stayed on `/members/codex-prof03-0909` instead of redirecting to Login and rendered the account's private states; the saved test session opened `/profile/codex-prof03-0909`, copied the exact public URL, confirmed success and reopened it. Page errors, console errors and failed frontend/backend responses were all zero. Clipboard-denied behavior also rendered explicit failure feedback. The live account stayed all-private; mixed public/follower authorization is covered by the focused backend test rather than a production privacy mutation. |
| 2026-09-10 | PROG-01 | CT `@sunsteel/contracts@0.18.0` query/summary/point/response types; BE `GET /workouts/progress/strength`, partial event-expression index and three focused tests; FE active Progress navigation, service/query integration, responsive record cards, two hand-rolled SVG charts and range/chart utilities (+ three tests); isolated repository gates, PostgreSQL integration CI and Railway/Vercel deployments green | The view intentionally shows the persisted best-set record frontier rather than every performance: `PROG-02` still owns session-by-session sets, RPE and notes. Bounded ranges include the last pre-range record as a baseline and all reads cap at 500 changes without a lifetime `SetLog` scan. **Authenticated-browser verified 2026-09-10 in production:** the test account exposed 22 trained exercises; Deadlift returned five all-time points, 90-day and 30-day reads exercised carried baselines, switching to Dumbbell Curl and All rendered both charts plus the exact 12 kg × 12 record row, and every strength request returned 200. The 390 × 844 view had zero horizontal overflow; console, page and failed-request logs were empty. Components remain outside the Node-only Vitest rendering boundary. The roadmap size is corrected from `M` to `L` because the delivery required an indexed database migration and activated a new destination. |
| 2026-09-11 | PROG-02 | CT `@sunsteel/contracts@0.19.0` exercise summary, prescription, session, query and paginated response types; BE `GET /workouts/progress/performance`, partial completed-set/progression-event indexes and three focused tests; FE shared selector/range integration, infinite query, responsive performance cards and three API-contract assertions; isolated repository gates, PostgreSQL integration CI and Railway/Vercel deployments green | Snapshot-backed routine/day context and prescription notes stay truthful after routine edits, while canonical session notes, completed `SetLog` rows and `PROGRESSION_CHANGED` events supply the remaining fields. Terminal completed and aborted sessions are included only when the exercise has completed reps; exercise-specific observations are not invented because `LIVE-16` still owns that input. **Authenticated-browser verified 2026-09-11 in production at 390 × 844:** the test account exposed 24 trained exercises; Deadlift loaded 10 sessions plus two through a real cursor, an earlier page contained RPE, switching to Incline Dumbbell Press exposed a session note and progression, and `All` preserved every section. Four performance reads returned 200, width stayed 390/390, and console, page, failed-request and HTTP-error logs were empty. Components and hooks remain outside the Node-only Vitest rendering boundary. The roadmap size is corrected from `M` to `L` because indexed database migration and a published contract were required. |
| 2026-09-11 | PROG-03 | CT `@sunsteel/contracts@0.20.0` query, weekly cell and response types; BE `GET /workouts/progress/muscles`, bounded active-projection read and three focused tests; FE 4/8/12-week control, top-muscle summary, accessible numeric heatmap and five utility/API-contract assertions; isolated repository gates, PostgreSQL integration CI and Railway/Vercel deployments green | Existing `WorkoutMuscleRollup` rows already preserve the requested primary 1.0 / secondary 0.5 completed-set contribution, so no migration or backfill was needed. The endpoint reads only 4–12 Monday-based weeks from the active projection, fills every canonical muscle/week with truthful zeroes and marks the current partial week; it never scans session logs. **Authenticated-browser verified 2026-09-11 in production:** Europe/Berlin returned all 17 muscles and 136 initial cells; switching 8W → 4W → 12W produced three HTTP 200 responses and 204 cells at 12 weeks, with Quads, Lats and Glutes leading the live account. At 390 px the 998 px table scrolled inside a 364 px container while the page stayed 390/390; the 768 px shell similarly contained 704 px inside 462 px, and the 1440 px table fit without scrolling. Console, page, failed-request and HTTP-error logs were empty. Component rendering remains outside the Node-only Vitest boundary. |
| 2026-09-12 | PROG-04 | CT `@sunsteel/contracts@0.21.0` weekly point/series/query/response types; BE `GET /workouts/progress/volume`, three focused tests and PostgreSQL integration coverage; FE independent 4/8/12-week control, total/muscle/routine/exercise breakdowns, unit-aware summaries and four utility/API-contract assertions; isolated repository gates, all three CI pipelines and Railway/Vercel deployments green | Existing overall and muscle `DATA-03` weekly rollups supply the projected totals; historical routine snapshots plus completed-session summaries and completed set logs supply the two bounded detail dimensions. No migration or rebuild was required. The current partial week is marked and excluded from the full-week percentage comparison; bodyweight work counts sets without inventing load, and muscle totals retain 1.0/0.5 weighting. **Authenticated-browser verified 2026-09-12 in production:** 4W, 8W and 12W all returned populated totals; Total, Muscle, Routine and Exercise rendered their correct set/set-equivalent wording, selectors and exact accessible weekly values. The 8W total showed 343 completed sets and a latest full week of 27,535 kg, +41% versus its prior full week. At 390 and 768 px only the chart scrolled horizontally (896/350 and 904/623) while the page remained exactly 390/390 and 768/768; at 1440 px the chart fit. The pass exposed two controlled-selector warnings, fixed them in separate commits, then confirmed reload plus scope switching added no console warnings or errors. No account data was changed. |
| 2026-09-13 | PROG-06 | CT `@sunsteel/contracts@0.22.0` query, routine-day, session, exercise, set and response types; BE `GET /workouts/progress/session-comparison`, repeatable-read composition and three focused tests; FE routine-day selector, metric/notes comparison, exact-set accordion and two matching/query assertions; isolated repository gates, all three CI pipelines and Railway/Vercel deployments green | Existing `DATA-04` snapshots and stable `sourceRoutineDayId` / `routineExerciseId` / `setNumber` identities were sufficient, so no schema migration or backfill was needed. Routine-day discovery is capped at 100 and the selected comparison loads only its latest two completed sessions, persisted notes and completed sets. **Authenticated-browser verified 2026-09-13 in production:** the default Thursday comparison rendered duration, 6,500 vs 5,885 kg, 3 vs 15 completed sets, both notes and exact Deadlift set rows; switching to Wednesday loaded a second comparison, while a one-session Friday rendered explicit unavailable values. The page had no horizontal overflow, retained one visible `h1` and rendered the section in both themes at 320, 375, 390, 430, 768, 1024, 1280 and 1440 px; browser warnings and errors were empty. The 14-case automated Progress layout slice also passed in production. The full 283-case sweep was invoked but its unrelated public Login cases report a production 404 resource error. No account data was changed. |
| 2026-09-13 | PROG-07 | CT `@sunsteel/contracts@0.23.0` timeline query, cursor, session context and discriminated record/progression event types; BE `GET /workouts/progress/timeline`, stable combined ordering, previous-record explanation and four focused tests; FE infinite-query hook, All/Records/Progression controls, ruled event list, expandable set changes and four utility/API assertions; isolated repository gates, all three CI pipelines and Railway/Vercel deployments green | Existing owner-scoped `PERSONAL_RECORD` and `PROGRESSION_CHANGED` events plus `DATA-04` snapshots were sufficient, so no schema migration or historical backfill was needed. The endpoint returns 20 events by default, caps pages at 50, validates the cursor against its owner and filter, and joins only the immediately previous record for each returned frontier. **Authenticated-browser verified 2026-09-13 in production:** the mixed feed interleaved both event types; Records explained first frontier, heavier-load and same-load-more-reps cases; Progression expanded exact old → new set prescriptions and rule evidence; Load earlier appended an older cursor page; every event linked to its session recap. Both themes rendered at 320, 639, 640, 767, 768, 1023, 1024, 1279, 1280, 1439 and 1440 px with page width equal to viewport and no console errors. After refreshing the saved dev login, the automated Progress layout slice also passed 14/14 across light/dark at 320, 390, 430, 768, 1024, 1280 and 1440 px. The production checks used the already authenticated in-app browser and changed no account data. Historical output is intentionally limited to the persisted best-set record frontier rather than inventing the four transient LIVE-07 celebration categories. |
| 2026-09-13 | NAV-06 | FE `features/shell/components/Sidebar.tsx` (`History` entry → `/workouts/history`), introduced in `9afcb44` on 2026-09-06 | Recorded retroactively. The dedicated navigation entry already met the criterion: `/workouts` redirects into a live session, so its own "View History" button is unreachable while training, and the sidebar entry keeps history one tap away in both states. No code change was needed. |
| 2026-09-13 | SOC-02 | CT `@sunsteel/contracts@0.24.0` relationship member, list, cursor and suggestion types; BE `user-relationships.service.ts`, `GET /users/:identifier/followers\|following\|mutuals` and `GET /users/me/suggestions` with six focused tests; FE `/profile/<identifier>/<kind>` lists, linked profile counts, search suggestions, `useRelationships` and eleven utility tests; all three CI pipelines, Railway `ef1c4c2` and Vercel `a96c151` green | Opaque `createdAt\|userId` cursors page `UserFollow` newest first; viewer follow state is resolved in two bounded reads per page, and suggestions read at most 200 candidates of each kind. `0.23.0` was taken by `PROG-07` in parallel, so this delivery rebased and published `0.24.0`. **Locally verified 2026-09-13** on an integration build of all four items (frontend :3001, SOC-02 backend :4001) with the saved owner session: every list, the mutuals copy, suggestions and the unknown-subpath state rendered in both themes at 320/390/768/1024/1440 px with zero page overflow, console errors or failed requests; a Follow/Unfollow round trip on a list row restored the original relation; the full `ui:regression` sweep passed 283/283. **Production:** the new routes answer 401 unauthenticated and an unknown sibling route 404 on Railway; **Authenticated-browser verified 2026-09-13 in production** with a fresh owner sign-in: followers (4), following (4), mutuals (2), suggestions (2), the unknown-subpath state and linked profile counts rendered in both themes at 320/390/768/1024/1440 px with zero page overflow, console errors or failed Vercel/Railway requests. The production pass was read-only; the follow round trip was exercised locally. |
| 2026-09-13 | DASH-02, DASH-10, TRUST-02 | FE `dashboard-primary-action.ts` (+ seven tests) and `TodaysWorkouts.tsx`; `empty-states.ts` (+ three tests), `components/layout/empty-module.tsx`, dashboard modules and history list; `privacy-overview.ts` (+ three tests) and `privacy-overview-card.tsx`; frontend CI and Vercel `a96c151` green | Frontend-only, no contract publish. DASH-02 also closes a gap where an open session for a day not scheduled today offered no Resume, and removes a second filled Start when two workouts were planned (§4.3 rule 1). DASH-10 fixes Clear filters leaving a Completed status chip behind. Verified in the same local authenticated pass and 283/283 sweep as SOC-02: the dashboard card rendered one filled Start among two planned workouts, the filtered history offered Clear filters and returned the URL to the default, and the privacy overview rendered in both themes at 390/768/1440 px without overflow. The same authenticated production pass found one filled Start on the dashboard card, Clear filters returning history to the default URL, and Settings, Dashboard and History clean in both themes at every width. |
| 2026-09-13 | SOC-07 | CT `@sunsteel/contracts@0.25.0` share field, request, link and shared-recap types; BE `SessionShare` migration `20260913180000_session_shares`, `workout-session-share.service.ts`, owner `/workouts/sessions/:id/shares` routes and unguarded `GET /shared/sessions/:token` with twelve focused tests; FE `SessionShareButton` on the history recap, field-scoped `SessionRecapContent`, public `/shared/sessions/<token>` page and three utility tests; all three CI pipelines, Railway `827e893` (migration applied) and Vercel `09342d9` green | Links carry a 144-bit base64url token; the public read resolves only active tokens, rebuilds the recap and serializes only the selected fields plus the owner's identity and weight unit, with `Cache-Control: no-store`. Profile privacy does not gate a share: creating one is explicit per-session consent, while the linked `/members` profile keeps its own privacy. **Authenticated-browser verified 2026-09-13 in production:** the dialog opened with duration, volume, completed sets and records checked and progression/notes unchecked; creating a link returned 201 with exactly those fields; signed out, the page showed the routine, owner and those four regions, hid notes, progression and the comparison, exposed no email and linked to `/members/<username>`, with zero page overflow in both themes at 320/390/768/1024/1440 px and no failed Vercel/Railway requests. Revoking returned 204 and the same URL then rendered "This link is no longer available". An interrupted first run had left one active link; it was revoked in the same pass, so no public link remains. |

| 2026-09-13 | A11Y-01 | FE `lib/utils/motion-preference.ts` (+ five tests), `hooks/use-motion-preference.ts`, `features/settings/motion-preference-card.tsx`, the pre-paint script in `app/layout.tsx`, the `data-motion` mirror in `app/globals.css`, `MotionConfig` in `providers/app-provider.tsx`, `InitialLoadAnimation.tsx` and `components/ui/toast.tsx`; local `npm run verify` (211 tests, build) with the mirror confirmed in the compiled stylesheet; frontend CI and Vercel `15375bf` green | The OS `prefers-reduced-motion` block already covered CSS; the gaps were framer-motion values it cannot reach and the absence of any in-app choice. The new Settings "Reduce motion" choice is stored per device, not per account: it must apply before first paint on the splash and on signed-out pages, before any account data loads, and syncing it would need a contract field. It can only add reduction, never re-enable motion the OS reduced. **Authenticated-browser verified 2026-09-13 in production:** enabling it set `data-motion="reduce"` and `ss-motion`, dropped `--motion-base` from 0.2s to 0.12s and stopped a probe spinner; all of that survived a reload and reverted when disabled. With the OS setting emulated, the card showed checked, disabled and explained why. A stored preference applied before paint on signed-out `/login`, and the reduced mobile splash rendered its load bar complete instead of growing it. The card had no overflow in both themes at 320/390/768/1024/1440 px, and no console or page errors occurred. |
| 2026-09-13 | EXER-09 | CT `@sunsteel/contracts@0.26.0` `MOVEMENT_PATTERNS`, `EXERCISE_MECHANICS`, `EXERCISE_EQUIPMENT` and six new `Exercise` fields; BE single-source `prisma/exercise-catalog.ts`, migration `20260913200000_exercise_metadata` (two enums, six columns, two indexes, 80 generated backfill updates), `mapExercise` contract boundary and six focused tests in `scripts/exercise-catalog.test.ts` (`917666f`, `f04ba72`); FE contract bump `2cc2a82`; all three CI pipelines and Railway `f04ba72` (migration applied) green | Backend and contracts only; no user-facing surface changes. One catalog file now feeds both the seed and the migration: `buildExerciseMetadataSql()` generated the backfill, and a test fails if the two drift, so reclassifying production exercises needs a new generated migration, not only a seed run. Equipment is stored as text but `GET /exercises` drops values outside the closed vocabulary. The first deploy was gated by a red `analytics-integration` job because its hand-assembled test schema lacked the new migration and the generated client selected the new columns; `f04ba72` appended it and the pipeline went green before Railway deployed. **Production API verified 2026-09-13** with the owner session: `GET /exercises` returned 200 with 80 exercises, none missing a pattern, mechanic, substitution group or equipment; 22 movement patterns and 30 substitution groups were in use, `instructions` was empty and `mediaUrl` null for all of them. |
| 2026-09-13 | ROUT-10 | FE `features/routines/wizard/utils/routine-quality.ts` (+ thirteen tests), `components/RoutineQualitySummary.tsx`, `hooks/useRoutineQualitySummary.ts` and the review step in `ReviewAndCreate.tsx` (`c4985c2`); no contract publish, backend change or migration; worktree `npm run verify` (224 tests, build), frontend CI run 34773723473 and Vercel `c4985c2` green | The review step of both the create and edit wizards now shows weekly muscle set-equivalents (primary 1.0 / secondary 0.5, the Progress weighting), a per-day duration estimate with its assumptions printed beneath it, the equipment the routine needs checked against the default training location, and likely imbalances. Everything is derived client-side from the planned sets and the `EXER-09` catalog metadata already cached under `['exercises']`. Imbalance flags cover an upper- or lower-body gap, pressing vs pulling at 1.5 : 1 and quads vs hamstrings at 2 : 1, only once the larger side reaches six weekly sets, and each states the numbers it came from; per the retained rule on automated recommendations they advise and never block saving. Location equipment is free text, so plurals and a small alias table map it onto the catalog vocabulary and an unmatched item reads "not listed", never "missing". **Authenticated-browser verified 2026-09-13 in production**, read-only with every non-GET routine request aborted (none were attempted): the 20-exercise "Upper / Lower - Autumn Block" editor rendered sixteen muscle rows, 45–50 min days, no imbalances and a "Not listed at Home Gym" warning for five items; "UL 1" rendered the "No lower-body exercises" flag with its evidence. Both themes at 320/390/768/1024/1440 px had zero page overflow, blocks stayed within the 480 px data-cluster cap (246–414 px) and there were no console, page or failed Vercel/Railway requests. The production `ui:regression` sweep passed 269/283, including all 14 `routines-new` and 14 `routine-edit` layout cases; the 14 failures are the signed-out Login cases, where Login links to a `/forgot-password` route that does not exist and its prefetch logs a production 404 (the same pre-existing error recorded under `PROG-06`, now flagged as its own fix). |

| 2026-09-13 | EXER-05 | FE `lib/utils/exercise-alternatives.ts` (+ four tests), the shared `lib/utils/exercise-equipment.ts` (+ three tests, moved out of the ROUT-10 summary), and the change-exercise dropdown in `features/routines/wizard/WizardExerciseCard.tsx` / `ExerciseList.tsx`; no contract publish, backend change or migration; worktree `npm run verify` (230 tests, build), frontend CI run 34777846417 and Vercel `63d6bb1` green | Alternatives come only from `EXER-09` metadata: exercises sharing the target's substitution group rank first as near-identical, then exercises sharing its movement pattern and at least one primary muscle, each with a one-line reason. Equipment listed at the default training location reorders them and names what is not listed ("Machines not listed at Home Gym") but never hides one, and an empty equipment list counts as unknown. The routine wizard offers up to five at the top of an exercise card's change dropdown, skipping exercises already on that day; choosing one swaps the exercise and keeps its sets, rest, note and progression, and nothing is applied without that choice. The ranking is a pure function so `LIVE-11` can reuse it in the session. The pass also fixed the dropdown itself running 37 px off-screen at 320 px. **Authenticated-browser verified 2026-09-13 on the owner's local dev servers**, read-only with every non-GET routine request aborted (none were attempted): Bench Press in "Upper / Lower - Autumn Block" offered Push-ups first (bodyweight fits Home Gym's barbell, bench and rack), then Dumbbell Bench Press and Hammer Chest Press as near-identical and Incline Bench Press and Incline Dumbbell Press as same-movement, each naming what Home Gym does not list; choosing Push-ups swapped the card in wizard state only. Both themes at 320/390/768/1024/1440 px had zero page overflow, the panel stayed inside the viewport, and there were no console, page or failed requests. The local `ui:regression` sweep passed 283/283 just before the merge and its 56 routine cases again afterwards. Production was not browser-checked because the saved session now belongs to the local origin. |
| 2026-09-13 | FIX-11 | FE `app/(auth)/forgot-password`, `app/(auth)/reset-password`, shared `app/(auth)/components/AuthPageParts.tsx`, `lib/auth/password-reset.ts` and `schema/password-reset-schema.ts` (+ five tests), `requestPasswordReset`/`updatePassword` in `supabaseAuthService.ts` with their hooks, the callback's no-session branch and both pages added to the `ui:regression` signed-out routes; no contract publish, backend change or migration; worktree `npm run verify` (239 tests, build), frontend CI run 34780691677 and Vercel `9bf7b47` green | Login's "Forgot password?" pointed at a route that did not exist, so every production Login visit prefetched a 404 and logged a console error (the error recorded under `PROG-06` and the cause of the 14 failing production Login regression cases). The reset now uses Supabase recovery email: `/forgot-password` confirms without revealing whether the address has an account, and the link returns through the existing `/auth/callback` (the redirect shape Google sign-in already uses, so no new Supabase redirect-URL entry) to `/reset-password`, which sets the new password or explains that a link without a session cannot be used. An expired or already-used link now lands back on the request form with that explanation instead of an unexplained Login page. The pass also made the link keyboard-reachable (it had `tabIndex={-1}`) and bound Login's Email and Password labels to their inputs, which pointed at the icon wrapper. **Browser-verified 2026-09-13 on the owner's local dev servers and signed out in production**, with Supabase's recover and user-update writes intercepted so no email was sent and no password changed: the Login link is keyboard-reachable and its labels are bound; an invalid address never reaches Supabase; a mocked send shows the "If an account exists" confirmation and a mocked 429 the rate-limit explanation; an expired-link callback lands on the request form with its notice; `/reset-password` without a session offers a new link and, signed in locally, blocks mismatched passwords and explains a refusal. Both pages had zero overflow and no console errors in both themes at 320/390/768/1024/1440 px, and production Login now loads with no console errors, so the 404 is gone. The local `ui:regression` signed-out cases passed 56/56 including both new pages; the production sweep could not start because the saved session belongs to the local origin. The real email round trip is left to the owner. |
| 2026-09-13 | PROG-08, PREF-02 | CT `@sunsteel/contracts@0.27.0` goal kinds, directions, inputs and progress response; BE `MeasurableGoal` model plus `20260913213000_measurable_goals`, private `GET/PUT /users/preferences/goals` and bounded `GET /workouts/progress/goals` with five focused tests; FE Settings editor, Progress goal list and four utility/API assertions; isolated full repository gates, all three CI pipelines and Railway/Vercel deployments green | Owners can store at most eight targets: one each for weekly sessions, weekly external-load volume, streak and current body weight, plus one estimated-1RM target per exercise. Kilograms are canonical and Settings converts at the input boundary; the backend evaluates current-week goals in the requested time zone and reads streak, strength and body weight from their authoritative bounded sources. At-most body-weight progress exposes current, target and exact gap without inventing a misleading percentage. **Authenticated-browser verified 2026-09-13 in production:** the empty state rendered, a weekly target of four sessions saved, Progress reported the real ten sessions and "Target reached", and deleting the goal restored `0/8`; the account was left as found. The deployed desktop goal row and end-to-end writes were checked without console errors. A full production responsive sweep of the populated row remains unverified because the saved Playwright session is scoped to localhost and production signed it out; the earlier authenticated dev Progress sweep passed 14/14 before the new row was added. |

| 2026-09-14 | LIVE-11 | CT `@sunsteel/contracts@0.28.0` `SessionExerciseSubstitution`, `WorkoutSession.exerciseSubstitutions` and the substitute request/response; BE migration `20260914090000_session_exercise_substitutions`, `workout-session-substitution.service.ts` with `PUT`/`DELETE /workouts/sessions/:id/exercises/:routineExerciseId/substitution`, `session-substitutions.ts` and twelve tests; FE `exercise-swap-dialog.tsx`, `session-substitutions.ts` (+ four tests), the session page, exercise row and history overlay; contracts publish `0da6502`, backend `1b627f8`/`30b3867` with CI run 34824965809 (checks and analytics-integration) and Railway green, frontend `4332c4a`/`9bffecd`/`f5020ee` with CI run 34826699878 and Vercel `f5020ee` green; backend 121 and frontend 243 tests | A swap is a session fact beside the immutable `DATA-04` snapshot, stored as JSON on the session with the substitute's muscles captured at the swap. The server refuses a swap once the slot has a completed set, clears the slot's incomplete drafts (their values belonged to the other exercise) and can also update the routine for future sessions (`applyToRoutine`, reporting whether the routine still had the slot). Set logging accepts the substitute; finish progression never advances a swapped slot; analytics muscle and record attribution (including rebuilds), session comparison and the session response all resolve the slot to the exercise performed, while recap and volume already read names from the logged set. The live screen shows the substitute with a "Swapped from" caption, keeps rep targets but drops the prescribed load, and shows "Last time" only for the same exercise; history shows the same caption. Suggestions reuse the `EXER-05` ranking against the routine's own exercise, plus a catalog search. **Browser-verified 2026-09-14 on the owner's local dev servers against a mocked in-progress session**: the swap, revert and set-log writes were answered in the browser, so no real session or set was written, while the routine ("Upper / Lower - Autumn Block"), catalog and gym were the owner's. Swapping Bench Press offered Push-ups first, then Dumbbell Bench Press, Hammer Chest Press, Incline Bench Press and Incline Dumbbell Press with their reasons; the request named the slot and exercise with `applyToRoutine` off; the row then showed Push-ups with "Swapped from Bench Press", hid "Last time" and left the weight empty; a completed set was logged against Push-ups and a further swap was refused; Switch back restored Bench Press and search reached the catalog. The first pass caught the planned load carrying into the swapped set inputs; `f5020ee` keys them by the performed exercise and the rerun passed. Both themes at 320/390/768/1024/1440 px had zero page overflow, the dialog stayed inside the viewport and there were no console errors; the local `ui:regression` workouts and history cases passed 42/42. Production was not browser-checked because the saved session belongs to the local origin; a real end-to-end swap is left to the owner. |
| 2026-09-14 | ACH-01, DATA-02 | CT `@sunsteel/contracts@0.29.0` achievement catalog, response and `STREAK_MILESTONE`/`ACHIEVEMENT_UNLOCKED` payloads; BE nullable event session, migration `20260914103000_milestone_achievements`, `achievement-events.ts`, protected `GET /achievements` and four focused tests; FE protected `/achievements` ledger, service/hook and four presentation tests; contracts CI 34826055556, backend CI 34826830681, frontend CI 34828839264, Railway route/migration and Vercel production verified; backend 125 tests, frontend 247 tests | The fixed catalog has 25 thresholds across sessions, completed sets, external-load volume, current personal-record exercises and best streak days. The analytics writer awards every newly crossed threshold with a unique event key, so finish retries and rebuilds cannot duplicate it; streak thresholds also emit their own addressable event family. The first owner read reconciles existing READY projections and record counts into bounded immutable events marked `backfilled`, with no invented source session or earned date. The UI therefore says "Recognized from history" for prior work and links future unlocks to their source session. **Authenticated-browser verified 2026-09-14 in production:** the owner read returned 200 and displayed 19 earned milestones across all five categories, strongest threshold first, with no console errors or page overflow in either theme at 320/375/390/430/768/1024/1280/1440 px. The full production `ui:regression` passed 324/325 initially; the sole saved-token refresh race passed on its exact retry, and all 14 Achievements cases passed in the original run. No session, preference or goal data was changed. |
| 2026-09-14 | EXER-02 | FE protected `/exercises` (`app/(protected)/exercises/page.tsx`), `features/exercises/exercise-catalog.tsx` and `use-catalog-filters.ts`, pure `lib/utils/exercise-catalog.ts` (+ fifteen tests), `useTrainedExercises` over the existing performance read, the shared `components/ui/native-select.tsx` (also used by workout history), the enabled sidebar entry, middleware prefix and both `/exercises` routes in the `ui:regression` sweep; no contract publish, backend change or migration; worktree `npm run verify` (262 tests, build), frontend `b7ee94d`/`301dc7a`/`ed8dc03` with CI run 34851160071 and Vercel `ed8dc03` green | The first Exercises page lists the 80-exercise catalog as a ruled ledger (name, primary and secondary muscles, movement and mechanic, required equipment and, for exercises the owner has trained, the last date) and filters it by name, muscle (primary matches first), one equipment item or everything listed at the default training location, movement pattern and "Trained by me". Trained exercises come from `/workouts/progress/performance`, which already returned every exercise with completed work regardless of its range, so no backend read was added. Filters live in the URL, survive a reload and reset from the sidebar link. Loading, catalog/history/location errors with retry, an empty catalog, no trained exercises, no results and a gym without listed equipment each have their own state. Instructions and media stay hidden until `EXER-03`/`EXER-04`; favorites were left out for `EXER-07` (see Exercise library). **Browser-verified 2026-09-14 on the owner's local dev servers**, read-only: both themes at 320/390/768/1024/1440 px had zero page overflow and no console or request errors for the full and filtered views; "press" gave 11 of 80, adding Triceps 10 and Trained by me 5; "Listed at Home Gym" gave 23 and Hinge within it 4; forced 500s, a hanging catalog and empty or unlisted locations rendered each state in both themes. The first pass caught Clear filters being undone by a search write composed from lagging search params (`301dc7a`); the rerun passed, and `ed8dc03` moved the writes to `history.replaceState` so no filter change costs an RSC request. The local `ui:regression` subset for the affected cases (new `exercises` and `exercises-filtered` layouts, history and history detail, navigation, sidebar, drawer, hover and keyboard focus) passed 94/94. In production, signed out, `/exercises` redirects to Login with `redirectTo=/exercises` and the route serves behind the session marker; the signed-in production view was not browser-checked because the saved session belongs to the local origin. |

| 2026-09-14 | ACH-02 | CT `@sunsteel/contracts@0.30.0` stable six-rank catalog and typed rank progress; BE `renaissance-ranks.ts`, active-week count in the existing protected `GET /achievements` response and one added focused test; FE `renaissance-rank.tsx`, presentation helpers and two tests; contract `9068ccf` with CI run 34850379851, backend `c5bcbb0` with CI run 34851110292 plus Railway green, frontend `b6d5c8a` with CI run 34852119532 plus Vercel green; backend 126 tests and the pre-rebase frontend worktree gate 249 tests/build, with the merged tree verified again by CI | The ladder is Initiate, Apprentice, Artisan, Maestro, Virtuoso and Laureate. A rank requires both completed sessions (participation) and distinct calendar weeks containing at least one completed session (consistency); neither absolute weight nor consecutive training days affects it, and both inputs are monotonic so a rank cannot be lost. The value is derived from the active analytics projection and existing weekly rollups on every achievement read, with no duplicated persisted rank, migration or backfill. The UI presents current evidence and exact remaining requirements for the next rank while leaving per-achievement progress to `ACH-04`. **Authenticated-browser verified 2026-09-14 in production:** the response returned 200 and placed the owner at Artisan from 48 completed sessions and 13 active weeks, with the session requirement for Maestro met and three active weeks remaining. Both themes at 320/375/390/430/768/1024/1280/1440 px had zero page overflow, console errors or failed requests; the complete production `ui:regression` passed 353/353, including all 14 Achievements layouts and keyboard focus checks. No account data was changed. |
| 2026-09-14 | EXER-01 | CT `@sunsteel/contracts@0.31.0` (`ProgressTimelineQuery.exerciseId`, `522a7ea`); BE `exerciseId` on the timeline DTO, payload filter and cursor check in `workout-progress-timeline.service.ts` plus one test (`6613189`, CI run 34857694540 with checks and analytics-integration green, production deployed); FE `/exercises/[id]` (`features/exercises/exercise-detail.tsx`, `lib/utils/exercise-detail.ts` + four tests), `useProgressTimeline`/`useExercisePerformanceHistory` options, heading/copy props on `ProgressTimeline` and `ExercisePerformanceHistory`, catalog and Progress links and two new `ui:regression` routes (`d4d72cb`, CI run 34862836664 and Vercel green); worktree `npm run verify` (backend 127 and frontend 268 tests, both builds); no migration | The page shows catalog facts (muscles, movement, equipment, last trained), every routine day that programs the exercise with its planned sets, the current best set and estimated 1RM with an all-time chart, recent performances with their sets, notes and progression, and a progression history filterable by records or prescription changes. An untrained exercise shows one honest empty state and requests no history; an id outside the catalog says so and links back. The timeline filter reads the exercise id both event families already store, so no backfill was needed. **Browser-verified 2026-09-14 on the owner's local dev servers**, read-only: a trained (Bench Press), an untrained (Cable Curl) and a missing exercise rendered in both themes at 320/390/768/1024/1440 px with zero overflow, one `h1` and no console or request errors; Bench Press listed four routines, a 678.5 kg × 10 best set with a 904.7 kg estimate, five performances (ten after Load earlier sessions) and six timeline events, all record changes, sent with `exerciseId`; catalog and Progress links opened the page. The local `ui:regression` subset for the affected cases (the new `exercise-detail` and `exercise-missing` routes, both Exercises layouts, Progress, navigation, sidebar, drawer, hover and keyboard focus) passed 108/108 across two runs, because the first was cut off after 28 when the session ended and the dev servers had to be restarted. The first browser pass found the frontend dev server serving empty pages after `npm install --no-save` in the owner's checkout; it was restarted with the owner's approval. Railway's GitHub deployment status stayed `in_progress`, so production was confirmed by an authenticated read-only timeline request with `exerciseId` returning 200 (the previous build rejects unknown query fields with 400). |
| 2026-09-14 | EXER-07 | CT `@sunsteel/contracts@0.33.0` (`StarredExercise`, `StarredExercisesResponse`, `STARRED_EXERCISES_MAX`, `1bf79f0`); BE `StarredExercise` model and migration `20260914160000_starred_exercises` (also in the analytics test database), `exercise-stars.service.ts` behind `GET /exercises/starred` and `PUT`/`DELETE /exercises/:id/star` plus four tests (`77f6d48`, CI run 34865815937 and Railway green, migration applied in production); FE `StarToggle`, `useStarredExercises`/`useToggleExerciseStar`, `lib/utils/exercise-picker.ts` (+ five tests), the catalog `starred` filter (+ two tests), the exercise-page star, grouped add and change pickers in the routine wizard and an `exercises-starred` `ui:regression` route (`d09f98d`, CI run 34869443484 and Vercel green); worktree `npm run verify` (backend 133 and frontend 274 tests, both builds) | Stars are private, idempotent and capped, and the name keeps them apart from the public `PROF-05` favorites. The catalog stars from each row and filters to starred exercises with its own empty and error states; the exercise page stars from beside its back link; both routine-wizard pickers list starred exercises, then up to five recently trained ones, then the rest, and a search shows plain matches. A failed star restores the previous list and says so. **Browser-verified 2026-09-14 on the owner's local dev servers**, with routine writes aborted: the owner started with no stars (the starred filter showed its empty state); starring Bench Press from its row and Deadlift from its page returned 200 and filled both glyphs; the filter then listed exactly those two; the add picker grouped Starred (Deadlift, Bench Press) and Other exercises, and once history loaded Recently trained (5); the change picker showed Alternatives, Starred, Recently trained and Other exercises; a search showed plain matches; unstarring both returned 200 and restored the empty list. Catalog, starred filter and exercise page had zero overflow and no console or request errors in both themes at 320/390/768/1024/1440 px. The local `ui:regression` subset for the affected cases (all Exercises layouts including the new `exercises-starred` route, the exercise page, every routine layout, dropdowns, navigation, sidebar, drawer, hover and keyboard focus) passed 171/171. The owner's backend needed `prisma generate` for the new model; the engine copy failed with EPERM while the server ran, but the generated client and schema updated and the local and production `GET /exercises/starred` both returned 200. |
| 2026-09-14 | PROG-09 | CT `@sunsteel/contracts@0.34.0` (`PLATEAU_*` thresholds, `ExercisePlateau`, `PlateausResponse`, `5991a94`); BE `workout-plateaus.service.ts` with the pure `evaluatePlateaus`, `GET /workouts/progress/plateaus` and six tests (`a4a7611`, CI run 34874917608 and Railway green); FE `features/progress/plateau-watch.tsx`, `lib/utils/plateaus.ts` (+ four tests), `usePlateaus`, the Progress section and the exercise-page note (`80db670`/`2f06787`, CI run 34876293229 and Vercel green); worktree `npm run verify` (backend 139 and frontend 279 tests, both builds); no migration | A plateau needs every threshold at once: at least four terminal sessions with completed loaded sets since the current best (the session that set it excluded, counted inside an 8-week window), a best at least three weeks old, and the lift trained in the last three weeks. A lift whose estimated 1RM rose without a new best-set record is never flagged. The read uses only the owner's `PersonalRecord` rows and the window's set logs. Rows name the lift (linking to its page), the sessions without a new best, the best set with its estimate and the closest set since ("matched that estimate" or a floored percent). Copy states numbers, never causes, and the styling is neutral. **Browser-verified 2026-09-14 on the owner's local dev servers**, read-only: the real response checked 19 lifts and flagged 10, all of them repeating their exact best (for example Bulgarian Split Squat, 6 sessions at 20 kg × 10 since 16 Jul 2026); that surfaced the "100% of that estimate" wording, replaced by "matched that estimate" in `2f06787`. Crafted rows passed in both themes at 320/390/768/1024/1440 px with zero overflow and no console errors, including a best older than the window. Empty, nothing-to-check, error and pending states were forced in the browser, and the exercise page repeated the note. The local `ui:regression` Progress and exercise-page layout cases passed 28/28. |
| 2026-09-14 | PREF-05 | CT `@sunsteel/contracts@0.35.0` (`PLATEAU_MIN_SESSIONS_MIN`/`MAX`, `PlateauPreferences`, `0996f9e`); BE `User.plateauMinSessions` with a 3–8 check constraint (migration `20260914210000_plateau_min_sessions`, also in `scripts/prepare-analytics-test-db.ts`), `PUT /users/preferences/plateaus`, `evaluatePlateaus` taking the account minimum, and two new tests (`56d3e45`, CI run 34883374441 and Railway green); FE sensitivity control in `features/progress/plateau-watch.tsx`, `useUpdatePlateauPreferences` and `lib/utils/plateaus.ts` options (+ one test) (`66fc2cc`, CI run 34885128915 and Vercel green); worktree `npm run verify` (backend 141 and frontend 280 tests, both builds) | The minimum is an account preference, not a device one, so both plateau surfaces agree everywhere; the window, best age and recent-training thresholds stay fixed. There is no separate read: the plateau response already reports the minimum in use. The mutation stays pending until the plateau list has refetched, so the control never shows a value the list was not computed with. Production answered an out-of-range PUT with 400 and the plateau read with the stored minimum before the frontend shipped. **Browser-verified 2026-09-14 on the owner's local dev servers**: one real round trip on the owner's account (4 → 5 → 4; 10 lifts flagged at 4, 6 at 5, restored to 4); the resting control in both themes at 320/390/768/1024/1440 px with zero overflow and no console errors; saving (buttons disabled, section busy) and a failed save after the two app-wide mutation retries ("Not saved. The list still uses 4 sessions.") forced in the browser. A harness artefact during those checks — Playwright forwarding a never-answered forced PUT when its page closed — briefly saved 6, then 7, on the owner's account; both were restored to 4 and re-read. `npm run ui:regression` for the progress and exercise-detail layouts (28 cases, 320–1440 px, both themes) passed. |
| 2026-09-14 | ROUT-11 | CT `@sunsteel/contracts@0.36.0` (`ROUTINE_SCHEDULE_MODES`, `ROUTINE_DAYS_MAX`, nullable `dayOfWeek`, day `name`, `nextRotationDayId`, `routineDayLabel`, `8a5a0b2`); BE migration `20260914230000_rotation_routines` (also in `scripts/prepare-analytics-test-db.ts`), `src/routines/routine-schedule.ts`, rotation reads and `routineDayName` across history responses, seven new tests (`0c3ce1f`, CI run 34888406815 and Railway green); FE `lib/utils/routine-schedule.ts`, `features/routines/wizard/utils/schedule.ts` (+ two test files), the schedule step, day names in the builder, routine card, detail page and dashboard (`1492bab`/`0112bb5`, CI run 34892536244 and Vercel green); worktree `npm run verify` (backend 148 and frontend 293 tests, both builds) | Approved design: per-routine mode, optional names in either mode, the next rotation day follows the last completed session and aborted sessions do not advance it. Existing routines stay weekly with unchanged weekdays; weekly days still start only on their weekday while rotation days start any day. The next day is resolved by the backend from one lookup per rotation routine, so every device agrees. Production answered an invalid rotation (a rotation day with a weekday) with 400 and listed existing routines as `WEEKLY` before the frontend shipped. **Browser-verified 2026-09-14 on the owner's local dev servers** without writing anything: a crafted rotation and a named weekly routine were added to the real routines response in the browser; the routine list, detail page, dashboard, schedule step and builder passed in both themes at 320/390/768/1024/1440 px with zero overflow and no console errors; the full wizard (preset, day name, exercises on every day, review) produced a `ROTATION` request with null weekdays and the names, answered in the browser; and editing the rotation loaded its mode and order, switching to weekly kept all three days. Disabled reorder arrows at the list ends took the primitive's sunk fill and read as selected, so they are hidden instead. `npm run ui:regression` for the dashboard, routines, new routine, routine detail and edit, session, history detail, Progress and exercise-page layouts (126 cases, 320–1440 px, both themes) passed. |
| 2026-09-14 | SCHED-01 | FE `app/(protected)/schedule/page.tsx`, `features/schedule/schedule-week-view.tsx`, `lib/utils/schedule-week.ts` (+ five tests), the Schedule sidebar entry, `/schedule` in the middleware and the regression sweep (`d332e69`, CI run 34897005224 and Vercel green); no backend, contract or migration; `npm run verify` | The owner delegated the three open rules on 2026-09-14 and they were decided as recorded in revision 62. The week reads the cached routines, the sessions list for the week (filtered by end, so one extra day is read and sessions are placed by their start) and the active session. **Browser-verified 2026-09-14 on the owner's local dev servers**, read-only: this, last and next week on real data (12 planned; 10 completed, 4 ended early and 6 not logged last week), a crafted rotation note, pending and error states, both themes at 320/390/768/1024/1440 px with zero overflow and no console errors; the only non-GET request was the sign-in verification. During verification a frontend `npm run verify` ran in the owner's main checkout instead of the worktree and its production build broke the running dev server (500 on every route); it was restarted and answered normally again, and verify was re-run in the worktree. `npm run ui:regression` for the schedule and dashboard layouts (28 cases, 320–1440 px, both themes) passed. |
| 2026-09-14 | SCHED-03 | FE `scheduleEntryAction`/`rotationStartAction` in `lib/utils/schedule-week.ts` (+ one test), Start/Resume in `features/schedule/schedule-week-view.tsx` and the start flow in `app/(protected)/schedule/page.tsx` (`6feac58`, CI run 34900603984 and Vercel green); worktree `npm run verify` (299 tests, build) | No new rules: weekly days start only on their weekday and rotation days any day, as on the routine card and dashboard. The schedule hides Start while a session is live because the start flow would resume that session instead of the one clicked. **Browser-verified 2026-09-14 on the owner's local dev servers** without writing anything: the start request was answered in the browser and every other write would have been aborted (none occurred). This week offered Start on the owner's three Monday plans and a crafted rotation's next day, last and next week offered none; starting a plan and the rotation sent their routine and day and opened the session route; a crafted live session showed only Resume, linking to it; both themes at 320/390/768/1024/1440 px with zero overflow and no console errors. The owner's local backend had exited during verification; it was restarted in a new window, and the saved sign-in, whose session cookie the app had cleared while the backend was down, was restored through the login page without re-entering credentials. `npm run ui:regression` for the schedule and dashboard layouts (28 cases, 320–1440 px, both themes) passed. |
| 2026-09-15 | SCHED-07 | CT `@sunsteel/contracts@0.37.0` (`Routine.restDays`, `69755f0`); BE migration `20260915090000_routine_rest_days` (also in `scripts/prepare-analytics-test-db.ts`), `normalizeRestDays` in `src/routines/routine-schedule.ts` and two new tests (`867fc00`, CI run 34905294907 and Railway green); FE `toggleRestDay`, the rest-day group in the schedule step, rest entries in `buildScheduleWeek` and the routine card line (+ three tests) (`11f9368`/`c3e98ff`, CI run 34906618833 and Vercel green); worktree `npm run verify` (backend 150 and frontend 301 tests, both builds) | Rules delegated by the owner and recorded in revision 66. Production listed existing routines with empty `restDays` before the frontend shipped. **Browser-verified 2026-09-15 on the owner's local dev servers** without writing anything (a crafted weekly routine with rest days added to the real routines response; the wizard POST captured and answered in the browser; no other write occurred): past, today's and future rest days read "Rest day" and never "not logged", the routine card listed them, the schedule step offered only non-training weekdays and dropped a rest day that became a training day, rotation mode hid the group, and the request carried `restDays: [0, 6]`; both themes at 320/390/768/1024/1440 px with zero overflow and no console errors. `npm run ui:regression` for the schedule, routines, new routine, routine detail and edit, and dashboard layouts (84 cases, 320–1440 px, both themes) passed. |
| 2026-09-15 | ACH-04 | CT `@sunsteel/contracts@0.32.0` `AchievementCategoryProgress` and `AchievementsResponse.milestoneProgress` (`73ee32f`, CI run 34861874205, npm publish verified); BE next-threshold derivation from the existing authoritative achievement totals plus two focused cases (`56d9ace`, CI run 34863115095 and Railway green), followed by batched idempotent historical-event reconciliation after the local sweep exposed the old sequential writer's five-second timeout (`7357124`/`e437589`, CI runs 34889000662/34889122529 and Railway green); FE `milestone-progress.tsx`, presentation helpers and four tests (`149cc3a`, CI run 34884727804 and Vercel green); isolated full repository gates (backend 141 and frontend 283 tests, both builds), then lint, types and 301 tests on the current closure rebase; no migration or one-off backfill | The existing read now returns the first unearned fixed milestone for each category and marks a category complete after its catalog is exhausted. The UI keeps the five-category catalog order, prints exact current/target evidence, omits percentages and deadlines, and treats best streak as historical evidence rather than a live consecutive-day demand. Volume and streak guidance explicitly defer to planned loads and recovery. **Authenticated-browser verified 2026-09-14 in production, read-only:** the owner saw 48/50 sessions, 553/1,000 sets, all volume milestones complete, 22/25 record exercises and best 16/next 20 training days. Both themes at 320/375/390/430/768/1024/1280/1440 px had one page heading, five milestone rows, zero page/section overflow, zero clipped text and no console warnings or errors. Two authenticated development API reads then returned 200 with nineteen earned and five progress rows after the batched reconciliation fix; the first reconciled the already-earned immutable achievement events from existing history as designed. The complete local `ui:regression` coverage passed 395/395 across recovery runs: 280 before the timeout, 113/115 after the fix, then both exact infrastructure/concurrency-affected cases in the isolated stack. No layout assertion failed, and no account, workout, profile or preference data was changed. |
| 2026-09-15 | PROG-05, SCHED-02 | FE `lib/utils/schedule-month.ts` (+ three tests), `features/schedule/schedule-month-view.tsx`, the shared `schedule-status.ts` and `use-schedule-data.ts`, the Week/Month toggle in `app/(protected)/schedule/page.tsx` and `features/progress/consistency-calendar.tsx` on Progress (`af7dd3c`, CI run 34978178578 and Vercel green); no backend, contract or migration; worktree `npm run verify` (304 tests, build) | No new rule, as recorded in revision 68: each cell is the week builder's day, and its one mark is the strongest of its entries (completed, in progress, ended early, not logged, planned, rest day). Totals count only the month's own days, and trained days only up to today. Both surfaces share one data hook and one view; the Progress cells are not interactive and the section links to the Schedule. **Browser-verified 2026-09-15 on the owner's local dev servers**, read-only (a crafted weekly routine with rest days added to the real routines response; no write occurred): this month (14 completed, 4 ended early, 29 planned, 17 not logged and 8 rest days; trained on 8 of 15 days so far), last month (14 completed, 38 not logged, 10 rest days; 14 of 31 days), a chosen day opening its week, the month read spanning the grid plus one day, and the Progress section; both themes at 320/390/768/1024/1440 px with zero overflow and no console errors. The owner renewed the expired saved sign-in, and the owner's stopped frontend dev server was restarted in a new window. Port 4000 was held by another agent's `ACH-04` backend whose installed contracts predated `routineDayLabel`, so sessions reads containing routine sessions answered 500 there; verification and `ui:regression` used a detached main-branch backend on port 4002, with the browser mapping port 4000 to it. `npm run ui:regression` for the schedule, progress and dashboard layouts (42 cases, 320–1440 px, both themes) passed. |
| 2026-09-15 | ROUT-08 | CT `@sunsteel/contracts@0.38.0` (`RoutineVersion`, `RoutineVersionSetup`, `ROUTINE_VERSIONS_MAX`, `ffe6c5c`); BE `RoutineVersion` and `Routine.lastVersionNumber` (migration `20260915150000_routine_versions`, also in `scripts/prepare-analytics-test-db.ts`), `src/routines/routine-versions.ts` and `routine-versions.service.ts` behind `GET`/`POST /routines/:id/versions`, `POST …/:versionId/restore` and `DELETE …/:versionId`, restores through the extracted `RoutinesService.updateInTransaction`, seven new tests (`ed593e6`, CI run 34983782931 with checks and analytics-integration green, Railway green); FE `lib/utils/routine-versions.ts` (+ five tests), `useRoutineVersions`, `features/routines/components/RoutineVersions.tsx` on the routine detail page (`ecdc937`/`9e30e40`, CI run 34985616170 and Vercel green); worktree `npm run verify` (backend 157 and frontend 309 tests, both builds) | Rules delegated by the owner and recorded in revision 71. The backend tests caught a version number reused after deleting the latest version; a per-routine counter fixed it before shipping. **Browser-verified 2026-09-15 on the owner's local dev servers** without writing anything to the account. The real endpoints, served by a main-branch backend on port 4002 because port 4000 was still held by another agent's backend, listed no versions for the owner's routine and refused an over-long name and an unknown field (400) and a missing version or routine (404) before any write. Every versions request in the UI was then answered in the browser, over crafted versions of the owner's "Upper / Lower - Autumn Block": the list (a named saved version and an automatic before-restore one); a comparison naming a load change, a rest change and a removed exercise; restore (request captured, toast naming the version it saved); an identical version with restore disabled; saving a named version; deleting through the confirmation; and the empty, full (20, saving disabled), error (retry) and loading states. Both themes at 320/390/768/1024/1440 px had zero overflow, the dialog inset 16 px on phones and no console errors. The first pass showed the identical-version sentence twice and a lone Delete wrapping at 390; both were fixed in `9e30e40`. `npm run ui:regression` for the routines, routine detail and edit layouts, dialogs and keyboard focus (70 cases, 320–1440 px, both themes) passed. |
| 2026-09-15 | SCHED-06 | CT `@sunsteel/contracts@0.39.0` (`Routine.rotationWeekdays`, the request field and `RoutineVersionSetup.rotationWeekdays`, `6f02c87`); BE migration `20260915170000_rotation_weekdays` (also in `scripts/prepare-analytics-test-db.ts`), `normalizeRotationWeekdays`, create/update and version capture/restore, two new tests (`954c708`, CI run 34990524229 with checks and analytics-integration green, Railway green); FE the rotation plan in `buildScheduleWeek`, today's sessions read by `useScheduleData`, the builder's training-weekday group and carry-over, the dashboard filter and the card and version schedule line (+ two test files, six tests) (`7cc925d`, CI run 34992026625 and Vercel green); worktree `npm run verify` (backend 159 and frontend 315 tests, both builds) | The owner chose this scope for `SCHED-06` and delegated the details, recorded in revision 73. Today's sessions decide where the plan starts, so the schedule reads sessions from today even for a later week or month; the routine card keeps "Next in rotation" without a date and names the weekdays in its schedule line, because it has no session data to know whether today's slot was used. **Browser-verified 2026-09-15 on the owner's local dev servers** without writing anything: the real routines (served by a branch backend on port 4002, since port 4000 was held by another agent's backend) all carried `rotationWeekdays: []`, and a PATCH giving a weekly routine weekdays was refused with 400, leaving it unchanged. Two crafted rotations were added to the routines response: one on yesterday, tomorrow and three days out showed yesterday "Not logged", Pull tomorrow and Legs after it, and was absent from the dashboard; one training today showed its planned day with Start on the schedule and was listed on the dashboard. The routine card read "Rotation · Push · Pull · Legs · Mon, Wed, Fri". In the builder a weekly Push/Pull/Legs split turned into a rotation on Monday, Wednesday and Friday, the toggles and review agreed, and the captured request carried `rotationWeekdays: [1, 3, 5]`. The schedule and the builder's weekday group had zero overflow and no console errors in both themes at 320/390/768/1024/1440 px. `npm run ui:regression` for the schedule, dashboard and routine layouts (84 cases, 320–1440 px, both themes) passed. |
| 2026-09-15 | SCHED-04 | CT `@sunsteel/contracts@0.40.0` (`ScheduleOverride`, `MoveOccurrenceRequest`, `SCHEDULE_MOVE_MAX_DAYS`, `39ebe47`); BE `ScheduleOverride` and migration `20260915190000_schedule_overrides` (also in `scripts/prepare-analytics-test-db.ts`), `src/schedule/` with the pure `assertMovable` behind `GET /schedule/overrides`, `PUT /schedule/overrides/move` and `DELETE /schedule/overrides/:id`, three new tests (`8562aa2`, CI run 34995659586 with checks and analytics-integration green, Railway green); FE overrides in `buildScheduleWeek` and the month, `scheduleMoveAction`/`moveTargets`, `useScheduleOverrides`, the move dialog, Move and Undo on the week and the dashboard filter (+ six tests) (`04376cf`/`6800ed6`, CI run 34997560775 and Vercel green); worktree `npm run verify` (backend 162 and frontend 321 tests, both builds) | Rules delegated by the owner and recorded in revision 75. The override is kept by calendar date because routine edits replace the days; the module is `ScheduleOverridesModule` because `ScheduleModule` is `@nestjs/schedule`'s cron module. **Browser-verified 2026-09-15 on the owner's local dev servers** without writing anything to the account. The real endpoints (a branch backend on port 4002, since port 4000 was held by another agent's backend) listed no overrides and refused a 150-day range, a malformed date, an unknown routine, a date the routine does not train on and undoing a missing override, all before writing; every override request in the UI was then answered in the browser. On a crafted weekly routine the dialog offered only free, non-past days within six days (never the routine's other training day); moving tomorrow's workout to today showed "Moved to" on the planned day and the workout, marked as moved from it, with Start today; reopening offered putting it back, and Undo restored the plan. The month marked the moved day and its legend gained Moved, and the dashboard listed the workout moved onto today. The first pass showed the day the workout already sat on as a disabled option that read as selected; it is left out since `6800ed6`. The week and the dialog had zero overflow, the dialog inset 16 px on phones, and no console errors in both themes at 320/390/768/1024/1440 px. `npm run ui:regression` for the schedule, dashboard and Progress layouts, dialogs and keyboard focus (70 cases, 320–1440 px, both themes) passed. |
| 2026-09-15 | SCHED-05 | CT `@sunsteel/contracts@0.41.0` (kind `SKIP`, `SkipOccurrenceRequest`, `SCHEDULE_SKIP_PAST_DAYS`, `14e58c2`); BE enum value `SKIP` and migration `20260915210000_schedule_skip` (also in `scripts/prepare-analytics-test-db.ts`), `PUT /schedule/overrides/skip` with the pure `assertSkippable`, moves onto skipped days, two new tests (`e43e363`, CI run 35006613587 with checks and analytics-integration green, Railway green); FE `SKIPPED` entries in `buildScheduleWeek` and the month, the skip action and `postponeTarget`, `useSkipOccurrence`, the Reschedule dialog with Postpone and Skip, Mark skipped and Undo on the week and the dashboard filter (+ four tests) (`e70cea3`, CI run 35010802400 and Vercel green); worktree `npm run verify` (backend 164 and frontend 325 tests, both builds) | Rules delegated by the owner and recorded in revision 77. **Browser-verified 2026-09-15 on the owner's local dev servers** without writing anything to the account. The real endpoints (a branch backend on port 4002) refused a malformed date, an unknown routine, a date the routine does not train on and a planned date nine days back ("up to 6 days back"), all before writing; every override request in the UI was then answered in the browser. On a crafted weekly routine, yesterday's not-logged day was marked skipped (the week total gained "1 skipped") and undone; tomorrow's workout was postponed to the next free day, reopened (Postpone then offered the next free day past the routine's other training day), skipped from the dialog, which skipped the occurrence and cleared the day it had moved to, and undone. With a later day skipped, the dialog offered it as a free day; the month marked skipped days and its legend gained Skipped, and the dashboard dropped the workout skipped for today but listed it without the skip. The week and the dialog had zero overflow, the dialog inset 16 px on phones, and no console errors in both themes at 320/390/768/1024/1440 px. The full `npm run ui:regression` sweep (409 cases: every layout, dialogs, menus, hover and keyboard focus, 320–1440 px, both themes) passed. |

| 2026-09-15 | ACH-05 | [ACH-05 closure](../history/ach05-closure-2026-09-15.md); CT `@sunsteel/contracts@0.42.0` (`3c30956`, CI 35008072872); BE bounded completed-event derivation (`59125d7`/`63da005`, CI 35009430065 and 35011591180, Railway green); FE comeback ledger and evidence helper (`d1acabb`, CI 35011944416, Vercel green); isolated full gates (backend 166 and frontend 326 tests, both builds) | Fourteen full inactive local-calendar days followed by three distinct active dates inside an inclusive 14-day return window record one comeback; same-day sessions count once. The read scans 500 completed events, reports truncation and returns at most 20 newest recognitions. A targeted authenticated local smoke passed both themes at 320/768/1024/1440 px with no overflow or console errors. The full regression could not use the isolated `localhost:3001` origin because backend CORS allows the owner's `localhost:3000`, and the localhost session did not transfer to the Vercel-protected production origin; the production account's real comeback state remains unobserved. No account data was changed. |
| 2026-09-15 | NOTIF-01 | CT `@sunsteel/contracts@0.43.0` (`AppNotification`, `NotificationsResponse`, the mark-read request and response, `NOTIFICATIONS_LOOKBACK_DAYS`/`RETENTION_DAYS`/`LIST_LIMIT`, `24a1824`); BE `Notification` and migration `20260915230000_notifications` (also in `scripts/prepare-analytics-test-db.ts`), `src/notifications/` with the pure `gatherNotifications` behind `GET /notifications` and `POST /notifications/read`, four new tests (`004c914`, CI run 35014364039 with checks and analytics-integration green, Railway green); FE `useNotifications`/`useMarkNotificationsRead`, the pure `describeNotification`/`markReadInCache`, the top-bar bell, the Notifications page with Today and Updates (+ three tests) (`8de6151`, CI run 35017381392 and Vercel green); worktree `npm run verify` (backend 170 and frontend 328 tests before the rebase onto `ACH-05`, 329 after it, both builds) | Rules delegated by the owner and recorded in revision 79. Notifications are gathered on read, keyed by source, so no write path elsewhere changed and `ACH-05`, in flight at the same time, was untouched. **Browser-verified 2026-09-15 on the owner's local dev servers.** The real endpoint (a branch backend on port 4002) gathered the owner's last 30 days, 11 session notes and 4 new followers, as the shipped app does on first read, and refused a malformed id and an unknown field before writing; every other notifications request in the UI was answered in the browser, so read state was never changed. With a crafted list the bell read "Notifications, 2 unread" with the dotted glyph; the page showed Today (a crafted routine planned today with Start enabled) and Updates with two "New" rows; Mark all read sent one request, cleared the labels and the bell and disabled itself; opening a follower row marked only that one read and opened the profile. The empty state, and the error state with Retry once the retries ran out, rendered. The page and the top bar had zero overflow and no console errors in both themes at 320/390/768/1024/1440 px. The full `npm run ui:regression` sweep (409 cases: every layout, dialogs, menus, hover and keyboard focus, 320–1440 px, both themes) passed. |

## Document history

- **2026-09-06:** Created the canonical product registry from a review of the
  frontend, backend, shared contracts, existing technical-debt roadmap, and the
  combined feature brainstorm.
- **2026-09-06 (revision 2):** Corrected `DATA-01`-`DATA-05` to `IN_PROGRESS`
  against the verified analytics implementation and recorded its rollout gates;
  reordered the active queue so it describes real state; split the integrity
  section by size and reassigned the feature-sized parts of `FIX-04` and
  `FIX-06` to `LIVE-06` and `SOC-02`; clarified that `FIX-03` is a presentation
  concern because canonical storage is already kilograms; and gave the two
  recurring hidden blockers their own IDs (`EXER-09`, `NOTIF-08`). Other
  unowned dependency-column items -- schedule-instance model, moderation
  tooling, content authoring, accessibility audit, data inventory -- still lack
  IDs.
- **2026-09-06 (revision 3):** Added a structural `Size` column to every feature
  table and to the active queue, with a legend defining size by reach rather
  than duration. Translated [technical-debt.md](technical-debt.md) to English so
  both active registers match the language of the code and the four
  `CLAUDE.md`/`AGENTS.md` files, and repointed the `TD-27` link at a stable
  anchor. The July 2026 audit in `docs/history/` stays in Spanish as frozen
  evidence.
- **2026-09-06 (revision 4):** Deferred `FIX-05` instead of shipping an interim
  share fix, after confirming that `/profile` sits behind the middleware and a
  shared link would strand signed-out visitors on `/login`. Added `PROF-12`
  (public profile route and sharing) to own the real capability, added it to
  queue group 5 and to `PROF-11`'s dependencies, and recorded the general rule
  in the retained decisions. The honesty group is now six frontend-only fixes:
  `FIX-01`, `FIX-02`, `FIX-04`, `FIX-06`, `FIX-07`, `FIX-08`.
- **2026-09-07:** Recorded the Quick Workout decision: `FIX-04` hides the entry
  point rather than seeding it, and `LIVE-06` moves to `BLOCKED` because its
  replacement flow is not designed yet. Noted the template-selection direction
  and its overlap with `ROUT-03`, whose `Curated templates` dependency still has
  no owning ID.
- **2026-09-08:** Added `LIVE-19` (discard an in-progress session), found while
  restoring account state during the UI-restyle baseline capture: a session
  started for a screenshot could not be abandoned through the UI, only finished.
  Filed as `S` because every layer below the missing control already exists and
  is already exercised by the stale-session dialog.
- **2026-09-08 (revision 2):** Added `FIX-09` and `FIX-10`, both spotted in the
  UI-restyle baseline screenshots and both out of scope for a restyle because
  they are formatting logic rather than styling. `FIX-09` is the more useful
  find: it is not a missing feature but two duration formatters coexisting and
  disagreeing, which is why the same magnitude renders differently on two
  screens.
  Queue group 2 had been marked complete in the meantime; it reopens with these
  two, which were never part of the shipped set.
- **2026-09-07 (revision 2):** Shipped the six frontend-only honesty fixes as a
  single change -- `FIX-01`, `FIX-02`, `FIX-04`, `FIX-06`, `FIX-07`, `FIX-08` --
  and moved them to `SHIPPED`. Queue group 2 now holds only `FIX-03`. Three
  decisions are worth carrying forward. `FIX-01` removed the invented biography
  and location but kept the About block with a one-line honest empty state,
  because the capability is wanted and `PROF-04` owns the real editable fields;
  it did not build an empty state elaborate enough for `PROF-04` to discard.
  `FIX-02` deleted the badge outright rather than conditioning it, because
  neither `UserProfile` nor `PublicUserProfile` carries a membership, plan or
  tier field to condition it on. `FIX-04` hid the entry point behind a
  `SHOW_QUICK_WORKOUT_ENTRY` flag instead of deleting the flow, so `LIVE-06`
  resumes from working code once its product decision is made.
- **2026-09-07 (revision 5):** Recorded the scope limit on `NOTIF-03` after the
  capability was asked for directly: the end-of-rest **alert** is reachable
  through `NOTIF-08` and `NOTIF-02`, but a **visible countdown on a locked**
  **screen** is not a web capability and no amount of work in this stack
  delivers it. The row previously said only "notify when rest ends", which was
  correct but left the impossible half unaddressed and available to be promised
  by mistake. Also noted that `LIVE-01`'s in-app tone cannot be made to fire
  while backgrounded, so it must not be filed as a bug against `LIVE-01`.
- **2026-09-07 (revision 4):** Closed the analytics foundation against verified
  production evidence. `DATA-01`, `DATA-03`, `DATA-04` and `DATA-05` moved to
  `SHIPPED` and queue group 1 is done; `TD-27` is closed in
  [technical-debt.md](technical-debt.md) with its own closure record. `DATA-02`
  was deliberately left `IN_PROGRESS` rather than swept into the group: the
  event stream exists but emits only two of its intended event types, and the
  closure record explicitly declines to claim progression, milestone or
  achievement events. Marking it `SHIPPED` would have made `LIVE-08`, `ACH-01`
  and `ACH-05` look unblocked when their dependency is still partial.
- **2026-09-07 (revision 3):** Traced `weightUnit` across both repositories while
  sequencing the work after the honesty group, and recorded the result on
  `FIX-03`: the kg/lb defect is latent rather than live because no screen can set
  the preference, and the conversion boundary must therefore land before the unit
  selector that `PROF-01` already flags as missing from Settings. No status
  changed; `FIX-03` stays `NEXT`.
- **2026-09-07 (revision 6):** Shipped `LIVE-03` across contracts, backend and
  frontend after DATA-04 made an exact, bounded previous-session read possible.
  The comparison is scoped to the same preserved routine-day identity and set
  identity rather than names or an arbitrary page of history. Also corrected
  `LIVE-07`'s dependency: persistent records exist, but neither set-log upserts
  nor session finish responses expose earned records, so live PR detection is
  cross-repository work requiring a published mutation-result contract.
- **2026-09-07 (revision 7):** Recorded the owner's end-to-end confirmation of
  `LIVE-03`. The browser evidence remains explicit about what was automated and
  avoids claiming a physical-device pass that was not identified.
- **2026-09-07 (revision 8):** Shipped `FIX-03` after preserving the required
  order: one canonical kilogram conversion boundary first, then the Settings
  selector for the already-supported `weightUnit`. Closed queue group 2 with no
  backend, contract or migration change, and recorded the authenticated-browser
  regression that led to the rounded-pound transition guard.
- **2026-09-07 (revision 9):** Added the owner's physical-phone confirmation for
  `FIX-03`; the unit preference and converted workout flow worked without a
  reported issue. The feature remains closed and queue group 2 remains complete.
- **2026-09-07 (revision 10):** Shipped `LIVE-10` across backend and frontend
  without changing the published contract. Removed the timer that silently
  aborted inactive sessions, made the reused start path refresh `lastActivityAt`,
  and added a global 48-hour recovery decision with Resume, finish saved work,
  and discard. The frontend fetches full session detail only after the compact
  active-session response identifies a stale candidate, so normal navigation
  pays no extra request. Queue group 4 now records `LIVE-10` as shipped while
  leaving `LIVE-07`, `LIVE-08` and their dependent recap work open.
- **2026-09-08 (revision 11):** Added authenticated browser evidence for
  `LIVE-10`. A temporary one-millisecond local threshold exposed the real
  recovery prompt without changing stored timestamps; it reported 5/12 saved
  sets, and Resume exercised the backend heartbeat, dismissed the prompt,
  showed the success toast and navigated to the intact active session with no
  console errors. Restored the production threshold to 48 hours immediately.
  Finish and discard remain intentionally unexecuted against owner data.
- **2026-09-08 (revision 12):** Completed the remaining `LIVE-10` browser paths
  after the owner confirmed the authenticated account was test data. Finish
  saved work returned 200, retained the five completed sets in history and
  removed the session from the active slot. A newly started session with zero
  completed sets then exercised Discard: it returned 200, cleared the active slot and did not
  appear in completed history. No console errors were recorded, and the local
  threshold used to expose each prompt was restored to 48 hours.
- **2026-09-08 (revision 13):** Shipped `LIVE-07` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.9.0` so set-log mutations can
  expose the saved row and independently earned weight, rep, volume and
  estimated-1RM records. The live frontier includes completed history and the
  active session, while pre-mutation values suppress duplicate celebrations on
  identical or RPE-only autosaves. Recorded the authenticated browser pass and
  left `LIVE-08` as the remaining backend/event prerequisite for `LIVE-09`.
- **2026-09-08 (revision 14):** Recorded the final green CI runs for contracts,
  backend and frontend, including the frontend production build that was kept
  out of the live dev checkout. `LIVE-07` is fully closed.
- **2026-09-08 (revision 15):** Shipped `FIX-09` as a frontend-only correction.
  Removed the formatter that expressed long sessions as unbounded minutes and
  routed Dashboard, Workout History and session-detail metrics through the same
  hour-aware formatter as the active-session header. Added the 22-hour
  regression case, recorded the authenticated browser pass, the clean isolated
  production build and green frontend CI, and left `FIX-10` as the only open
  item in queue group 2.
- **2026-09-08 (revision 16):** Shipped `FIX-10` as a frontend-only correction.
  Centralised weekly day-count pluralisation, covered zero, singular and plural
  values with pure tests, and verified both the routine card and creation wizard
  in the authenticated browser. Queue group 2 is complete again; no backend,
  contract or migration work was required.
- **2026-09-08 (revision 17):** Shipped `LIVE-19` by exposing the already-
  supported `ABORTED` resolution on the active-session screen. Added a distinct
  destructive confirmation, mutation feedback and pure copy tests, then
  verified cancel and confirmed discard against the authenticated test account.
  The discarded session returned to Dashboard and no longer occupied the active
  workout slot. Queue group 3 now leaves `LIVE-05` waiting on `PREF-01` and
  `LIVE-06` blocked on its product decision.
- **2026-09-08 (revision 18):** Shipped `PREF-01` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.10.0`, deployed the normalized
  training-location migration and exposed an atomic owner-scoped replacement
  API. Settings now manages default location, bar, plate-pair inventory and
  equipment with the existing kg/lb boundary. The authenticated persistence and
  conversion pass unblocks `LIVE-05`; `LIVE-06` remains unchanged and blocked.
- **2026-09-08 (revision 19):** Shipped `LIVE-05` as the first active-session
  consumer of PREF-01. Weighted exercises now open an inventory-bounded plate
  calculator using the next incomplete target and default training location,
  with explicit exact, closest-below and bar-too-heavy outcomes. Added five pure
  combination tests, completed an authenticated narrow-viewport pass and
  discarded its temporary session. Queue group 3 now has only `LIVE-06`, which
  remains blocked on the owner's product decision.
- **2026-09-09 (revision 20):** Shipped `LIVE-08` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.11.0`, added the
  `PROGRESSION_CHANGED` event and made finish responses retry-stable. The
  active-session result now explains the exercise, rule, achieved reps and old
  to new load before returning to Dashboard. Pure tests also close the latent
  case where entered reps on an incomplete set advanced its prescription. The
  authenticated browser and deployed-API pass confirmed 40 to 42 kg dynamic
  progression without console errors. `DATA-02` remains partial only for its
  missing milestone and achievement event families; `LIVE-09` is now the next
  visible-workout-payoff item.
- **2026-09-09 (revision 21):** Shipped `LIVE-09` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.12.0` and added an owner-scoped
  recap read so a completed session can reconstruct its metrics, final record
  frontier, persisted progression events, notes and previous same-day
  comparison without relying on transient mutation responses. Finish now opens
  the recap before Dashboard and history renders the same payload. Isolated
  verification, all three CI pipelines, Railway, Vercel, the deployed API and
  the authenticated narrow-viewport finish/history flow passed. Queue group 4
  is complete; `DATA-02` remains partial only for future milestone and
  achievement event families, and `LIVE-06` remains unchanged and blocked.
- **2026-09-09 (revision 22):** Shipped `PROF-03` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.13.0`, deployed the required
  lowercase unique username migration and removed email from public search
  results. Settings now owns handle changes; name and exact `@username` search
  navigate to readable authenticated profile URLs, while UUID links remain
  compatible. Shared format and reserved-name constants keep client and server
  validation aligned, with database constraints as the final boundary. All
  repository gates, integration CI, Railway, Vercel and the authenticated
  production browser pass succeeded. Queue group 5 now advances to `PROF-06`;
  `PROF-12` still owns signed-out profile sharing.
- **2026-09-09 (revision 23):** Shipped `PROF-06` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.14.0`, deployed five private-default
  visibility columns and centralized owner/follower/public authorization before
  sensitive profile reads. Settings controls each domain independently; member
  profiles now render only allowed training summaries, records and body metrics,
  while routine and achievement rules remain stored for their owning features.
  All repository gates, PostgreSQL integration CI, Railway, Vercel and the
  authenticated production browser pass succeeded. The test account returned to
  all-private; `PROF-04` is the recommended next profile slice and `PROF-12`
  still owns signed-out profile sharing.
- **2026-09-09 (revision 24):** Shipped `PROF-04` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.15.0`, deployed nullable biography
  and location columns plus two private-default visibility columns, and added
  settings editors with shared 500/100-character limits. The authorized member
  read resolves each new field before querying it, omits denied values entirely,
  and the profile renders distinct real, empty and private states. Contracts,
  both applications, CI, Railway and Vercel passed. An authenticated production
  browser pass verified save, reload persistence, the own multi-line About view
  and redaction of an existing member's private fields without console errors;
  cleanup restored the test account. The roadmap size is corrected from `M` to
  `L` because the delivery required a schema migration. Queue group 5 advances
  to `PROF-05`; `PROF-12` still owns signed-out profile access and sharing.
- **2026-09-10 (revision 25):** Shipped `PROF-05` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.16.0`, deployed structured identity
  columns, an ordered exercise-catalog relation and an eighth private-default
  profile domain. Settings now edits bounded goals, experience, disciplines,
  preferred style and favorite exercises; member reads resolve authorization
  before querying or serializing the block. Contracts, both applications,
  PostgreSQL integration CI, Railway and Vercel passed. The authenticated
  production browser pass verified save, reload persistence, allowed and denied
  responses, desktop/mobile rendering, clean console/network state and account
  restoration. The roadmap size is corrected from `M` to `L` because the work
  required a schema migration. `PROF-09` is the next dependency-ready profile
  slice; `PROF-12` still owns signed-out profile access and sharing.
- **2026-09-10 (revision 26):** Shipped `PROF-09` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.17.0`, deployed independent
  discovery columns for name, username and future contact matching, and gated
  each current search path at the database query. Existing discovery defaults
  remain enabled; future contact matching is disabled and explicitly described
  as unavailable today. Contracts, both applications, CI, Railway and Vercel
  passed. The authenticated production browser pass verified save/reload
  persistence, direct profile access while both current discovery paths were
  hidden, mobile rendering, clean console state and account restoration. A
  second-account hidden-result pass was unavailable, so that behavior is backed
  by focused backend query tests. The roadmap size is corrected from `M` to `L`
  because the work required a schema migration. Queue group 5 advances to
  `PROF-12`, which owns signed-out profile access and truthful sharing.
- **2026-09-10 (revision 27):** Shipped `PROF-12` and closed `FIX-05` across
  backend and frontend without a contract publish or migration. Added an
  unguarded, no-store anonymous profile endpoint that reuses the centralized
  privacy resolver with no viewer, plus `/members/<username>` outside the
  protected shell and a resilient copy-link action with success/failure
  feedback. Backend/frontend gates, GitHub CI, Railway and Vercel passed. The
  production browser pass covered signed-out direct access, authenticated copy
  and reopen at 390 x 844, private-state rendering and clean page, console and
  network state. The test account remained all-private; mixed privacy is backed
  by focused backend assertions. Queue group 5's dependency-ready foundation is
  complete; its remaining items wait on achievements, routine sharing or a
  moderation model, so `PROG-01` is the recommended next slice.
- **2026-09-10 (revision 28):** Shipped `PROG-01` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.18.0`, deployed an owner-scoped
  strength-trend endpoint plus a partial expression index over record events,
  and activated Progress with lift/range selection, responsive summary cards,
  exact change rows and dependency-free SVG charts for best-set load and
  estimated 1RM. All repository gates, PostgreSQL integration CI, Railway and
  Vercel passed. The authenticated production browser pass covered 22 exercise
  options, range baselines, exercise switching, populated all-time charts,
  exact record detail, 390 px rendering and clean console/network state. This
  remains a record-frontier view; `PROG-02` is the recommended next slice for
  every session, set, RPE and note attached to an exercise.
- **2026-09-11 (revision 29):** Shipped `PROG-02` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.19.0`, deployed a paginated
  owner-scoped performance endpoint over completed-set and progression-event
  partial indexes, and extended Progress with snapshot-backed routine context,
  completed sets, RPE, session/prescription notes and exact progression changes.
  Completed and aborted sessions with real work remain visible; incomplete sets
  do not. All repository gates, PostgreSQL integration CI, Railway and Vercel
  passed. The authenticated production pass covered 24 exercises, a 10 + 2
  cursor boundary, exercise/range switching, every history section, 390 px
  rendering and clean console/network state. `PROG-03` is the recommended next
  dependency-ready progress slice.
- **2026-09-11 (revision 30):** Shipped `PROG-03` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.20.0` and exposed the existing
  `DATA-03` muscle rollups through a bounded, timezone-matched 4–12-week read;
  no schema migration or rebuild was required. Progress now shows all canonical
  muscles, honest zero cells, a partial-current-week marker, top-three summary
  and numeric intensity cells using the established primary 1.0 / secondary
  0.5 weighting. Repository gates, PostgreSQL integration CI, Railway and
  Vercel passed. Authenticated production checks covered all three ranges and
  390/768/1440 widths with contained table scrolling and clean console/network
  state. `PROG-04` is the recommended next dependency-ready progress slice.
- **2026-09-12 (revision 31):** Shipped `PROG-04` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.21.0` and added a bounded,
  timezone-matched volume endpoint that combines existing overall and muscle
  rollups with at most 12 weeks of completed-session summaries and set logs;
  no schema migration or rebuild was required. Progress now compares weekly
  external-load volume and completed work across the total, muscles,
  historical routines and exercises, while keeping the current partial week
  out of the completed-week delta. Repository gates, PostgreSQL integration
  CI, Railway and Vercel passed. Authenticated production checks covered all
  three ranges, all four breakdowns, selector changes and 390/768/1440 widths
  with contained chart scrolling and no new console warnings or errors. Two
  selector-control warnings found during the pass were corrected before
  closure. `PROG-06` is the recommended next dependency-ready progress slice;
  `PROG-05` still waits on the schedule-instance model owned by `SCHED-01`.
- **2026-09-13 (revision 32):** Added [In-flight work](#in-flight-work) so
  parallel agents working in separate worktrees can see each other's claims on
  `main`, and tightened step 4 of the usage rules to require a claim before
  implementation. Recorded the then-current claims: Codex on `PROG-06`, Claude on
  `SOC-02` with `DASH-02`, `DASH-10` and `TRUST-02` reserved as `NEXT`, chosen
  because none of them touches the Progress page, workout sessions, routine
  snapshots or routine-day identity that `PROG-06` reads. Closed `NAV-06` as
  already shipped by the sidebar `History` entry.
- **2026-09-13 (revision 33):** Shipped `PROG-06` across contracts, backend and
  frontend. Published `@sunsteel/contracts@0.22.0` and added a bounded,
  owner-scoped comparison endpoint over existing `DATA-04` snapshots; no
  migration or historical rebuild was required. Progress now selects any
  preserved routine day and compares its latest two completions across
  duration, unit-aware load volume, completed sets, notes and exact set rows,
  with explicit one-session, skipped-work and changed-exercise states. All
  repository gates, CI pipelines, Railway and Vercel passed. Authenticated
  production checks covered populated, switched and single-session routine
  days plus both themes at every required 320-1440 px width with no page
  overflow or browser errors; the 14-case automated Progress layout slice also
  passed. The broader sweep remains red on a pre-feature public Login resource 404. `PROG-07` is the recommended next dependency-ready progress slice;
  `PROG-05` still waits on the schedule-instance model owned by `SCHED-01`.
- **2026-09-13 (revision 34):** Reconciled the claim ledger with the shipped
  `PROG-06` delivery, released its completed claim and claimed `PROG-07` for
  Codex across contracts, backend and frontend before implementation.
- **2026-09-13 (revision 35):** Shipped `SOC-02`, `DASH-02`, `DASH-10` and
  `TRUST-02` and released their claims. `SOC-02` published
  `@sunsteel/contracts@0.24.0` after rebasing over `PROG-07`'s `0.23.0`, the
  serialized-version rule working as intended. Relationship lists are readable
  by any signed-in member and carry identity only; a per-profile relationship
  privacy setting would need a migration and is not scheduled. The three
  dashboard and privacy items are frontend-only. All four were verified
  together on a local integration build and a 283/283 regression sweep, then
  in an authenticated read-only production pass with no overflow, console or
  request errors.
- **2026-09-13 (revision 36):** Shipped `SOC-07` and released its claim.
  Owners share selected parts of a completed session through revocable,
  unguessable links; the signed-out page renders only those parts in the
  owner's unit and never the previous-session comparison. The item grew from
  `M` to `L` because revocation needs a persisted `SessionShare` model and
  migration; a stateless signed link was rejected because it could not be
  revoked.
- **2026-09-13 (revision 37):** Shipped `A11Y-01` and released its claim.
  "Account preference" was delivered as a per-device setting, because it has
  to take effect before first paint and before the account loads; an
  account-synced copy would need a contract field and is not scheduled.
- **2026-09-13 (revision 38):** Shipped `EXER-09` and released its claim.
  Catalog metadata (movement pattern, mechanic, required equipment,
  substitution group) is in production for all 80 exercises, which unblocks
  `EXER-02`, `EXER-05` and `ROUT-10`. `LIVE-11` still waits on `EXER-05`.
  Instruction and media fields exist but remain empty by design until
  `EXER-03`/`EXER-04`.
- **2026-09-13 (revision 39):** Shipped `ROUT-10` and released its claim.
  The quality summary is frontend-only and advisory: it computes weekly
  muscle sets, duration, equipment and imbalance flags from the planned
  routine and `EXER-09` metadata, states the numbers behind every flag and
  never blocks saving.
- **2026-09-13 (revision 40):** Shipped `PROG-07` across contracts, backend and
  frontend and released its claim. Published `@sunsteel/contracts@0.23.0` and
  added a stable, owner-scoped cursor timeline over the existing persisted
  record and progression events plus historical session snapshots, without a
  migration or backfill. Production verified the mixed and filtered feeds,
  older-page loading, record reasons, exact prescription changes and recap
  links in both themes at every required 320-1440 px boundary with no overflow
  or console errors. Historical records remain the persisted best-set frontier;
  the four transient LIVE-07 celebration categories are not invented after the
  fact. `PROG-08`, beginning with its `PREF-02` storage dependency, is the next
  dependency-ready Progress slice; `PROG-05` still waits on `SCHED-01`.
- **2026-09-13 (revision 41):** Completed the deferred authenticated dev
  regression for `PROG-07`: all 14 Progress layout cases passed across both
  themes and the required 320-1440 px widths after refreshing the saved login.
  Claimed `PROG-08` together with its `PREF-02` storage dependency for Codex;
  the goals work is isolated from Claude's frontend-only `EXER-05` wizard work.
- **2026-09-13 (revision 42):** Shipped `EXER-05` and released its claim.
  Alternatives are frontend-only and advisory: ranked from catalog
  metadata, reordered but never filtered by the equipment the default gym
  lists, and applied only when chosen. `LIVE-11` still needs `DATA-04`
  context for in-session substitution but no longer waits on suggestions.
- **2026-09-13 (revision 43):** Added and shipped `FIX-11`: the Login
  page linked to a Forgot password route that did not exist. The fix is a
  real Supabase password reset rather than removing the link, reusing the
  existing auth callback so no new redirect configuration is needed.
- **2026-09-14 (revision 44):** Shipped `PROG-08` and its `PREF-02` storage
  dependency across contracts, backend and frontend and released the claim.
  Private, unit-aware goals now cover weekly sessions and volume, active
  streaks, exercise estimated 1RM and current body weight through bounded
  authoritative reads. The production save/evaluate/delete flow passed and
  restored the test account; only the populated row's full responsive/theme
  matrix remains unverified because production cannot reuse the localhost-only
  Playwright session.
- **2026-09-14 (revision 45):** Shipped `LIVE-11` and released its claim.
  A swap is recorded on the session beside the immutable snapshot rather
  than rewriting it, so history stays truthful about both the prescription
  and what was performed. Swapped slots never trigger progression, and a
  swap is refused once a set of that slot is completed.
- **2026-09-14 (revision 46):** Shipped `ACH-01`, completed `DATA-02` and
  released the claim. Published `@sunsteel/contracts@0.29.0`, added the final
  streak/achievement event families plus idempotent live and historical award
  paths, and activated the protected Achievements ledger. Production showed
  19 verified milestones for the owner across all five categories in both
  themes at every required 320–1440 px boundary, with no overflow or console
  errors. The full production regression passed 324/325 on its first run: the
  first authenticated Dashboard case saw the saved token expire before its
  refresh and logged five 401s; that exact case passed immediately after the
  refreshed state was persisted. All 14 Achievements cases passed in the
  original run. `ACH-02` is the next dependency-ready achievement slice.
- **2026-09-14 (revision 47):** Claimed `EXER-02` for Claude. The first
  Exercises slice is frontend-first; backend or contracts change only if no
  existing read already lists the exercises the user has trained.
- **2026-09-14 (revision 48):** Shipped `EXER-02` and released its claim.
  Catalog browsing is frontend-only: the existing performance read already
  listed trained exercises, so no contract or backend change was needed. The
  favorites filter was left out and recorded as a retained decision: it
  belongs to `EXER-07`'s preference model, and `PROF-05` profile favorites are
  not a substitute. `ROUT-07` and `NAV-01` no longer wait on `EXER-02`;
  `EXER-01` (personal exercise page) is the natural next Exercises slice.
- **2026-09-14 (revision 49):** Claimed `EXER-01` for Claude. The personal
  exercise page starts from the existing strength-trend, performance-history
  and routine reads; backend or contracts change only if those fall short.
- **2026-09-14 (revision 50):** Shipped `ACH-02` and released its claim.
  Published `@sunsteel/contracts@0.30.0` and added six attendance-based ranks
  derived from completed sessions plus distinct active weeks. Production placed
  the owner at Artisan and the full 353-case regression passed. `ACH-04` is the
  next dependency-ready achievement slice; `ACH-03` still waits on `PROF-07`.
- **2026-09-14 (revision 51):** Claimed `ACH-04` for Codex. The existing
  achievement read already derives every authoritative category total, so this
  slice extends that response with one stable next milestone per category and
  needs no migration or backfill. The presentation will preserve catalog order
  and avoid deadlines, proximity rankings, load prescriptions or
  consecutive-day prompts.
- **2026-09-14 (revision 52):** Shipped `EXER-01` and released its claim.
  The page composes existing bounded reads; the only addition is an optional
  `exerciseId` on the progress timeline (`@sunsteel/contracts@0.31.0`, no
  migration), because a complete per-exercise progression history could not be
  assembled from paginated performance pages. With `EXER-01` and `EXER-02`
  shipped, the Exercises section covers browsing and per-exercise history;
  `EXER-07` (favorites and recents) is the next slice that needs a new model.
- **2026-09-14 (revision 53):** Claimed `EXER-07` for Claude and resized it
  from `M` to `L`: persisted per-user favorites need a new model and
  migration. Recents derive from the existing trained-exercise read. The
  claim covers the catalog, the exercise page and the routine wizard's
  pickers; Quick Workout stays hidden under `FIX-04`/`LIVE-06`.
- **2026-09-14 (revision 54):** Shipped `EXER-07` and released its claim.
  Favorites shipped as private stars in their own model, so they cannot be
  confused with the public `PROF-05` favorites; recents reuse the trained
  exercise list. With `EXER-01`, `EXER-02`, `EXER-05`, `EXER-07` and `EXER-09`
  shipped, the remaining Exercises items need content (`EXER-03`/`EXER-04`),
  an ownership model (`EXER-06`) or moderation (`EXER-08`). `PROG-09` is the
  next dependency-ready slice with no new model.
- **2026-09-14 (revision 55):** Follow-up to `EXER-07`: while training
  history loads, both routine-wizard pickers show the "Recently trained"
  group as a "Loading recent exercises…" status row instead of letting it
  appear unannounced and push the list down. Frontend only.
- **2026-09-14 (revision 56):** Claimed `PROG-09` for Claude. Plateaus will
  be derived from the persisted best-set frontier plus a bounded read of
  recent completed sessions, with every threshold stated beside the result;
  no migration is planned.
- **2026-09-14 (revision 57):** Shipped `PROG-09` and released its claim.
  Plateaus come from the persisted best-set frontier plus an 8-week window
  of completed loaded sets, with the thresholds exported from contracts and
  printed beside the result; no migration. The owner's real data flagged
  lifts repeated at exactly their best, which is the intended signal when a
  prescription is not progressing. `PROG-10` (fatigue indicators) is the
  natural follow-up but combines RPE and misses, so it needs its own rule
  review before it starts.
- **2026-09-14 (revision 58):** Added and claimed `PREF-05` for Claude at the
  owner's request: the plateau watch's minimum sessions without a new best
  becomes a per-account preference (default four, bounded), stored on the
  account so every device and both plateau surfaces agree. It needs a
  migration, hence `L`; the other `PROG-09` thresholds stay fixed.
- **2026-09-14 (revision 59):** Shipped `PREF-05` and released its claim.
  The plateau watch's minimum sessions is now an account preference (3–8,
  default four) chosen inside the section, backed by one new `User` column
  with a check constraint; the other `PROG-09` thresholds stay fixed.
  `PROG-10` (fatigue indicators) remains the natural follow-up and still
  needs its own rule review before it starts.
- **2026-09-14 (revision 60):** Claimed `ROUT-11` for Claude with the
  owner's approved design: each routine is either weekly (days tied to
  weekdays, as today) or a rotation (ordered days with no weekday); every day
  may carry its own name in either mode; a rotation's next day follows the
  last completed session of that routine, an aborted session does not
  advance it, and the dashboard shows the next rotation day instead of a
  weekday match. It changes the routine model, hence `L`, and unblocks
  `SCHED-01` and `SCHED-06`.
- **2026-09-14 (revision 61):** Shipped `ROUT-11` and released its claim.
  Routines are weekly or a rotation, days may be named, and a rotation offers
  its next day on any weekday from the backend's `nextRotationDayId`. This
  unblocks `SCHED-01` (weekly calendar) and `SCHED-06` (recurring and rotating
  plans); `SCHED-01` is the next dependency-ready schedule slice.
- **2026-09-14 (revision 62):** Claimed `SCHED-01` for Claude. The owner
  delegated its three open rules, decided as follows. A past weekly day with
  no session reads "Not logged" in neutral styling, never "missed", and only
  from the routine's creation date on; rotation days have no date, so they
  are never "not logged". Rest days are neither inferred nor stored: a day
  with nothing planned says so, and intentional recovery stays with
  `SCHED-07`. Rotations are not projected onto future dates; the week shows
  each rotation's next day as a note, and its sessions on the days they
  happened.
- **2026-09-14 (revision 63):** Shipped `SCHED-01` and released its claim.
  The Schedule page is live in the sidebar with the rules from revision 62.
  `SCHED-03` (start from calendar) and `SCHED-07` (rest-day planning) are
  now dependency-ready; `SCHED-02` also waits on the monthly consistency view
  it names.
- **2026-09-14 (revision 64):** Claimed `SCHED-03` for Claude. The
  Schedule page will start today's planned weekly days and each rotation's
  next day, and resume the live session, under the same start rules the
  routine card and dashboard already apply; frontend only.
- **2026-09-14 (revision 65):** Shipped `SCHED-03` and released its claim.
  The Schedule page starts today's work and resumes the live session.
  `SCHED-07` (rest-day planning) is the next dependency-ready schedule item
  and needs its own product rules (how rest is marked and whether it counts
  in the week) before it starts.
- **2026-09-15 (revision 66):** Claimed `SCHED-07` for Claude and resized
  it to `L`, since it stores a new routine field. The owner delegated its
  rules, decided as follows. Rest is marked by weekday on each weekly
  routine, as part of that program; per-date rest would need the
  schedule-instance model of `SCHED-04`. A planned rest day reads "Rest day"
  on the schedule before and after today and is never "not logged"; a
  session of that routine on the day replaces it. Rotations get no rest
  days: they advance only on completed sessions, so a rest day inside the
  cycle would need an invented "rest done" state, and rest in a rotation is
  simply the days you do not train.
- **2026-09-15 (revision 67):** Shipped `SCHED-07` and released its claim.
  Weekly routines carry planned rest days, and the schedule shows them. The
  remaining schedule items need the schedule-instance model (`SCHED-04`,
  `SCHED-05`) or a monthly view with its own consistency rules (`SCHED-02`).
- **2026-09-15 (revision 68):** Claimed `PROG-05` and `SCHED-02` together
  for Claude: one monthly consistency calendar, shown on the Schedule page
  and in Progress, built from the same week rules. It needs no new rule:
  "missed" in `PROG-05` reads "Not logged" as decided for `SCHED-01`, and its
  "recovery days" are the planned rest days of `SCHED-07`. Frontend only.
- **2026-09-15 (revision 69):** Shipped `ACH-04` and released its claim.
  Published `@sunsteel/contracts@0.32.0` and extended the existing achievement
  read with one next fixed milestone per category, derived from the same
  verified totals as the earned ledger. The presentation keeps catalog order,
  uses exact evidence without percentages or deadlines, and preserves planned
  loads and recovery as the safety boundary. The local sweep also exposed and
  closed the historical-event writer's transaction timeout by batching its
  idempotent inserts. Production passed the complete responsive/theme matrix,
  and all 395 local regression cases passed across the documented recovery
  runs. No migration or one-off backfill was needed. `ACH-05` is the next
  dependency-ready achievement slice.
- **2026-09-15 (revision 70):** Shipped `PROG-05` and `SCHED-02` and
  released their claims. One monthly calendar serves the Schedule page and
  Progress, completing the Progress destination group. The remaining
  schedule items need the schedule-instance model (`SCHED-04`, `SCHED-05`)
  or belong to push notifications.
- **2026-09-15 (revision 71):** Claimed `ROUT-08` for Claude. The owner
  delegated its rules, decided as follows. A version is an immutable copy of
  the whole routine setup (schedule mode, rest days, days, exercises, sets,
  loads, progression, rest and notes), saved intentionally with an optional
  name; restoring first saves the current setup as a "Before restore"
  version, so a restore can always be undone. A version is compared with the
  current routine day by day, exercises matched by exercise within a day,
  because every edit replaces the stored days. Restoring replaces the setup,
  loads included (undoing later automatic progression, which the
  confirmation shows), keeps the routine, its favorite flag and its history,
  and is refused while a session of the routine is in progress. A routine
  keeps at most 20 versions, and any can be deleted. Contracts, backend
  (a new table) and frontend.
- **2026-09-15 (revision 72):** Shipped `ROUT-08` and released its claim.
  Routines keep intentional versions that can be compared and restored.
  `ROUT-09` (training blocks and deloads) is now dependency-ready and needs
  its own product rules before it starts.
- **2026-09-15 (revision 73):** Claimed `SCHED-06` for Claude. `ROUT-11`
  already delivered weekday schedules and non-weekly rotations, so the owner
  chose to scope the rest as rotations on fixed training weekdays; the owner
  delegated the details, decided as follows. A rotation may choose the
  weekdays it trains on (for example Monday, Wednesday and Friday); without
  any it behaves as before, on any day and without dates. With weekdays, the
  Schedule projects the rotation from today: its next day falls on the first
  training weekday from today without a session of the routine, and the
  following days on the training weekdays after it. A past training weekday
  without a session of the routine reads "Not logged", with no day name,
  because which day it would have been is not known. Any rotation day can
  still be started on any day, and the rotation still advances only on
  completed sessions. Weekly routines are unchanged, rotations keep no rest
  days (the other weekdays are simply not training days), the routine card
  and dashboard give the next day its date, and versions (`ROUT-08`) keep
  the weekdays. Contracts, backend and frontend.
- **2026-09-15 (revision 74):** Shipped `SCHED-06` and released its claim.
  Rotations can train on chosen weekdays and appear on the schedule. The
  remaining schedule items need the schedule-instance model (`SCHED-04`,
  `SCHED-05`) or belong to push notifications.
- **2026-09-15 (revision 75):** Claimed `SCHED-04` for Claude. The owner
  delegated its rules, decided as follows. It introduces the
  schedule-instance model as per-date overrides on a routine, so `SCHED-05`
  (skip and postpone) can add its own kind later. An occurrence is a weekly
  routine's planned day on one date; it can be moved to another date up to
  six days before or after, neither date in the past, and not onto a date
  where that routine is already planned. Moving again changes the target;
  undoing restores the plan. Rotations are not moved: since `SCHED-06` their
  plan re-flows from the next day on its own. On the Schedule the original
  date reads "Moved to <day>" (never "not logged") and the target date
  carries the planned day, marked as moved, with Start when it is today; a
  session of the routine on either date completes the occurrence, and a
  moved day on a planned rest day replaces the rest. The dashboard follows
  the moves for today. Overrides are stored by calendar date, not by routine
  day, because routine edits replace the days. Contracts, backend (a new
  table) and frontend.
- **2026-09-15 (revision 76):** Shipped `SCHED-04` and released its claim.
  One planned workout can move to another day through the new per-date
  override model. `SCHED-05` (skip and postpone) is now dependency-ready and
  adds its own override kinds; it needs its rules decided before it starts.
- **2026-09-15 (revision 77):** Claimed `SCHED-05` for Claude. The owner
  delegated its rules, decided as follows. Skipping adds a `SKIP` kind to
  the `SCHED-04` overrides: a weekly routine's planned day marked skipped
  reads "Skipped" in neutral styling, never "not logged", and can be undone.
  It can be marked ahead of time or, to explain a day that passed, for an
  occurrence up to six days back; a session of the routine on that date
  still counts. Skipping a moved workout skips the occurrence, and a skipped
  day is free for another workout of the routine to move onto. Postponing
  is a one-tap move to the first free day after where the workout sits,
  under the `SCHED-04` limits, so it needs no kind of its own. Rotations
  are neither skipped nor postponed: their plan re-flows on its own. The
  dashboard drops a workout skipped for today. Contracts, backend (a new
  enum value) and frontend.
- **2026-09-15 (revision 78):** Shipped `SCHED-05` and released its claim.
  A planned weekly workout can be skipped, ahead of time or for a day that
  passed, and postponed in one tap; a skip never reads as not logged. Every
  `SCHED-*` item has now shipped; the rest of group 9 is push notifications.
- **2026-09-15 (revision 79):** Claimed `NOTIF-01` for Claude. The owner
  delegated its rules, decided as follows. The center is a Notifications page
  reached from a bell in the top bar, whose glyph and accessible name show
  unread notifications; restrained sidebar and schedule indicators stay with
  `NAV-05`. Stored notifications carry read state and are gathered, when the
  list is read, from records that already exist rather than hooked into each
  write path: an achievement earned live (never one recognized from history),
  one note per finished session that set records or changed loads, and a new
  follower. Each source reaches back at most 30 days. Opening a notification
  marks it read, and all can be marked read at once. Above them, a Today
  section lists what can be acted on now, without read state: today's planned
  workouts with Start and a live session with Resume. Push delivery,
  reminders and per-category controls stay with `NOTIF-02` to `NOTIF-05`.
  Contracts, backend (a new table) and frontend.
- **2026-09-15 (revision 80):** Shipped `ACH-05` and released its claim.
  Published `@sunsteel/contracts@0.42.0` and added a bounded, local-calendar
  comeback derivation over immutable completed-session events. The UI rewards
  returning after time away without changing streaks, ranks or milestones.
  Contracts, backend and frontend CI passed; Railway and Vercel deployed. The
  production account's actual comeback state remains unverified because the
  localhost session was not valid on the Vercel-protected production origin.
- **2026-09-15 (revision 81):** Evaluated `PROF-07` against current contracts,
  backend schema/authorization and both profile surfaces. Corrected it from `M`
  to `L`: it needs a new ordered selection model plus CT, BE and FE changes.
  PROF-07 now owns the generic typed/public foundation and record selection;
  `ACH-03` owns the earned-medal and rank-title picker on that foundation, so it
  can proceed without a second migration.
- **2026-09-15 (revision 82):** Shipped `NOTIF-01` and released its claim.
  The app has an in-app notification center: a top-bar bell, today's
  actions and read-state updates gathered from records that already exist.
  `NAV-05` (navigation badges) is now dependency-ready; push delivery
  (`NOTIF-08`, `NOTIF-02`) remains the base for reminders and alerts.
